2026-03-01
[11:18:05.0330] <Justin Ridgewell>
[asumu](https://matrix.to/#/@atakikawa:igalia.com) can you link to those remapping slides you did a few weeks ago? I can only find the ones from Dec.

[11:19:27.0744] <Nic>
https://docs.google.com/presentation/d/1xMlkgiE-4PoFyqqJYw5XgZVmb6gMc8gSGqe7JWEC71k/edit?usp=sharing

[11:20:27.0484] <Justin Ridgewell>
Ty


2026-03-02
[23:30:43.0046] <Justin Ridgewell>
Trying to implement the consumer side tests now. What does a `generatedPositionFor()` return when the regular mapping was a range?
```
const tracer = new TraceMap({
  version: 3,
  sources: ['input.js'],
  sourceRoot: 'https://astexplorer.net/',
  names: ['foo'],
  mappings: 'AAAA', // (0, 0) -> (0, 0)
  rangeMappings: 'B', // first mapping is range
});

const pos = generatedPositionFor(traceMap, {
  source: 'input.js',
  line: 2,
  column: 2,
});
// ^?
```

[23:31:16.0855] <Justin Ridgewell>
Do ranges cover original source code until the next mapping position in the original file?

[23:59:07.0715] <Justin Ridgewell>
The ranges could be differently sized. Eg, `AAAA,BAee` only covers 0 lines/1 column in generated code, but 15 lines/15 columns in original code.

[00:47:32.0707] <Justin Ridgewell>
This is even worse:
```
mappings: 'AAAA,BAee'
rangeMappings: 'B'
```


[00:48:52.0580] <Justin Ridgewell>
Doing `generatedPositionFor({ line: 1, column: 10 })` would return `{ line: 1, column: 10 }` offset from the range mapping, which is beyond the start of the `BAee` segement.

[00:49:23.0188] <Justin Ridgewell>
We have to cap these so that only 1 column (in this case) of source code is reverse mapped.

[12:40:55.0649] <asumu>
I think the semantics that makes sense is that ranges are inclusive of the start point but exclusive of the end point, which makes the ranges consistent because the original position mapped by the end point doesn't matter.

[12:43:09.0114] <asumu>
So `'AAAA,BAee'` maps just one point in the range. If it was `AAAA,CAee` it would map the `[0, 2)` columns

[12:45:48.0202] <asumu>
It's arguably a little weird when the mapping spans multiple lines though, because you don't know how long any particular line is from just the source map.

[12:48:29.0636] <asumu>
* I think the semantics that makes sense is that ranges are inclusive of the start point but exclusive of the end point, which makes the ranges consistent because the original position mapped by the end point doesn't matter (and the length of the range is determined only by the generated positions).

[12:51:24.0487] <asumu>
For this case, the range mapping doesn't have an end point so it's a bit weird. I've been thinking it might be good to make that an (optional) error case. An alternative would be to assume the range goes on until the end of the file, but I'm not sure that's intuitive.

[12:53:59.0712] <asumu>
Right now the draft spec doesn't explicitly cover that case actually, so by default it does the "until end of file" behavior

[13:02:36.0452] <Nic>
One of the goals was that for a ts-blank-space use case we'd be able to use a static source map that can be hard coded, so it'd be great to keep it as having an auto-close at the end of the file by default

[13:17:16.0949] <Justin Ridgewell>
This was specifically about the reverse mappings. I agree with the regular `originalPositionFor()`, the range ends when the next mapping begins.

[13:17:47.0126] <Justin Ridgewell>
But when going the reverse direction, how much of the generated file is covered from a ranged original mapping -> generated file

[13:18:08.0833] <Justin Ridgewell>
In this case, I think we can only allow 1 column of the source file to be mapped

[13:18:54.0093] <asumu>
Yeah, I think only the positions that would be reachable in the generated->original lookup should be covered in the other direction too. Is that what you mean?

[13:19:02.0088] <Justin Ridgewell>
Actually, let's pretend it's 2 columns, for the sake of example

[13:20:10.0990] <Justin Ridgewell>
So doing `generatedPositionFor({ line: 1, column: 0 })` would fine the reverse range mapping  exactly.
`generatedPositionFor({ line: 1, column: 1 })` would calculate an offset from the reverse range mapping.
`generatedPositionFor({ line: 1, column: 2 })` would find the reverse range mapping, determine that the range is ended before this source point, then return `null`

[13:21:23.0557] <Justin Ridgewell>
For that last case, the GLB lookup would find the the range mapping, because it's less than the lookup point

[13:21:29.0477] <Justin Ridgewell>
But we can't allow it to return

[13:21:55.0305] <Justin Ridgewell>
Because returning `{ line: 1, column: 2 }` in the generated file would invalidate the existing `BAee` mapping in the generated file.

[13:22:29.0325] <asumu>
Oh I see, yes I agree it should be capped in that case.

[13:22:44.0839] <Justin Ridgewell>
(And that should have been `CAee`, and now `Eaee` to cover 2 columns)

[13:22:55.0867] <Justin Ridgewell>
* (And that should have been `CAee`, and now `Eaee` to cover 2 columns, VLQ is hard sometimes)

[13:23:52.0459] <asumu>
That particular thing isn't speced out right now in the draft because we don't have the original to generated lookup algorithm in the spec in general

[13:27:58.0000] <asumu>
* That particular thing isn't speced out right now in the draft because we don't have the original to generated lookup algorithm in the spec in general, but the spec machinery should probably also look up the next mapping to check that the offset is in the range.

[13:28:09.0131] <asumu>
* That particular thing isn't speced out right now in the draft because we don't have the original to generated lookup algorithm in the spec in general, but the spec machinery would probably also look up the next mapping to check that the offset is in the range.

[13:36:37.0375] <Justin Ridgewell>
This is complicated, because we also need to verify line-wise

[13:37:33.0486] <Justin Ridgewell>
My array per-line works out really well for some inputs, but checking other lines becomes a pain point.


2026-03-04
[07:46:32.0560] <Simon Zünd>
I wrote (or let AI write) a small script that outputs the scope boundaries of V8 of various ECMAScript snippets: https://crrev.com/c/7633389. Anybody interested in adapting this for Firefox and Safari? I'm currently working on some spec text that how generated ranges should look like for generated JavaScript and I'd be interested what engines currently think the scope boundaries are. Maybe Holger Benl  ? :)

[07:47:00.0166] <Simon Zünd>
* I wrote (or let AI write) a small script that outputs the scope boundaries of V8 of various ECMAScript snippets: https://crrev.com/c/7633389. Anybody interested in adapting this for Firefox and/or Safari? I'm currently working on some spec text on how generated ranges should look like for generated JavaScript and I'd be interested what engines currently think the scope boundaries are. Maybe Holger Benl  ? :)

[07:55:55.0022] <Holger Benl>
Unfortunately Firefox currently doesn't provide the scope boundaries via the debug protocol.


2026-03-05
[22:14:25.0598] <Simon Zünd>
Any chance we could run these snippets with some internal test runner and log the boundaries? e.g. d8 (the v8 shell) has `--print-scopes`.

The reason is that for the spec text I'd allow the maxium boundary possible but it would be nice if we could recommend where generators put range start/end if all engines agree somewhat.

[01:46:02.0425] <Simon Zünd>
I have a draft up how we could give generators some boundaries where to put start/end for generated ranges for JS: https://tc39.es/ecma426/pr/249/#sec-generated-range-record-js. Previously I did this via a record and SDO over ECMA-262 productions but I think a simple table like the PR does should be sufficient. We can discuss in the next scopes meeting :)

