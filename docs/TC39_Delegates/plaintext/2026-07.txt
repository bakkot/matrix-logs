2026-07-01
[09:29:58.0285] <Rob Palmer>
Reminder:  Please take 30 seconds to fill in this interest survey for September 2026 meeting in Tokyo 🇯🇵 

- https://github.com/tc39/Reflector/issues/585

Thanks to the 30 folk who have already replied.


2026-07-02
[07:59:34.0827] <Chris de Almeida>
Chairs have reviewed the Interest Survey results and concluded the Tokyo 2026 meeting has sufficient interest. So the meeting is now confirmed 🎉


2026-07-08
[08:08:43.0415] <bakkot>
reminder: slightly less than two days until deadline to add things to the agenda for advancement at the upcoming meeting


2026-07-10
[23:24:01.0694] <bakkot>
Justin Ridgewell can I take over https://github.com/tc39/proposal-private-declarations/

[23:24:59.0054] <Justin Ridgewell>
Sure

[23:25:52.0961] <Justin Ridgewell>
@littledan was interested in the design space for adding privates to objects. He wanted to change the syntax for reusing the outer private in a class/object declaration

[23:26:54.0399] <bakkot>
yeah I want it to be `[#x]`

[23:27:11.0768] <bakkot>
I think that's pretty natural now that we have `#x in foo`

[23:29:39.0084] <Justin Ridgewell>
That's what I suggested. Dan was leaning towards `(#x) = …`

[23:38:10.0194] <bakkot>
ehhhhh

[23:38:26.0599] <bakkot>
If we'd done `(#x) in foo` I could see it

[23:38:54.0255] <bakkot>
but `#x in foo` already introduces the notion of `#x` in expression position so I think we should just roll with that

[23:39:27.0361] <Justin Ridgewell>
Dan's concern was to have a uniform syntax for both. I don't remember exactly why `[#x]` wouldn't work.

[23:39:39.0915] <Justin Ridgewell>
* Dan's concern was to have a uniform syntax for both classes and objects. I don't remember exactly why `[#x]` wouldn't work.

[23:40:01.0852] <Justin Ridgewell>
Off the top of my head, I think it could work?

[23:47:28.0103] <bakkot>
yeah it should work fine

[23:49:39.0782] <bakkot>
there's this one horrible edge case where you can today already write

```
class A{
  #x;
  prop = foo
  [#x in y]
}
```
(which is parsed as `prop = foo[#x in y]`)

but that happens whenever you have any computed field, not just with this new form

[23:50:25.0390] <bakkot>
threw together some slides, not gonna ask for stage 2 at this meeting but I'll bring it up https://docs.google.com/presentation/d/1Cmk_vpi6yID9dJjZqqSfKMhvYmO2xXGRakAxcXvHq4s/edit?usp=sharing

[00:19:02.0793] <Justin Ridgewell>
Friend functions would be very useful for pipelines 😉

[05:45:58.0763] <nicolo-ribaudo>
I find it a bit weird to use the computed property syntax on declaration but not on access

[09:36:14.0588] <bakkot>
I agree but /shrug

[09:36:19.0179] <bakkot>
better than `outer #x` IMO

[09:36:42.0347] <bakkot>
or `with #x` or whatever


2026-07-14
[08:02:30.0976] <Chris de Almeida>
We still have a good amount of capacity available for next week's meeting. Please add your topics! 🙏


2026-07-17
[08:25:02.0738] <eemeli>
Has the preliminary schedule been posted yet?

[10:34:54.0682] <Chris de Almeida>
draft schedule is up, link can be found on the reflector issue:  https://github.com/tc39/Reflector/issues/584

[13:45:52.0514] <Chris de Almeida>
gentle reminder to please create PRs for changes to agendas, especially when we are close to the meeting start.  otherwise we may not notice new/changed things.  thank you 🙏


2026-07-20
[06:54:52.0847] <Chris de Almeida>
👋

[06:55:12.0657] <Chris de Almeida>
📢 plenary begins in ~1 hr

[08:02:11.0275] <nicolo-ribaudo>
Be careful because Teams seems to default to having the mic on when you join

[08:05:03.0174] <Aki>
yeah it does, sorry about the crunching, I was sure i had muted

[08:08:18.0898] <Michael Ficarra>
29th Sept to 21 Oct is a long meeting!

[08:10:20.0830] <eemeli>
They did advertise that length already at the last meeting, though.

[08:15:36.0511] <Chris de Almeida>
no crunching heard. I guess your mic has *Cronch Reduction™️*

[08:15:43.0705] <Chris de Almeida>
* no crunching heard. I guess your mic has __Cronch Reduction™️__

[08:15:52.0613] <Aki>
😮‍💨

[08:24:31.0529] <nicolo-ribaudo>
Chris de Almeida: I can help with notes until 66 minutes from now (30 min before lunch)

[08:24:46.0326] <Chris de Almeida>
thank you!  🙏

[08:25:18.0570] <nicolo-ribaudo>
bakkot: I feel like this new version of the model is way to happy to add commas everywhere

[08:25:24.0544] <nicolo-ribaudo>
* bakkot: I feel like this new version of the model is way too happy to add commas everywhere

[08:28:17.0071] <bakkot>
Yeah I've been noticing that

[08:30:13.0869] <bakkot>
I suspect it's an artifact of the fact that I'm using the provisional rather than finalized tokens (for latency reasons), which suggests it might be fixable in the wrapper

[08:30:40.0105] <bakkot>
google docs does pretty consistently offer to fix those so it's just two clicks to fix at least

[08:34:23.0697] <Justin Ridgewell>
OMG page is… busy today.

[08:34:29.0132] <Justin Ridgewell>
Definitely a Google outage.

[08:35:49.0362] <Justin Ridgewell>
* Definitely a Docs outage.

[08:36:32.0457] <Ashley Claymore>
10 Min Doc Fixing Break 🫖

[08:37:38.0299] <Ashley Claymore>
Docs are (maybe) back

[08:42:18.0065] <Justin Ridgewell>
Open 2 tabs?

[08:53:00.0605] <rbuckton>
I will be unable to attend the remainder of plenary due to a family emergency. I've spoken to Devin Rousso about my intent to block advancement to Stage 2 for Declarations in Conditionals as my concern from the November 2025 plenary has not been met. There appears to be a perspective among some in committee that Declarations in Conditionals may open the door to a different form of pattern matching than what has been discussed in the Pattern Matching champions group and as a result the two proposals are likely to conflict in some areas. As the pattern matching proposal wholly covers the capabilities introduced by Declarations in Conditionals, though with a different syntax, I still feel it is imperative that we discuss the cross-cutting concerns amongst the champions of both proposals before Declarations in Conditionals advances to Stage 2. That discussion has yet to happen since November, so my concern still stands. Without this discussion, I am unconvinced this is a preferred solution, per the description of Stage 2.
If necessary, I've asked someone from the Pattern Matching champions to speak on my behalf if advancement is requested.

[09:12:39.0100] <nicolo-ribaudo>
As I'm next on the agenda -- my mic is being annoying right now, hopefully it's solved in the next minute

[09:13:04.0433] <Michael Ficarra>
reminder: there's a default saved topic in the ♻️ button on TCQ that expresses support

[09:13:25.0728] <nicolo-ribaudo>
Ok all good

[09:14:47.0412] <bakkot>
oooo

[09:18:33.0436] <Chris de Almeida>
we will need a new notes helper in ~10 minutes, for the final 30 minutes of this session

[09:18:53.0857] <bakkot>
I can do

[09:19:03.0555] <Chris de Almeida>
thank you!

[09:21:23.0131] <nicolo-ribaudo>
Ohh https://proposals.fyi is cool

[09:22:57.0699] <Aki>
😬 I already have some amount of this data in a structured form…

[09:27:31.0886] <Aki>
(I use mine for v secretarial purposes so this isn't particularly redundant)

[09:28:06.0427] <Aki>
incrementing IDs or UUIDs pleeeeeease

[09:33:20.0794] <eemeli>
I did not expect to need to consider Classification by Compilation in this context.

[09:42:04.0729] <jmdyck>
should meeting notes drop filler words, e.g. "like"

[09:42:24.0992] <ryzokuken>
I think so

[09:42:33.0225] <ryzokuken>
the notes don't have to be verbatim

[09:42:53.0593] <jmdyck>
ok thanks

[11:19:39.0505] <nicolo-ribaudo>
Oh damn I was meant to replace that (???) with something for Safari and I forgot to check if it's in TP

[11:31:10.0420] <nicolo-ribaudo>
ljharb: If you get the slides from the agenda, it's the first section after the "implementation status"

[11:31:12.0057] <ljharb>
can someone post the bug here so i can look at it?

[11:31:14.0168] <ljharb>
ah k thanks

[11:31:14.0723] <nicolo-ribaudo>
The one marked a #85

[11:31:55.0666] <ljharb>
in the future, please indicate on the agenda that a decision will be made - "updates" isn't an item where consensus is requested.

[11:40:49.0690] <dminor>
Yoshi is implementing import defer in SpiderMonkey

[12:03:40.0145] <ljharb>
totally agree that linters can impose any mental models they want, including that "things that won't actually be deferred shouldn't have `defer`"

[12:04:59.0031] <Bradford Smith>
One can say that 'import defer' isn't guaranteed, but if my customers within Google were using 'import defer' and it didn't actually defer when it technically could have, I'm sure they would be quite unhappy with my team, because it could have significant user experience impact. That's what 'import defer' is trying to help with after all. Their reaction would likely be "why define this at all if we cannot count on it?"

[12:05:42.0850] <ljharb>
it can't ever be counted on.

[12:05:48.0445] <ljharb>
that's just the nature of ESM

[12:06:03.0774] <ljharb>
so the answer is "because it's removing the requirement of eager loading"

[12:06:07.0746] <ljharb>
not because it's forcing delayed loading

[12:06:10.0835] <bakkot>
in browsers specifically it can in at least some cases be counted on

[12:06:24.0477] <ljharb>
only with caveats, because it could always have been loaded prior

[12:06:35.0657] <bakkot>
not if you control the whole application, as is common

[12:07:15.0899] <nicolo-ribaudo>
I think even in this case it's fulfilling its promise of making `import defer * as ns from "foo"` not cause evaluation (module TLA). It's just not guarantee that the evaluation will then happen when you do `ns.something`

[12:07:23.0460] <nicolo-ribaudo>
* I think even in this case it's fulfilling its promise of making `import defer * as ns from "foo"` not cause evaluation (modulo TLA). It's just not guarantee that the evaluation will then happen when you do `ns.something`

[12:08:28.0986] <nicolo-ribaudo>
<del>fulfilling its promise</del><ins>keeping its moral duty</ins>, no promises involved!

[12:12:48.0840] <keith_miller>
I forget, did we investigate not calling a getter on `then` in the spec for web compatibility? I assume some then-able implementation uses getters for `then`?

[12:12:59.0372] <nicolo-ribaudo>
Svelte does iirc

[12:13:12.0243] <nicolo-ribaudo>
For tracking some side effects

[12:14:12.0030] <ljharb>
any promise polyfill will use getters to test compliance, i'm quite certain it'll be wildly web incompatible to change that

[12:29:32.0092] <Olivier Flückiger>
mgaudet:  hmm, so maybe this case would need a "Assert does not run uc" then that would basically override the [uc] annotation and error if we do end up in user code?

[12:30:15.0114] <bakkot>
we do also have that annotation internally although "error if we do end up in user code" is not something that we can do statically

[12:30:18.0931] <mgaudet>
Yeah, I'm not entirely sure if we can talk about the UC annotation as part of the 'normative text' 

[12:30:39.0714] <Olivier Flückiger>
right, I guess what I was trying to say is that we should make it to be so

[12:30:55.0154] <Olivier Flückiger>
(not saying you should do it in this proposal)

[12:30:56.0048] <mgaudet>
new proposal :D 

[12:31:10.0082] <mgaudet>
"user-code-assertions"

[12:31:11.0528] <bakkot>
IMO it is an editorial error if the UC annotation is wrong, same as any other violated assertion in spec text

[12:31:20.0373] <bakkot>
* IMO it is an editorial error in the spec if the UC annotation is wrong, same as any other violated assertion in spec text

[12:31:35.0041] <Olivier Flückiger>
does engine262 enforce it then?

[12:31:39.0967] <Chris de Almeida>
fascinating how often the JSON spec gets downloaded

[12:31:43.0165] <nicolo-ribaudo>
No

[12:31:45.0934] <Chris de Almeida>
The People yearn for more JSON

[12:32:00.0941] <bakkot>
would be fun to make it do so though!

[12:33:01.0429] <Michael Ficarra>
I wonder why 402 doesn't show up in those download counts

[12:34:06.0008] <eemeli>
Probably because so many of its downloads are likely from https://tc39.es/ecma402/, which isn't counted by ECMA at all.

[12:37:46.0560] <Justin Ridgewell>
We'll need to get a cake next time we see Kevin

[12:38:13.0292] <Chris de Almeida>
"Kev in is Stage 4"

[12:39:19.0617] <Justin Ridgewell>
Tom Poral 😆

[12:41:13.0571] <bakkot>
I feel like this should come with a free "advance one proposal by fiat" card

[12:42:21.0889] <Aki>
n.b. that beautiful thing from ISO-IEC/JCT 1 is *huge*

[12:43:40.0268] <Chris de Almeida>
need banana for scale

[12:43:44.0007] <Aki>
i agree

[12:43:57.0623] <Chris de Almeida>
these blank slides really build the tension

[12:44:06.0681] <Aki>
right? i love it

[12:44:21.0654] <Chris de Almeida>
hits me right in the anxiety

[12:45:51.0403] <Chris de Almeida>
next year swag:  we need to finally realize the dream of the TC39 socks

[12:49:52.0215] <nicolo-ribaudo>
MARK it's es2015

[12:50:11.0995] <Aki>
I have made socks as swag before that I was so proud of. fine knit cotton/nylon instead of dye sublimation and polyester.

[12:50:20.0114] <Michael Ficarra>
ES6 = ES2015

[12:50:46.0645] <nicolo-ribaudo>
So what if we go sleep earlier instead

[12:50:51.0539] <nicolo-ribaudo>
Damn

[12:53:01.0338] <mgaudet>
bakkot & nicolo-ribaudo here's an attempt at the host hook: https://github.com/tc39/proposal-thenable-curtailment/pull/19 Note, I've separated out the module namespace check as it wouldn't be covered by a host hook implementation.

[12:53:35.0424] <nicolo-ribaudo>
I would put the namespace check together with the proxy check, and similarly do it based on an internal slot

[12:54:01.0976] <nicolo-ribaudo>
Note that it's only deferred namespaces that would cause user code, but it's probably good to make them behave coherently with each other

[12:54:36.0766] <mgaudet>
oh they do have some! (sorry, was reading https://tc39.es/ecma262/#sec-module-namespace-objects and didn't realize https://tc39.es/ecma262/#sec-module-namespace-exotic-objects had the slots) 

[12:55:19.0388] <nicolo-ribaudo>
And maybe add an "Assert: the following step does not run user code", that we currently don't have, but so that in the future editors can fix things if we forget to list objects there without considering it to be a normative change :)

[12:55:57.0165] <nicolo-ribaudo>
* And maybe add an "Assert: the following step does not run user code", that we currently don't have, but so that in the future editors can fix things if we forget to list new objects there without considering it to be a normative change :)

[13:01:49.0769] <mgaudet>
man the module namespace object definition is kinda funky; "An object is a <dfn id="module-namespace-exotic-object" variants="module namespace exotic objects">module namespace exotic object</dfn> if its [[GetPrototypeOf]], [[SetPrototypeOf]], [[IsExtensible]], [[PreventExtensions]], [[GetOwnProperty]], [[DefineOwnProperty]], [[HasProperty]], [[Get]], [[Set]], [[Delete]], and [[OwnPropertyKeys]] internal methods use the definitions in this section" 

I guess I copy-paste the list eh? 

[13:01:52.0358] <hax (HE Shi-Jun)>
Set.prototype.take() seems not very useful because set.delete already returns boolean.

[13:02:58.0584] <nicolo-ribaudo>
Oh well, then it's probably fine as-is, modulo reordering

[15:34:35.0626] <Chris de Almeida>
📢  the schedule has shifted a bit  👀


2026-07-21
[07:56:43.0551] <Chris de Almeida>
meeting start is imminent!

[08:02:02.0441] <Michael Ficarra>
oooh lower latency on the transcription will be great!

[08:03:57.0030] <bakkot>
I have no idea why that would be

[08:04:06.0761] <bakkot>
unless it's like, yesterday google docs was slow, which I could totally believe

[08:08:25.0327] <Chris de Almeida>
it is noticeably better than last meeting

[08:08:39.0483] <Chris de Almeida>
just my observation 🤷

[08:10:57.0667] <Justin Ridgewell>
I don't think I need `set.getAndDelete()`, but have wanted `set.getAndInsert(): boolean` a few times

[08:11:08.0377] <Justin Ridgewell>
To both check if it's currently in, and insert it if it's not.

[08:11:45.0226] <Justin Ridgewell>
Mainly to guard `if (!set.getAndInsert()) { expensive() }`

[08:12:53.0110] <Justin Ridgewell>
Really `add()` should have returned the current boolean status all along…

[08:17:20.0916] <nicolo-ribaudo>
What if we had a special flag on objects to make them falsish, so that the Map returned by `.add()` turns into falsish for the first subsequent boolean check if the value was already there 🧠

[08:18:27.0815] <rbuckton>
> <@jridgewell:matrix.org> Really `add()` should have returned the current boolean status all along…

I end up calling it `tryAdd`

[08:19:07.0988] <Justin Ridgewell>
That works too

[08:20:33.0743] <bakkot>
just do your side effects in `getOrInsertComputed`

[08:21:08.0585] <bakkot>
`set.getOrInsert(v, () => { expensive(); return newValue })`

[08:21:09.0545] <Justin Ridgewell>
That's only `Map`, right?

[08:21:43.0052] <ljharb>
for take, what's the proposal name at stage 1 (that describes the problem statement)?

[08:21:45.0855] <bakkot>
oh, sure

[08:21:49.0518] <Olivier Flückiger>
nicolo-ribaudo: to support this also for the declaration in conditionals it would have to be passed through assignments thoug

[08:21:59.0367] <Olivier Flückiger>
* nicolo-ribaudo: to support this also for the declaration in conditionals it would have to be passed through assignments though

[08:24:21.0723] <Michael Ficarra>
`parseFrozen` sounds like I need to pass `Object.freeze(Object("{ ... }"))`

[08:24:36.0028] <Michael Ficarra>
like somehow JSON.parse will mutate its input

[08:24:53.0867] <Michael Ficarra>
that said, I still think it's better than using an option for this

[08:25:44.0859] <ljharb>
cc Devin Rousso

[08:28:15.0306] <Devin Rousso>
im not sure what you mean? 😅

[08:28:27.0498] <Devin Rousso>
i think `getAndDelete` was what folks liked

[08:28:43.0693] <ljharb>
that's the method name. but what's the problem it's solving? stage 1 is about a problem statement, not a solution/API

[08:29:26.0967] <Michael Ficarra>
that's also the problem it's solving, which is why we suggested it as the method name

[08:29:27.0578] <Devin Rousso>
oh! gotcha i understand what you're asking

[08:29:55.0285] <Devin Rousso>
it's being able to in a single operation "take" things out of `Map`

[08:30:10.0141] <Devin Rousso>
i.e. get the value for a given key AND delete the key at the same time

[08:30:17.0262] <ljharb>
i'm looking for something concise to title the proposal in the proposals repo :-)

[08:30:49.0447] <Michael Ficarra>
agh, Arrays still having Array.prototype is unfortunate

[08:30:52.0659] <Devin Rousso>
i described it as "Read a value and remove it in the same step" in the proposal repo

[08:31:41.0560] <Michael Ficarra>
Map read and remove?

[08:31:54.0084] <bakkot>
primitives still have their prototypes too

[08:32:28.0181] <bakkot>
arrays we could _conceivably_ make a `FrozenArrayPrototype` which is just a copy with all the same methods but frozen + null protot, but we can't do that for primitives 

[08:33:22.0998] <ljharb>
SGTM

[08:33:36.0704] <ljharb>
please bounce the proposal repo into tc39-transfer and i'll take care of it :-)

[08:33:40.0726] <Michael Ficarra>
that said, the relevant ES terms (from Map.prototype) are already "get" and "delete", so I would personally go with those

[08:37:22.0878] <Justin Ridgewell>
Didn't we discuss packed arrays recently? As long as the array has no holes, it shouldn't matter that it has a prototype.

[08:40:35.0255] <Olivier Flückiger>
not if you read a named property on the array

[08:44:03.0573] <nicolo-ribaudo>
We could have options presets as static properties of JSON

[08:44:17.0252] <nicolo-ribaudo>
Like, `JSON.parse(..., JSON.FROZEN_AND_NULL_PROTO)`

[08:44:35.0125] <nicolo-ribaudo>
Well, with a shorter name than `JSON.FROZEN_AND_NULL_PROTO`

[08:44:44.0899] <nicolo-ribaudo>
Something that's much simpler to type than spelling out the options bag

[08:46:58.0075] <hax (HE Shi-Jun)>
It's look like PHP :P

[08:47:07.0041] <nicolo-ribaudo>
bakkot Right now attributes only support strings, but the idea is that they are easily extendable to support primitives that have literal syntax

[08:47:31.0796] <nicolo-ribaudo>
(the only reason they don't is due to lack of use cases so far)

[08:48:53.0892] <bakkot>
great, yeah

[08:50:21.0432] <hax (HE Shi-Jun)>
I heard dog barking...?

[08:51:02.0059] <nicolo-ribaudo>
It's unfortunately James's dog

[08:51:02.0613] <bakkot>
very loud dogs yes

[08:51:03.0999] <Michael Ficarra>
oh god

[08:51:08.0170] <nicolo-ribaudo>
oh dog

[08:56:11.0027] <James M Snell>
extremely bad timing, sorry about that

[08:57:40.0472] <bakkot>
https://www.jasnell.me/posts/fetch-needs-error-codes

[08:58:41.0978] <eemeli>
Historically, YAML libraries' approach to parse-immutable-ish concerns has been to define something like `JSON.parseSafe` and ask everyone to use that instead of `JSON.parse`.

I don't think this is necessarily a good idea, but it's the closest historical precedent to the proposed change.

[09:10:32.0146] <eemeli>
I don't think anyone has supported frozen without a null proto, so it should be fine for the shorter name to be e.g. `JSON.Frozen`.

[09:13:39.0862] <Ashley Claymore>
yeah, and JSON.parse always creates packed arrays

[09:14:53.0782] <Michael Ficarra>
the reason it matters is that someone will want to call `.slice` on an array that was parsed from JSON data

[09:16:30.0465] <James M Snell>
https://webidl.spec.whatwg.org/#js-DOMException-specialness

[09:16:40.0841] <Lea Verou>
https://webidl.spec.whatwg.org/#js-DOMException-specialness

[09:17:04.0555] <Lea Verou>
* https://webidl.spec.whatwg.org/#js-DOMException-specialness

> In the JavaScript binding, the interface prototype object for DOMException has its [[Prototype]] internal slot set to the intrinsic object %Error.prototype%, as defined in the create an interface prototype object abstract operation. It also has [[ErrorData]] and [[Stack]] slots, like all built-in exceptions.
> 
> Additionally, if an implementation gives native Error objects other special powers or nonstandard properties (such as line and column properties), it should also expose those on DOMException objects.

[09:19:07.0249] <James M Snell>
Sorry again about the dogs... but at least they're cute 

[09:19:52.0286] <eemeli>
You should take them on a walk.

[09:20:28.0870] <James M Snell>
That one is currently waiting on me to ^

[09:24:28.0238] <hax (HE Shi-Jun)>
I don't like Decl in Cond `if (let x = expr)` have subtle different semantic with other language like Swift 😪

[09:27:20.0101] <bakkot>
hax (HE Shi-Jun): what difference do you mean?

[09:27:29.0333] <bakkot>
fwiw there's already some subtle differences between languages

[09:27:38.0786] <bakkot>
with basically the same form

[09:32:57.0973] <hax (HE Shi-Jun)>
Swift semantic is like `if (let x = expr; x != null)` as Decl in Cond.

[09:36:11.0474] <rbuckton>
Rust can differentiate between a type and a variable in its pattern matching. JS can't. 

[09:36:14.0569] <Nikolaos Papaspyrou>
A declaration cannot fail; pattern matching may either succeed (and declare) or fail.

[09:38:17.0956] <bakkot>
Olivier Flückiger: 

```
if (using resource = getResource(); resource.needsUpdate) {
  resource.update();
} else {
  await postMessage('no updates required');
}
```

[09:44:34.0210] <nicolo-ribaudo>
For comparison, a way to write this if the variable was visible in `else` would be:
```js
let goToElse = false;
{
  using resource = getResource();
  if (resource.needsUpdate) {
    // ...
  } else {
    goToElse = true;
  }
}
if (goToElse) {
  await postMessage('no updates required');
}
```

[09:44:46.0801] <bakkot>
yeah

[09:44:55.0615] <bakkot>
or with a DisposableStack but that's even worse

[09:44:56.0191] <Olivier Flückiger>
yeah, or early return often

[09:45:08.0750] <Olivier Flückiger>
* yeah, or early return (depending)

[09:47:15.0464] <hax (HE Shi-Jun)>
They have overlap, for exmple, `if (let x = ...)` actually are pattern match in other languages like swift/rust/etc. 

[09:53:13.0905] <ljharb>
keith_miller: to be fair, it had one presentation and then the champion left tc39, and it's only been reactivated for ~3 years, and 2 years ago it was seeking stage 2 before we got last-minute objections

[09:53:14.0806] <bakkot>
fwiw I strongly want to use this syntax for extractors as well:

```
if (let Some(x) = val) {
  // only runs if the extractor succeeded
} else {
  // only runs if the extractor failed 
}
```

I think that's a natural extension

Ron really doesn't like this because he thinks it is not general enough for pattern matching, which I think inclines me against pattern matching rather than against this proposal

[09:55:29.0532] <nicolo-ribaudo>
Thoughts about cancelling the fourth day?

[09:55:38.0667] <Ashley Claymore>
Of every week?

[09:55:49.0845] <nicolo-ribaudo>
Yeah 3 consecutive days of work is already enough

[09:56:09.0300] <Clément Pit-Claudel>
Does Temporal support this?

[10:01:39.0670] <Lea Verou>
Just so I understand, we're debating whether this will be TDZ or a syntax error? Because I think it is perfectly meaningful from the author side. Could it be an error? Sure. But so _could_ many other things.

```js
let data = 1;
if (let data = 2) { ... }
else { console.log(data); }
```

[10:01:57.0319] <Lea Verou>
* Just so I understand, we're debating whether this will be TDZ or a syntax error? Because I think it is perfectly meaningful from the author side. Could it be a mistake? Sure. But so _could_ many other things.

```js
let data = 1;
if (let data = 2) { ... }
else { console.log(data); }
```

[10:01:57.0678] <nicolo-ribaudo>
I _think_ we had a ReferenceError for `foo() = 1` and then converted it to SyntaxError

[10:02:14.0002] <Lea Verou>
* Just so I understand, we're debating whether this will be TDZ or a syntax error? Because I think it is perfectly meaningful from the author side. Could it be a mistake? Sure. But so _could_ many other things.

```js
let data = whatever();
if (let data = foo()) { ... }
else { console.log(data); }
```

[10:02:25.0151] <Lea Verou>
* Just so I understand, we're debating whether this will be TDZ or a reference error? Because I think it is perfectly meaningful from the author side. Could it be a mistake? Sure. But so _could_ many other things.

```js
let data = whatever();
if (let data = foo()) { ... }
else { console.log(data); }
```

[10:02:46.0466] <Michael Ficarra>
why isn't the queue being advanced at all?

[10:02:53.0044] <Lea Verou>
* Just so I understand, we're debating whether this will be TDZ or a reference error? Because I think it is perfectly meaningful from the author side. Could it be a mistake? Sure. But so _could_ many other things.

```js
let data = whatever();
if (let data = foo()) { ... }
else { console.log(data); }
```

Making this an error is completely baffling and inconsistent with how scoping works.

[10:03:01.0476] <Lea Verou>
* Just so I understand, we're debating whether this will be TDZ or a reference error? Because I think it is perfectly meaningful from the author side. Could it be a mistake? Sure. But so _could_ many other things.

```js
let data = whatever();
if (let data = foo()) { ... }
else { console.log(data); }
```

Making this an error is completely baffling and inconsistent with how scoping works anywhere else.

[10:03:04.0447] <Michael Ficarra>
oooh, that may be right

[10:03:43.0316] <bakkot>
https://github.com/tc39/ecma262/pull/1527

[10:03:55.0829] <Lea Verou>
* Just so I understand, we're debating whether this will be TDZ or a reference error? Because I think it is perfectly meaningful from the author side. Could it be a mistake? Sure. But so _could_ many other things. That's why we have linters.

```js
let data = whatever();
if (let data = foo()) { ... }
else { console.log(data); }
```

IMO making this an error is completely baffling and inconsistent with how scoping works anywhere else.

[10:04:17.0382] <Michael Ficarra>
ooh thanks for the link @bakkot:matrix.org

[10:04:53.0278] <Michael Ficarra>
I have no memory of that PR at all

[10:05:15.0695] <Lea Verou>
* Just so I understand, we're debating whether this will be TDZ or a reference error? Because I think it is perfectly meaningful from the author side and I can see use cases for it. Could it be a mistake? Sure. But so _could_ many other things. That's why we have linters.

```js
let data = whatever();
if (let data = foo()) { ... }
else { console.log(data); }
```

IMO making this an error is completely baffling and inconsistent with how scoping works anywhere else.

[10:05:21.0780] <bakkot>
fwiw I kind of agree but I also kind of think that the code you wrote is baffling too. if people don't try to write that very confusing code then they never run into this proposed error and don't have to learn about it

[10:05:36.0475] <Lea Verou>
* Just so I understand, we're debating whether this will be TDZ or a reference error? Because I think it is perfectly meaningful from the author side and I can see use cases for it. Could it be a mistake? Sure. But so _could_ many other things. That's why we have linters.

```js
let data = whatever();
if (let data = foo()) { ... }
else { console.log(data); }
```

IMO making this an error or TDZ is completely baffling and inconsistent with how scoping works anywhere else.

[10:05:38.0534] <bakkot>
but I would also, personally, leave this up to linters

[10:06:01.0647] <Justin Ridgewell>
Devin Rousso: Did you want a continuation to ask for Stage 2?

[10:06:23.0830] <Lea Verou>
* Just so I understand, we're debating whether this will be TDZ or a reference error? Because I think it is perfectly meaningful from the author side and I can see use cases for it. Could it be a mistake? Sure. But so _could_ many other things. That's why we have linters.

```js
let data = whatever();
if (let data = foo()) { ... }
else { console.log(data); }
```

IMO making this an error or TDZ is completely baffling and inconsistent with how scoping works anywhere else. TDZ implies uninitialized but same scope. This is *not* the same scope.

[10:06:28.0108] <bakkot>
Ron had an objection to stage 2

[10:06:33.0864] <bakkot>
so I don't think there's any point

[10:06:37.0310] <Michael Ficarra>
at some point, I would like to rant about this committee's relationship with linters

[10:06:57.0040] <Jack Works>
> <@bakkot:matrix.org> fwiw I strongly want to use this syntax for extractors as well:
> 
> ```
> if (let Some(x) = val) {
>   // only runs if the extractor succeeded
> } else {
>   // only runs if the extractor failed 
> }
> ```
> 
> I think that's a natural extension
> 
> Ron really doesn't like this because he thinks it is not general enough for pattern matching, which I think inclines me against pattern matching rather than against this proposal

pattern matching gives you precised control over if a property should be matched or should be a declaration, so it may look like

```js
if (val is Some({ match: { x: Some(1) }, let binding })) {}
```

[10:07:02.0952] <Devin Rousso>
i get the sense i won't get stage 2 due to the pattern matching objection so as much as i'd like to probably not ☹️

[10:07:18.0746] <Ashley Claymore>
If something is ambiguous in the language I'd rather it wasn't allowed than I hope I never see it and have to think about it because I'll always get to work on teams that have a lint rule that bans it

[10:07:51.0186] <bakkot>
right, I get why you want that, but that syntax is _so_ much worse

[10:08:06.0487] <Lea Verou>
I agree, but it's no more or less confusing than 

``js
let foo = 1;
{
  let foo = 2;
}
{
  console.log(foo); // logs 1
}
```

which works just fine today. 

[10:08:08.0925] <Michael Ficarra>
let them cook

[10:08:11.0732] <Lea Verou>
* Just so I understand, we're debating whether this will be TDZ or a reference error? Because I think it is perfectly meaningful from the author side and I can see use cases for it. Could it be a mistake? Sure. But so _could_ many other things. That's why we have linters.

``js
let data = whatever();
if (let data = foo()) { ... }
else { console.log(data); }
```

IMO making this an error or TDZ is completely baffling and inconsistent with how scoping works anywhere else. TDZ implies uninitialized but same scope. This is _not_ the same scope.

[10:08:28.0627] <Lea Verou>
* I agree, but it's no more or less confusing than

```js
let foo = 1;
{
let foo = 2;
}
{
console.log(foo); // logs 1
}
```

which works just fine today. 
```

[10:08:38.0027] <Lea Verou>
* I agree, but it's no more or less confusing than

```js
let foo = 1;
{
  let foo = 2;
}
{
  console.log(foo); // logs 1
}
```

which works just fine today.

[10:08:43.0510] <bakkot>
Lea Verou I generally think we should move away from "this existing thing is already confusing so it's ok to have a new confusing thing" as a justification

[10:09:01.0936] <bakkot>
see also "stop coercing things"

[10:09:25.0425] <Lea Verou>
Yes and no. Breaking internal consistency needs a stronger justification, and IMO it's less clear that this exists here.

[10:09:50.0054] <bakkot>
fwiw I don't agree: I think that is very clear, in a way your earlier snippet is not

[10:09:51.0549] <Justin Ridgewell>
Agreed.

[10:09:52.0162] <Lea Verou>
* Yes and no. Breaking internal consistency needs a stronger justification, and IMO it's less clear that this exists here.

(re: https://www.w3.org/TR/design-principles/#consistency )

[10:10:07.0175] <Devin Rousso>
and see my argument is that that syntax is *_SO_* different that there's no overlap (i.e. even if the semantics of declarations in conditionals imply one thing then the different syntax of that can imply another)

[10:10:30.0557] <Richard Gibson>
but it _is_ more confusing, because the `if` binding isn't obviously out of scope inside `else`

[10:10:32.0337] <Justin Ridgewell>
I think if we want the binding to only be visible inside the `If` body, then it the `If` is creating its own lexical scope in it's body.

[10:10:57.0390] <Justin Ridgewell>
Either it's outside the `If` body and visible in the `Else`, or it's inside and not.

[10:11:28.0725] <Justin Ridgewell>
* I think if we want the binding to only be visible inside the `If` body, then the `If` is creating its own lexical scope in it's body.

[10:11:35.0799] <Justin Ridgewell>
* I think if we want the binding to only be visible inside the `If` body, then the `If` is creating its own lexical scope in its body.

[10:12:02.0301] <bakkot>
it's inside and not visible, but that doesn't mean it's not confusing to see code which is written as if it is visible

[10:12:07.0252] <Justin Ridgewell>
Having it poison the else's reference to parent scopes seems wrong.

[10:12:12.0274] <Lea Verou>
Unless I'm misunderstanding your point, it seems that would argue for making the binding available in `else`, which I think is also defensible. 

[10:13:54.0518] <Ashley Claymore>
The very fact that this committee think both being accessible and not accessible are both acceptable semantics proves that it is ambiguious.
A way to avoid someone making the wrong assumption is to make the ambiguous case an early error

[10:14:08.0363] <Richard Gibson>
I think there are reasonable intuitions in both directions regarding visibility in `else`, which are clearly resolved by making references there invalid

[10:14:39.0043] <Justin Ridgewell>
It's only ambiguous because we want a special disposal time for `using`. The least confusing answer is it's visible with `else`.

[10:15:04.0636] <Olivier Flückiger>
it could also be an argument against the proposal if we cannot agree

[10:15:06.0391] <Lea Verou>
That makes sense, as a temporary thing: make them an error now, which buys us time to figure out what the best behavior is without compat issues getting in the way. We do that a lot in CSS, wasn't sure if it's a thing here. 

[10:15:14.0760] <Ashley Claymore>
that is also true

[10:15:26.0496] <Olivier Flückiger>
I think this kinda goes back to an earlier mistake that was made, hoisting `let` up to the last block

[10:16:04.0545] <Ashley Claymore>
No, it is ambiguous. Because we have in these calls looked at the exact same code and assumed the value will be different things

[10:16:12.0449] <bakkot>
this is just not my intuition at all fwiw, even without `using` I think having it visible in the `else` would be more confusing (to me)

[10:16:22.0530] <bakkot>
* this is just not my intuition at all fwiw, even without `using` I think having it visible in the `else` would be more confusing/surprising (to me)

[10:16:31.0410] <Richard Gibson>
`using` motivates scoping to just the `if`, but conflicting intuitions would be there even without such motivation

[10:17:01.0434] <bakkot>
and yes I know it's visible in C++ (and Go, I'd forgotten) but those are... not languages I associate with non-confusing design decisions

[10:17:18.0490] <bakkot>
I actually hadn't known it was visible in C++ before looking it up, despite having used it > 100 times

[10:17:21.0107] <Lea Verou>
I wonder if we could reach a decision that nobody actively objects to, even if we don't agree with it

[10:17:57.0307] <Olivier Flückiger>
sorry but if `let` would start a scope in JS, then it would be less confusting. all the cases where it's not available in else are basically languages where `let` starts a scope and/or the if is a pattern match

[10:18:24.0261] <Olivier Flückiger>
in JS a scope is started by `{` exclusively.

[10:18:37.0927] <Olivier Flückiger>
in some cases like loops, we have sort of an imaginary `{` around the statement

[10:18:54.0661] <bakkot>
I just do not see this super clear distinction between "a pattern match" and "an if"

[10:18:56.0716] <Olivier Flückiger>
the only reasonable extension for the if/else statement is to have an invisible `{` around it

[10:18:57.0165] <bakkot>
it's still just an `if`

[10:19:17.0103] <Ashley Claymore>
`for (let i = 0; ...`

[10:19:26.0269] <bakkot>
loops are considerably more magic than that

[10:19:29.0351] <Olivier Flückiger>
a failing pattern match creates nothing. a declaration which assigns something which is not true, still creates a binding.

[10:19:31.0026] <bakkot>
but that's OK

[10:20:54.0811] <Olivier Flückiger>
I think the fact that we agree that we need to prevent people from using it wrongly hints at the fact that its maybe not the version we want. the design space is bigger...

[10:21:23.0926] <Lea Verou>
Yes, if the binding _is_ available in `else`, the mental model is "there is an invisible scope here that encompasses the `if() {}` and the `else {}`". It's a little weird, but defensible. But I cannot see _any_ reasonable mental model where it's TDZ. It breaks the user's mental model of what TDZ means.

[10:21:27.0184] <bakkot>
ok, so an `if (let x = foo)` is just a special kind of pattern matching where it's considered to fail if it's falsy, whatever?

[10:22:38.0456] <Richard Gibson>
I don't think anyone is still arguing for TDZ—early error has been deemed preferable

[10:22:41.0431] <Lea Verou>
Ironically, that makes the error option more defensible. :/ 

[10:22:45.0085] <Jack Works>
> <@dcrousso:matrix.org> and see my argument is that that syntax is *_SO_* different that there's no overlap (i.e. even if the semantics of declarations in conditionals imply one thing then the different syntax of that can imply another)

I worry if decl in if lands, PM may be required to not introduce `is` expression to switch to PM semantics, and forced to align with declaration in if condition, which requires the pattern be written in `if (let _here_ = expr)`.
Since it prefixed with `let`, it must completely compatible with destruction semantics, and push PM back to 2022 semantics (create binding by default).
2022 version was presented and proved to a non-starter

[10:22:53.0550] <bakkot>
the purpose of an error is to avoid users having to form a mental model of it, because they will never read code where the question could arise in the first place

[10:23:11.0926] <bakkot>
in the same way you simply do not need a mental model of precedence for `-2**2`

[10:25:27.0238] <Olivier Flückiger>
it's not a pattern match. that's why we can't have `if (let [a,b] = foo()) {` in this proposal. Because we don't ahve a notion of this not matching.

[10:25:57.0415] <Richard Gibson>
interestingly, it's somewhat analogous to how `use strict` in a function body is not compatible with non-simple _parameters_ (which precede the body)

[10:26:01.0750] <Lea Verou>
Has anyone asked actual developers what their mental model is and how they'd expect this to work? That would be useful data to get. None of us is completely representative of the average developer, so we shouldn't be designing this based on our own mental model. E.g. it may turn out that 99% of them expect it to be available. Or to just access the one from the parent scope. Or they may genuinely expect different things, in which case, error it is.

[10:27:30.0733] <bakkot>
I don't know what "it's not a pattern match" means. You said "a pattern match" is a thing where a failing match doesn't create a binding. ok, great, `if (let x = y)` doesn't create a binding if `y` is falsy. There are additional syntactic forms that you can't write but I don't see why that changes how `if (let x = y)` is interpreted

[10:28:02.0646] <Richard Gibson>
haven't we already definitively established "they may genuinely expect different things" without even looking outside this group?

[10:28:03.0535] <bakkot>
incidentally I would be totally fine with making `if (let [a, b] = foo())` legal, and making it take the `else` in the case that `foo()` is null (rather than throwing as it currently does).

[10:28:12.0610] <bakkot>
(but not in the MVP)

[10:29:32.0297] <Lea Verou>
It doesn't follow that because folks in this group have diverging mental models, regular developers at large will also have diverging mental models. 

[10:30:06.0903] <Lea Verou>
* It doesn't follow that because folks in this group have diverging mental models, regular developers at large will also have diverging mental models. It's likely, but not so likely that getting actual user data is pointless. 

[10:31:36.0558] <peetk>
does anyone a) strongly prefer binding in else and also b) really like and want this proposal? if so why?

[10:32:25.0805] <Richard Gibson>
I don't think collecting user data on this question is pointless, but I do think it is unnecessary. This group is a subset of the larger one, so we already know that the latter contains differing intuitions, even if they are (currently) wholly confined to our subset.

[10:33:59.0394] <Devin Rousso>
that's a lot of ifs. and i don't get why declarations in conditionals would require pattern matching to drop `is`. why can't both exist?

[10:35:22.0428] <Justin Ridgewell>
I don't think any user is really expecting this tranpsilation:
```
// In
if (using x = foo) {
  console.log(x);
} else {
  console.log('else')
}

// Out
outer: {
  {
    let x = foo;
    if (x) {
      console.log(x);
      break outer;
    }
  }
  console.log('else')
}
```

[10:35:48.0309] <bakkot>
I am expecting that transpilation

[10:36:11.0076] <Jack Works>
> <@dcrousso:matrix.org> that's a lot of ifs. and i don't get why declarations in conditionals would require pattern matching to drop `is`. why can't both exist?

I guess the committee may say we should not have two ways to doing the same thing (for this basic case: test and create new binding in `if`)

[10:36:13.0721] <Justin Ridgewell>
That is the semantics you expect, through?

[10:36:35.0085] <bakkot>
or rather, I am using this feature, and am expecting the `using` to be disposed before the `else`, and am not thinking about the transpilation because that's a weird thing to think about

[10:36:35.0574] <Justin Ridgewell>
Unless you want to force transpiling `using`, too.

[10:36:47.0139] <Devin Rousso>
but those arent the same thing! you said it yourself: test vs create a new binding

[10:36:59.0052] <Lea Verou>
That …is not how user research works. TC39 is not a representative subset of developers who write JS, just like the CSS WG is not a representative subset of developers who write CSS. There are huge selection biases that make a certain type of developer get into standards. :) 

[10:37:06.0767] <Justin Ridgewell>
But this is how I form mental models of new syntax. "How would I write this in the old syntax"

[10:37:10.0867] <Justin Ridgewell>
I think that's common.

[10:37:30.0087] <bakkot>
uhhh I think most people are not babel maintainers

[10:37:42.0420] <Justin Ridgewell>
That old syntax transpilation is not obvious.

[10:37:52.0342] <bakkot>
also it's only "new" syntax for a few years, after which it's just syntax

[10:38:05.0578] <bakkot>
the actual transpilation of `for (let x` is also not obvious

[10:38:11.0615] <bakkot>
but no one cares because it just works the way they expect

[10:38:20.0296] <bakkot>
if you actually write it out, it's pretty weird!

[10:38:45.0223] <Richard Gibson>
I'm not claiming that TC39 are representative. But that property is not necessary for an existence proof.

[10:38:45.0535] <Justin Ridgewell>
That's because of block scoping, and I think everyone wanted the let behavior from the beginning.

[10:39:02.0876] <bakkot>
it is not just because of block scoping

[10:39:08.0121] <bakkot>
even with block scoping it has _additional_ magic

[10:40:13.0812] <Justin Ridgewell>
That the binding is available within everything inside the `for`'s syntax? That makes it seem like `if { } else { }` should get it too.

[10:40:40.0918] <Justin Ridgewell>
The downlevel to `for (var …)` is obvious, it's only the block scoping that makes it magic.

[10:41:20.0066] <bakkot>
sorry, to spell it out, the additional magic is that the binding is per-scope but `++i` still works, somehow

[10:41:25.0556] <bakkot>
* sorry, to spell it out, the additional magic is that the binding is per-iteration but `++i` still works, somehow

[10:42:08.0053] <Justin Ridgewell>
That is, explicitly, block scoping's magic.

[10:42:22.0153] <Justin Ridgewell>
`for (var i; i; i++)` downlevel is easy to explain.

[10:42:33.0761] <bakkot>
I don't think of "block scoping" as meaning "we do weird things with loop updates", but sure, if that's what you mean then it is block scoping's magic

[10:42:46.0945] <Justin Ridgewell>
It's only block scoping's var capture for closures that has anything magical.

[10:43:05.0934] <bakkot>
no, it's the loop update which is magical here?

[10:43:08.0844] <bakkot>
anyway whatever

[10:43:13.0720] <bakkot>
the point is, this works because of, `for (let x` is a special syntactic form with a complex downleveling, and that's _fine_

[10:43:21.0783] <bakkot>
because no one actually thinks about the complex downleveling

[10:43:46.0200] <Justin Ridgewell>
Please explain why `for (var i; i; i++) ` is wrong?

[10:43:47.0915] <bakkot>
it is fine, _good_ even, to add new syntactic forms with good behavior but complex downlevelings. if they didn't have a complex downleveling they'd be _less_ worth adding!

[10:43:57.0797] <Lea Verou>
It's not about existence though. Every language concept has been misunderstood by someone, somewhere. It's about distribution of expectations. If 99% of developers expect A and 1% expect B, it would be a poor design choice to throw because "we can't possibly know". But if 70% expect A and 30% expect B it's defensible.

[10:44:01.0323] <Jack Works>
> <@dcrousso:matrix.org> but those arent the same thing! you said it yourself: test vs create a new binding

I mean test _and_ create new binding

if (let decl = expr())
if (expr() is let decl)

for this simple case I think it maybe ok because decl in if is better in this case
but if decl in cond supports destruction it will overlap more

if (let { x, y } = point(); x === 0)
if (point() is { x: 0, let y })

then the committee may think it's a big overlap and require pm to match inside the let { ... }

[10:44:23.0017] <bakkot>
I have not said anything about `for (var i` being worng and am confused why you are asking about it?

[10:44:28.0087] <bakkot>
* I have not said anything about `for (var i` being wrong and am confused why you are asking about it?

[10:44:50.0545] <Justin Ridgewell>
I think your `Promise.try` proposal points to this being wrong. We form a mental model of the code based on the way we'd write it now.

[10:45:29.0145] <Justin Ridgewell>
Because, literally my whole point, is that you form expectations based on how you'd write it today.

[10:45:48.0012] <bakkot>
we form a mental model of the code based on what we think it is. What I think Promise.try is is, "it's for calling wrapping sync errors", which suggests it should not do any additional stuff in the non-sync-error case

[10:46:09.0156] <Justin Ridgewell>
`for (let …)`'s transpilation is just `var`, unless you have a very specific case with block scoping and a closure.

[10:46:14.0393] <Justin Ridgewell>
It's extremely easy to explain.

[10:46:26.0333] <bakkot>
yes, block scoping and closures is the whole thing I am referring to wrt magic here

[10:47:10.0538] <Richard Gibson>
I understand this reasoning, and agree with this reasoning in many cases, but not in this one. Just like making `-2**2` invalid syntax would still be reasonable even if 99% of then-current developers expected it to be negative.

[10:47:11.0978] <Justin Ridgewell>
The obvious transpilation of `if (let x…)` is `let x; if (…)`

[10:47:19.0159] <Richard Gibson>
* I understand this reasoning, and agree with it in many cases, but not in this one. Just like making `-2**2` invalid syntax would still be reasonable even if 99% of then-current developers expected it to be negative.

[10:47:23.0893] <bakkot>
that is the obvious _to you_ transpilation

[10:47:46.0787] <bakkot>
but to _me_ if I see `if (using x = ...) { } else { }` I am _really_ expecting the `using` to be disposed before the `else`

[10:47:55.0426] <Justin Ridgewell>
I think that's going to be the way 99% of people think of it.

[10:48:01.0775] <bakkot>
it would be (to me) _completely insane_ for it not to be disposed before the `else`

[10:48:15.0325] <bakkot>
again, I strongly disagree with the claim that 99% of people think of transpiling code when reading code

[10:48:21.0926] <Justin Ridgewell>
I do not think anyone is going to expect this tranpsilation, because I struggled to come up with it.

[10:49:03.0818] <bakkot>
I don't think almost anyone would come up with the correct transpilation of `for (let x = ...) setTimeout(() => x)` either

[10:49:05.0395] <bakkot>
but that's ok!

[10:49:12.0653] <bakkot>
because people don't think in terms of transpilation!

[10:51:08.0531] <Justin Ridgewell>
```
for (var x;…) ((x) => {setTimeout(() => x)})(x);
```
We teach closure capturing as a interview gotcha question

[10:51:47.0231] <Devin Rousso>
does pattern matching create bindings in that destructuring example? or is it just testing whether it could destructure as described?

[10:51:49.0410] <Justin Ridgewell>
* ```
for (var x;…) ((x) => { setTimeout(() => x) })(x);
```

We teach closure capturing as a interview gotcha question

[10:52:51.0546] <Justin Ridgewell>
* ```
for (var x;…) ((x) => { setTimeout(() => x) })(x);
```

We teach closure capturing as an interview gotcha question

[10:53:17.0718] <bakkot>
right. it's a gotcha precisely because _the transpilation is unexpected_.

also, that suggests that you wouldn't be able to do an early return, so if you do think in terms of transpilation and that's your mental model you will _still_ be confused. but in practice it is fine because, again, _people do not think in terms of transpilation_

[10:55:16.0904] <Justin Ridgewell>
How would you break inside any closure?

> people do not think in terms of transpilation

I think **everyone does**.

[10:55:40.0206] <Justin Ridgewell>
It's just how new syntax features are taught.

[10:55:54.0007] <Lea Verou>
This is exactly why we're not representative users 😂

[10:57:25.0642] <Lea Verou>
* > I think **everyone does**.

This is exactly why we're not representative users 😂

[10:58:04.0204] <Aki>
Your employer's interviewing style doesn't exactly have the best reputation for like… actually filtering for people who will be good at their actual jobs. 

[10:58:29.0936] <Justin Ridgewell>
It's been banned for a while

[10:58:49.0163] <bakkot>
no one teaches this https://babeljs.io/repl#?config_lz=&code_lz=GYewTgBAFANgpgFwgDwgXggBgNwogHggGZcBqU5ASggG8AoCRiAZ0QBUBLAWzhAFcEUKNTQA-FOUrYGTDsGgBjEADsAJhwQcVw6gCMwcAIYBraUxbtuvAUJHjkk6QF8gA&lineWrap=true&version=8.0.4

[10:58:50.0828] <bakkot>
that is just not true

[10:59:13.0241] <bakkot>
they give an approximation which is good enough to get a sense of what it does

[11:00:49.0208] <Justin Ridgewell>
```
for (let x = 0; x < 3; ++x) {
    x = ((x) => (setTimeout(() => x++), x))(x);
    if (condition()) break;
    x = ((x) => (setTimeout(() => x++), x))(x);
}
```

[11:01:50.0227] <bakkot>
that has different semantics

[11:01:52.0830] <Aki>
👋🏻 hi could someone please volunteer to edit notes for an hour

[11:01:56.0163] <bakkot>
so I guess you don't think in terms of transpilation either?

[11:02:17.0234] <Aki>
❌❌❌❌❌❌❌❌❌

[11:02:41.0002] <Aki>
hehe whoa that is not how i thought that would look

[11:03:45.0845] <bakkot>
* <del>so I guess you don't think in terms of transpilation either?</del> (sorry, that was rude, I apologize)

[11:05:53.0218] <Justin Ridgewell>
Or if we really want:
```
for (var x = 0; x < 3; ++x) {
    let y = x;
    setTimeout(() => y++));
    if (condition()) break;
    setTimeout(() => y++);
    x = y;
}
```

[11:06:16.0236] <Lea Verou>
Re: developer expectations, for anyone interested I just posted:

- [Bluesky post](https://bsky.app/profile/lea.verou.me/post/3mr6e34faos2f)
- [Twitter poll](https://x.com/LeaVerou/status/2079623096810578311)
- [Mastodon poll](https://front-end.social/@leaverou/116959163204123808)
- https://github.com/LeaVerou/blog/discussions/139

Still too early to draw conclusions (and polling on social media has its own, separate, biases obvs), but could be interesting as another data point.

[11:06:19.0916] <Michael Ficarra>
I am so excited to get async iterator helpers rolling

[11:06:21.0968] <Justin Ridgewell>
And then it's just block scoping's transform.

[11:06:56.0527] <Jack Works>
> <@dcrousso:matrix.org> does pattern matching create bindings in that destructuring example? or is it just testing whether it could destructure as described?

current pattern matching does not create bindings by default, some examples:

`x is { let a, b: 1 }`, test `x.b === 1 && "a" in x` and create binding `a`
`x is { a }`, test `"a" in x` and no binding created.
`x is { a: Some(let val) and let a }`, test `"a" in x && Some[@@matcher](x.a)` and create binding `a` (equal to `x.a`) and `val` (equal to `Some[@@matcher](x.a).next().value`)

[11:07:22.0300] <Justin Ridgewell>
Need to run to a meeting

[11:08:01.0299] <Michael Ficarra>
`buffered` should totally be in the MVP

[11:08:29.0953] <Olivier Flückiger>
at least the bluesky post looks a bit biased as basically it focuses on the case where the else value is unexpected...

[11:09:14.0812] <Lea Verou>
I was trying for a snippet that is (a) short and (b) tests the else case, which is where the bone of contention lies. What would you use?

[11:11:06.0166] <Olivier Flückiger>
```
let a = "other";
if (let a = false) {
  log(a);
} else {
  log(a);
}
````

[11:11:29.0910] <peetk>
what's the goal? i think throw advocates acknowledge that throw is pretty novel semantics and thus wouldn't be anticipated by anyone. the point of throwing is people don't have to have a clear intuition. and throw mostly protects against a case where the developer doesn't realize the bound variable is shadowed

[11:12:41.0497] <Lea Verou>
Fair, that would be a little clearer. But do you think the difference might be significant enough to bias the results?

[11:13:05.0619] <Lea Verou>
Yes, it's not about "do people expect an error", it's about "what's the split between A and B"

[11:14:19.0657] <Lea Verou>
* Yes, it's not about "do people expect an error", it's about "what's the split between A and B". D only exists to keep the A vs B cleaner (instead of those who actually expect an error being forced to pick one of the two)

[11:18:53.0972] <Devin Rousso>
ok so yeah again there's a lot of syntax and behavior differences between those two. and for the few (debatably simple) scenarios that overlap it's a side-effect of the expressiveness of pattern matching, not an explicit goal

[11:19:48.0963] <Michael Ficarra>
@leaverou:matrix.org you should ask them what they think this should do:

```
if (let name = ...; name.length > 0) {
  log(name);
} else {
  log(name);
}
```

[11:20:23.0545] <Michael Ficarra>
I don't expect anybody will want it to log `globalThis.name`

[11:20:42.0822] <Devin Rousso>
[Jack Works](https://matrix.to/#/@jackworks:matrix.org) but more importantly, i don't really see how this conflict concern can be resolved without syntax/behavior/etc. changes to declarations in conditionals which likely would result in bringing in parts of pattern matching which is absolutely not going to happen

[11:22:16.0572] <Steve Hicks>
I find this case very compelling.  My mental model is that `if (x) { log(y); } else { log(y); }` should be reducible to `log(y);`.

[11:23:04.0481] <Michael Ficarra>
which is exactly why we should ban it

[11:23:18.0787] <Michael Ficarra>
there's no good option where it runs and does *something*

[11:23:41.0396] <Steve Hicks>
Even if "x" is "let y = foo()" then I'd still expect `log(foo())` to be equivalent.

[11:25:26.0403] <Devin Rousso>
* [@jackworks:matrix.org](https://matrix.to/#/@jackworks:matrix.org) but more importantly, i don't really see how this conflict concern can be resolved without syntax/behavior/etc. changes to declarations in conditionals which likely would result in bringing in parts of pattern matching which is probably not something we would want

[11:25:44.0232] <nicolo-ribaudo>
This demo is very cool

[11:26:35.0054] <Olivier Flückiger>
I don't think anybody would expect it to throw either....

[11:27:18.0689] <Michael Ficarra>
nobody is asking for it to throw

[11:27:24.0362] <Michael Ficarra>
we're asking for it to be an invalid program

[11:27:38.0454] <Michael Ficarra>
just like we did with exponentiation because nobody could agree what it *should* do

[11:28:11.0542] <Olivier Flückiger>
yeah, we only have runtime errors here. some are a bit earlier some later

[11:28:55.0496] <Olivier Flückiger>
this is actually gonna break lazy parsing btw.

[11:28:56.0929] <Ashley Claymore>
Developers will see SyntaxErrors very early...

[11:29:01.0227] <Ashley Claymore>
in their editor

[11:29:08.0847] <Olivier Flückiger>
one of the most important optimizations for the web where 80% of code is dead...

[11:29:12.0565] <Michael Ficarra>
that's not exactly an accurate model since it prevents earlier code from running

[11:29:39.0037] <Ashley Claymore>
maybe that was the motivation for TDZ over SyntaxError

[11:30:00.0155] <Olivier Flückiger>
might break, have to think about it some more

[11:30:28.0018] <nicolo-ribaudo>
Isn't it similar to how `let a = 1; ... something ... let a = 2` is a syntax error?

[11:30:38.0998] <nicolo-ribaudo>
The parser needs to do _some_ scope analysis

[11:31:08.0103] <Olivier Flückiger>
yes it does. I am not sure if it also looks at all uses

[11:31:18.0627] <Olivier Flückiger>
as i said, I have to think about it

[11:31:40.0961] <Lea Verou>
Yup, I think I agree with Michael Ficarra . It doesn't seem that developer expectations are sufficiently concentrated around either option, and an error can always become a not-error later, whereas if we pick either behavior, we're stuck with it forever.

[11:39:22.0706] <eemeli>
Urrr, would `if (var x = y()) ...` work? And if it does, would it too error for `x` access in the `else`?

[11:40:03.0312] <ljharb>
i assume it'd only allow let/const and not var

[11:40:08.0089] <nicolo-ribaudo>
I would be very in favour of just bannig var there

[11:40:14.0952] <Michael Ficarra>
it only allows let/const/using

[11:40:15.0290] <nicolo-ribaudo>
Regardless of which visibility we choose

[11:42:20.0688] <Steve Hicks>
var could let us have our cake and eat it too, since I'd absolutely expect the var binding to persist to the else...  not that it's a good idea.

[11:43:40.0598] <Ashley Claymore>
Well var wouldn't be scoped at all, so there's no benefit to putting it in there

[11:43:41.0159] <eemeli>
We do allow `for (var x = ...`.

[11:44:01.0116] <Ashley Claymore>
* Well var wouldn't be scoped at all, so there's no (real) benefit to putting it in there

[11:44:16.0073] <ljharb>
that predates let/const tho

[11:44:51.0276] <eemeli>
Benefit: not introducing a need to think of where which declarations are allowed or not.

[11:44:51.0637] <Michael Ficarra>
for-var-of doesn't 😜

[11:45:03.0718] <ljharb>
yeah true

[11:45:09.0115] <nicolo-ribaudo>
What if we allowed only `var`

[11:45:14.0666] <ljharb>
you already get that benefit by never using var ever

[11:45:27.0107] <ljharb>
* you already get that benefit by never using var ever (when targeting engines that support let/const, ofc)

[11:46:47.0331] <Lea Verou>
I think it makes sense to allow it. Per eemeli 's argument, it's allowed everywhere else and this would introduce another inconsistency that developers have to memorize (though as already mentioned, there's precedent). OTOH, it's mostly moot as developers have been so warned not to use `var` that they won't use it even where it would actually make their code cleaner because it's easier to just use `let`/`const` than to have to evaluate on a case by case basis.

[11:47:12.0481] <Lea Verou>
* I think it makes sense to allow it. Per eemeli 's argument, it's allowed everywhere else (edit: almost) and this would introduce another inconsistency that developers have to memorize (though as already mentioned, there's precedent). OTOH, it's mostly moot as developers have been so warned not to use `var` that they won't use it even where it would actually make their code cleaner because it's easier to just use `let`/`const` than to have to evaluate on a case by case basis.

[11:47:39.0936] <eemeli>
Seriously, I think we should allow `var`, even if it does exactly the same thing as if you had the declaration before the `if`.

[11:47:45.0295] <ljharb>
imo there's no excuse for hand-writing var once one is targeting let/const-supporting engines (there's one case for transpiler output of it, though)

[11:48:04.0971] <ljharb>
* imo there's no excuse for hand-writing var once one is targeting let/const-supporting engines (there's one case for transpiler output of it, though, but that doesn't affect this)

[11:51:28.0835] <Lea Verou>
A while back I briefly experimented with adopting it for `try ... catch` where the scoping is typically undesirable, but that didn't last long, everything and everyone treats it as a mistake rather than a conscious choice.

[11:51:40.0335] <nicolo-ribaudo>
I like using `var` for things like
```js
try {
  var foo = something():
} catch (e) {
  return;
}

use(foo)
```

[11:51:45.0329] <nicolo-ribaudo>
Oh well that :)

[11:52:28.0378] <ljharb>
we need to ship do expressions to solve that one :-p

[11:52:28.0700] <nicolo-ribaudo>
Real world example: https://github.com/babel/babel/blob/61bfda575b03e6a32af7877b65651a37eb339426/packages/babel-core/src/config/full.ts#L116

[11:55:14.0049] <Michael Ficarra>
try scoping is the next thing we should tackle after we get through this if/else scoping proposal

[11:55:36.0505] <Michael Ficarra>
those are the two places that I am constantly bothered by them being too broad

[11:55:38.0917] <Ashley Claymore>
ES2028 - the one where we fixed try

[11:55:55.0080] <eemeli>
Tackle how?

[11:56:02.0483] <Michael Ficarra>
I imagine it will just be try expressions

[11:56:24.0934] <Michael Ficarra>
give the developer a way to write a try that is scoped to exactly the thing they think might throw

[11:57:17.0894] <Michael Ficarra>
I hate seeing big blocks with like 10 statements inside a try/catch where the developer really only expects 1 little tiny expression in 1 of those statements to possibly throw

[11:57:32.0201] <Michael Ficarra>
but because of scoping, that's the easiest thing for them to do

[11:57:49.0810] <Michael Ficarra>
it's the same issue that we're solving with @dcrousso:matrix.org's proposal IMO

[11:57:52.0058] <ljharb>
to ship try expressions requires the same "completion value" question that blocks do expressions, and if we can resolve it for one we can do it for the other, so let's just advance do expressions :-)

[11:58:14.0280] <peetk>
python style try-else??

[11:58:21.0101] <Michael Ficarra>
I think one of them was slightly harder syntactically

[11:58:48.0799] <eemeli>
Pattern matching on `catch` would be really nice.

[11:59:29.0352] <Lea Verou>
Another idea might be a token to turn scoping off for certain blocks (a bit like regex's `(?:)` non-capturing groups). I just thought of this, so I may think it's a terrible idea later, but figured I'd put it out there :P 

[11:59:49.0572] <nicolo-ribaudo>
A try expression could take an expression rather than a statement

[11:59:58.0203] <nicolo-ribaudo>
Like the current try expression proposal from somebody from the community does

[12:00:04.0331] <nicolo-ribaudo>
The one that returns a "result"

[12:00:26.0960] <Ashley Claymore>
Need extractors first...

[12:01:24.0712] <ljharb>
> <@eemeli:mozilla.org> Pattern matching on `catch` would be really nice.

need pattern matching first :-)

[12:01:51.0568] <Michael Ficarra>
we're going to need a continuation of this async iterator helpers topic

[12:02:08.0253] <Michael Ficarra>
we really need to give Kevin feedback so he can make progress

[12:03:09.0390] <Olivier Flückiger>
I for my part are very happy with @erights Mark Miller (Agoric) MM setting the pace. If we run out of timebox then it's not because we are slow, but because it is complicated.

[12:03:26.0310] <Olivier Flückiger>
* I for my part am very happy with @erights Mark Miller (Agoric) MM setting the pace. If we run out of timebox then it's not because we are slow, but because it is complicated.

[12:03:32.0420] <peetk>
pattern matching but ONLY in `catch`

[12:04:47.0912] <Michael Ficarra>
I'm not complaining about us being slow, just saying it would be a shame to not finish giving Kevin feedback during this plenary (but we have plenty of time left at the end to continue, so I'm not worried)

[12:05:41.0000] <Michael Ficarra>
do failed pattern matches suppress or do they get rethrown?

[12:06:33.0113] <Justin Ridgewell>
Sorry, had another meeting. Why doesn't `filter` have a fast path for out-of-order?

[12:06:46.0447] <Justin Ridgewell>
The way we're discussing for `map`?

[12:07:33.0750] <eemeli>
Rethrow, probably?

[12:07:45.0393] <Michael Ficarra>
because you don't know which promise you can resolve with it while retaining the correct output ordering

[12:08:02.0566] <Justin Ridgewell>
But we're discussing that very thing for `map`?

[12:08:10.0923] <Michael Ficarra>
because for map you do know

[12:08:13.0006] <Justin Ridgewell>
Why is ordering important for `filter` but not `map`?

[12:08:21.0552] <Michael Ficarra>
input-output are 1:1 for map, not so with filter

[12:08:27.0792] <Olivier Flückiger>
agreed, but going to the next point can also evoke the expectation that the previous one is resolved.

[12:08:59.0739] <Michael Ficarra>
did someone ask to move along to the next point?

[12:09:04.0500] <Justin Ridgewell>
I don't understand.

[12:09:11.0305] <nicolo-ribaudo>
Do y'all see Ashley's slides as fullscreeon or in a big black rectangle?

[12:09:17.0438] <nicolo-ribaudo>
* Do y'all see Ashley's slides as fullscreon or in a big black rectangle?

[12:09:24.0630] <Chris de Almeida>
not full

[12:09:38.0745] <Justin Ridgewell>
Tiny little rectangle

[12:09:59.0444] <nicolo-ribaudo>
It is weird but I guess fine, not worth trying to fix it

[12:10:02.0758] <Michael Ficarra>
I know that the third input value corresponds to the third input value for `map`; for `filter`, it could correspond to any of the first, second, third, or none of them (be dropped)

[12:11:06.0772] <Justin Ridgewell>
I get the words you're saying, I don't get the impl. How would `map` know this and not `filter`?

[12:11:15.0703] <Michael Ficarra>
* I know that the third input value corresponds to the third output value for `map`; for `filter`, it could correspond to any of the first, second, third, or none of them (be dropped)

[12:11:38.0389] <Justin Ridgewell>
Why would it matter? If `map` can go out of order, why couldn't `filter`?

[12:11:48.0402] <Justin Ridgewell>
If `filter` can't, why would `map`?

[12:11:59.0061] <Michael Ficarra>
`map` resolves out of temporal order, but the values are yielded in sequence

[12:12:23.0771] <Michael Ficarra>
this entire proposal is order-preserving

[12:12:46.0915] <Michael Ficarra>
the out-of-order helpers are in a separate proposal (https://github.com/tc39/proposal-unordered-async-iterator-helpers)

[12:13:00.0714] <bakkot>
Justin Ridgewell: it is my hope that you can stare at https://bakkot.github.io/async-iterator-helpers-implementation/#map-concurrent and https://bakkot.github.io/async-iterator-helpers-implementation/#filter-concurrent-2 and be enlightened

[12:13:30.0925] <bakkot>
just play those scenarios out (hit right arrow or next repeatedly)

[12:14:35.0899] <hax (HE Shi-Jun)>
If want this, I think we should reserve `if (let x = val)` for consistency. (current proposal make it  same as `if (let x = val; val)`.

[12:14:47.0579] <Justin Ridgewell>
OHHHHH

[12:14:58.0340] <bakkot>
but, to put it in words:

in either scenario, you do two pulls from the result.
this does two pulls from the underlying.
suppose the second settles and then the mapper settles / the predicate returns true for that value.
for .map, you know that this value is going to go into the second result promise
for .filter, it might not!

[12:15:00.0547] <Justin Ridgewell>
That was very helpful

[12:15:05.0018] <bakkot>
yaaaaay

[12:16:05.0735] <Michael Ficarra>
that totally justifies the hours and hours Kevin spent on the demo

[12:18:33.0752] <Michael Ficarra>
a 100-value composite seems a little bit unrepresentative of real code

[12:18:49.0178] <Michael Ficarra>
I would think they'd almost always be 2 or 3 values with a sharp drop off

[12:20:00.0617] <Michael Ficarra>
oh, I think both graphs are labelled the same but the first graph actually is only 3 values

[12:20:02.0206] <Justin Ridgewell>
Ok, the demo looks like different scenarios for the function, and not different semantics

[12:20:18.0082] <bakkot>
yes, demo just has one semantics implemented

[12:20:29.0556] <bakkot>
well, one for each of the three

[12:20:38.0909] <bakkot>
first link is .map, second link is .filter

[12:20:44.0798] <Justin Ridgewell>
Yah

[12:20:50.0357] <bakkot>
* first link is a scenario for .map, second link is a scenario for .filter

[12:21:59.0125] <bakkot>
incidentally something like 80% of the complexity of this proposal is `flatMap` alone and we barely got to it, so, we have at least another hour of material to talk about I expect

[12:24:06.0479] <Michael Ficarra>
wait until you hear my opinions about flatMap, it's gonna be even more

[12:24:37.0207] <bakkot>
I had fable implement your thing, +647 -224

[12:24:44.0315] <bakkot>
I can do better but it's still going to be that order of magnitude

[12:25:11.0702] <bakkot>
I guess only +242 -131 for the actual implementation though, that was mostly tests

[12:32:44.0599] <Michael Ficarra>
I actually like the name Composite

[12:37:23.0496] <rbuckton>
I wish I were in a better position to discuss pattern matching, but all I have with me is my phone at the moment (and this takes awhile to type out). I looked into various ways to adopt pattern about 4 years ago now after researching the approaches taken in Rust, Swift, Python, C#, and a few other languahes, and was initially optimistic about the possibility of  `if..let`. I found, though, that our current semantics for binding  and assignment patterns are incompatible with well established pattern matching tenants. Our destructuring patterns aren't nearly strict enough, which would mean either inconsistent semantics between a normal `let` and `if..let`, or the need to introduce new syntax to opt-in to stricter matching behavior. The other big concern was how to differentiate between a binding and a reference. Typed languages can leverage static analysis and type systems, but JS does not have that capability. This, in turn, meant even more syntactic complexity to work around that results in footguns because it would by necessity be opt-in and not opt-out. The end result would either be confusing semantics or messy syntax, or a very limited, rough and inconsistent pattern matching experience.

I found, though, that the C# syntax and semantics were a good fit. C# separates destructuring (called "Deconstruction") from pattern matching and has both a branching `match` expression form and an infix `is` expression form. It's syntax for named bindings is both simple and explcit: `Pattern Identifier?`, where `Pattern` includes type names as well as `var`. Adapted to JS, this would mean you could write something like:

```
if (val is [Number x, Number y])...
```

or

```
if (val is { a: { b: String } a })...
```

or just

```
if (val is { a: let a })...
```

to both match and extract deeply nested values without bind/reference syntax confusion, while avoiding extra syntax for the most common cases (testing against types). I find patterns like `x is Map or Set`, `x is not String`, `x is Some(Number)`, and `x is [Number, Number] ` extremely convenient and clear, while `x is @String or @Number` (or similar) far less so. 

[12:37:31.0842] <Justin Ridgewell>
Is there a scenario where ordering matters? If we held `done` results, and pushed pending calls first, could that simplify everything?

[12:37:39.0817] <Justin Ridgewell>
Then the consumer still sees a coherent stream.

[12:38:05.0104] <bakkot>
is there a scenario where the order of values from an iterator matters? to ask the question is to answer, surely?

[12:39:02.0307] <Michael Ficarra>
@bakkot:matrix.org I think this is just more support for my hypothesis that everyone is basically always going to want to use the unordered variants of these things

[12:39:19.0151] <rbuckton>
(a few typos, sorry. Not going to try fixing them because edits in Element on mobile always mess up formatting) 

[12:39:23.0381] <Justin Ridgewell>
How would a consumer associate the pulled promise with the internal `map`/`filter`'s call?

[12:39:48.0557] <Justin Ridgewell>
I don't know if there's a way for them to hold an identity where they could prove it

[12:40:21.0500] <bakkot>
possibly I am not understanding the proposal. my position is: if the we do `[1, 2, 3].toAsync().map(f).buffered(3).toArray()`, we should get `[f(1), f(2), f(3)]` out, no matter what order `f` settles in

[12:40:30.0816] <bakkot>
* possibly I am not understanding your suggestion. my position is: if the we do `[1, 2, 3].toAsync().map(f).buffered(3).toArray()`, we should get `[f(1), f(2), f(3)]` out, no matter what order `f` settles in

[12:40:39.0986] <bakkot>
if your suggestion does not violate that property then I do not understand your suggestion

[12:40:57.0977] <bakkot>
* possibly I am not understanding your suggestion. my position is: if the we do `[1, 2, 3].toAsync().map(f).buffered(3).toArray()`, we should get `[f(1), f(2), f(3)]` out, no matter what order the calls to `f` settle in

[12:41:35.0282] <Justin Ridgewell>
Sorry, I'm not sure what `buffered` does

[12:41:57.0379] <Justin Ridgewell>
How does it interact with the fast delivery of `map`?

[12:42:17.0937] <bakkot>
pulls N times immediately, stores the resulting Promises in an internal buffer, hands them out in order to callers

[12:42:41.0542] <bakkot>
it doesn't inspect results of Promises at all

[12:42:48.0632] <Justin Ridgewell>
If `f(2)` returns `done`, does it matter that you receive `[1, done, 3]` or `[1, 3, done]`?

[12:43:09.0527] <Justin Ridgewell>
How does the consumer of the buffer associate the internal calls?

[12:43:43.0597] <bakkot>
I really really do not want to get `[1, 3, done]` in that case, `map` should not be changing order of results

[12:45:11.0294] <Justin Ridgewell>
I guess that does force an internal buffer of all things like `filter`.

[12:45:23.0129] <bakkot>
I don't understand the question.

`.buffer` is basically:

```
function buffer(iterator, N) {
  let buffer = [];

  for (let i = 0; i < N; i++) {
    buffer.push(iterator.next());
  }

  return {
    next() {
      buffer.push(iterator.next());
      return buffer.shift();
    },
  };
}
```

[12:45:52.0138] <bakkot>
modulo some handling of `.return` etc

[12:46:19.0140] <Justin Ridgewell>
My point was that whoever consumed the buffer couldn't tell which order the internal calls were made

[12:46:41.0132] <bakkot>
oh, yes, I agree with that, and it's OK / good to do the internal calls to the mapper/predicate function out of order

[12:46:45.0906] <Justin Ridgewell>
But this just becomes like a `filter`

[12:47:00.0830] <bakkot>
as long as the observed results are the same order you'd get if there was no concurrency

[12:47:02.0244] <Justin Ridgewell>
If we wanted to hold `done` values to reorder

[12:51:47.0560] <Michael Ficarra>
undefined values are dropped in JSON serialisation because JSON doesn't have undefined lol

[12:52:00.0660] <Michael Ficarra>
I don't think that says anything about how the *language* should behave

[12:53:43.0282] <bakkot>
I do occasionally kick around the idea of a `{ x?: v }` syntax that doesn't create `x` if `v` is `undefined`

[12:54:07.0288] <bakkot>
if we had that syntax, I would feel more strongly that `undefined` should not be treated as absent, because if it's present it's there on purpose

[12:56:46.0689] <bakkot>
did everyone else just get kicked out

[12:56:53.0888] <nicolo-ribaudo>
Lol

[12:56:54.0301] <Lea Verou>
just came to ask the same question

[12:56:54.0626] <nicolo-ribaudo>
Yes

[12:56:59.0540] <Olivier Flückiger>
yes

[12:57:00.0280] <nicolo-ribaudo>
Are we on a free teams plan?

[12:57:00.0573] <Aki>
hmm.

[12:57:00.0996] <Ashley Claymore>
"this meeting is going on too long"

[12:57:12.0051] <Michael Ficarra>
well that was unfortunate timing

[12:57:12.0412] <peetk>
that's one way to timebox

[12:57:12.0762] <James M Snell>
That's.... fun

[12:57:14.0129] <Richard Gibson>
wow, judgmental much?

[12:57:16.0691] <Olivier Flückiger>
I think we all agree, thanks teams

[12:57:27.0708] <Michael Ficarra>
can we agree never to use Teams ever again?

[12:57:33.0287] <Aki>
Drag me, teams.

[12:57:33.0958] <Justin Ridgewell>
Lol

[12:58:43.0446] <James M Snell>
That's not a timebox, that's a time hammer

[12:59:45.0659] <Olivier Flückiger>
so I guess restart tomorrow? I hope somebody can restart it

[12:59:56.0834] <James M Snell>
For those who were dropped and have not yet rejoined... you can use the same link to just rejoin

[13:00:10.0727] <James M Snell>
but we're almost done anyway

[13:00:53.0161] <Chengzhong Wu>
btw refreshing the page won't work. Join with the link again

[13:04:26.0159] <Michael Ficarra>
yes this is what I was suggesting: we should solve that problem for object initialisers, it's not a problem for composites to solve

[13:04:32.0021] <bakkot>
Ashley Claymore: my topic was, IIUC you were concerned that people would write `{ a: 0, b: possiblyUndefined }` and `{ a: 0 }` and expect them to be equal. currently it is annoying to omit the property in the first case, and it's clunky to explicitly _add_ a `b: undefined` in the second case, so I get that.

would you feel differently if we had syntax where they could write `{ a: 0, b?: possiblyUndefined }` where the property would not be created if the value was `undefined`? that way you could easily omit the property in the first case.

[13:04:43.0877] <Aki>
Woof the google doc's summaries and conclusions are sadddddd so far. I apologise for not being more annoying earlier.

[13:04:53.0589] <Aki>
DON'T FORGET TO WRITE YOUR SUMMARIES AND CONCLUSIONS

[13:04:57.0993] <Aki>
I WILL FIND YOU

[13:05:20.0484] <James M Snell>
I did mine already so I wouldn't incur Aki's wrath 

[13:05:24.0760] <Michael Ficarra>
there's still like 16 days left, I'm not worried

[13:05:39.0959] <Aki>
MF out here conspiring to give me a stroke

[13:05:40.0348] <Michael Ficarra>
I'll get to it in 15 days

[13:06:56.0616] <bakkot>
though, maybe we shouldn't do this because it encourages creating objects with different shapes, idk

[13:07:20.0135] <James M Snell>
Personally I'd prefer to avoid syntax and use something like `new Composite({a: 0, b: possiblyUndefined}, { omitUndefined: true })` ... but I suspect I might be in the minority. I don't like special casing `undefined` by default

[13:08:17.0921] <James M Snell>
To be fair, 15 days, 23 hours and 50 minutes or so

[13:08:20.0265] <James M Snell>
give or take

[13:08:42.0228] <bakkot>
James M Snell: partly I want this for completely unrelated reasons: I want to be able to write `x = { ...y, v?: undefined }` to get all the properties of `y` except for `v` (and not have a stray `v: undefined` on `x`)

[13:08:55.0255] <Michael Ficarra>
if they want to make objects different shapes, they're gonna make objects different shapes

[13:09:13.0170] <Michael Ficarra>
whether it's more readable/maintainable or not

[13:09:35.0946] <bakkot>
I mean, no, mostly they create objects with extraneous `undefined` properties currently

[13:10:12.0960] <Michael Ficarra>
oh sure, you're saying some portion of them will give up and just fill with undefined instead if it's too wordy

[13:10:15.0212] <Michael Ficarra>
yeah I buy that

[13:11:12.0665] <James M Snell>
I just think it's weird that if I have `Composite({ b: undefined, a: 0 })` that would be treated the same as `Composite({ a: 0, b: undefined })` while `Composite({ b: null, a: 0})` would be distinct from `Composite({ a: 0, b: null})`

[13:11:54.0038] <Richard Gibson>
is that being proposed? I thought property order was irrelevant.

[13:12:44.0820] <James M Snell>
Proposal shows, for instance, `Composite({x : 1, y: 2})` -> `Object.keys(c); // ["x", "y"]` ... I assume order ends up mattering

[13:12:52.0553] <James M Snell>
unless there's a normalization I missed

[13:13:09.0450] <Richard Gibson>
I believe there is. That's the key sorting Ashley Claymore was talking about

[13:13:45.0951] <James M Snell>
ah! ok, then I missed that part of the discussion entirely thanks to dogs ;-)

[13:14:44.0890] <James M Snell>
still, even with that, I'm not seeing a strong reason to special case `undefined`

[13:16:28.0607] <Richard Gibson>
I agree. And we didn't even discuss -0 this time, which has similar possibilities

[13:17:29.0793] <bakkot>
-0 :(

[13:18:59.0049] <Ashley Claymore>
If we intern we have to normalize -0 IMO

[13:19:06.0681] <Ashley Claymore>
normalize to 0

[13:19:45.0806] <Richard Gibson>
interning vs. normalizing primitives into equivalences classes seem like completely independent decisions to me

[13:19:59.0006] <Ashley Claymore>
-0 === 0

[13:20:12.0002] <Ashley Claymore>
So a composite should be ===

[13:20:13.0700] <Richard Gibson>
and `NaN !== NaN`

[13:20:34.0775] <Richard Gibson>
`===` is not an identity comparator

[13:20:47.0468] <Ashley Claymore>
I'm talking about intuition

[13:21:11.0797] <Ashley Claymore>
we also can't keep talking about -0 and NaN.
it's been over 5 years 

[13:21:13.0496] <bakkot>
the point of interning is that it lets you do `===` cheaply i.e. without a recursive walk; if it doesn't let you do `===` cheaply there is no point

[13:22:19.0713] <Ashley Claymore>
Exactly. Interning has to mean actual interning here 

[13:22:31.0598] <Ashley Claymore>
Not maybe intern but not for -0 and NaN

[13:23:07.0501] <Ashley Claymore>
* Not: maybe intern but not for -0 and NaN

[13:23:33.0875] <Richard Gibson>
that's true but immaterial... actual interning can be achieved with or without primitive normalization

[13:23:55.0476] <Richard Gibson>
they are independent

[13:24:04.0775] <Ashley Claymore>
true but not if we also want 0 and -0 to remain equal in a key, which I want 

[13:24:25.0282] <Ashley Claymore>
We already normalize them for Map and Set 

[13:24:30.0983] <Ashley Claymore>
That this what we should do here 

[13:24:53.0566] <Ashley Claymore>
Anything else is very unintuitive for 99.9% of JS developers 

[13:25:46.0365] <Ashley Claymore>
I frequently ask seniors engineers what they think about the equality of negative zero in floating point numbers and it's the first time they have ever heard of negative zero 

[13:26:26.0398] <James M Snell>
A case like
```
let obj = { a: 0, b: undefined };
let comp = new Composite(obj);
'b' in obj; // true! 
'b' in comp; // false! (because undefined was omitted)
```
Is likely to be a bit confusing for users... likely an edge case but still ... Is the proposal still planning to expose the members of composite as properties on the composite?

[13:27:04.0141] <Ashley Claymore>
I'm happy to change my stance on including undefined values 

[13:27:33.0710] <Ashley Claymore>
I get  the feeling that my original stance is in the minority 

[13:27:45.0119] <James M Snell>
I'm just trying to poke around edge cases to see if I can convince myself that my opposition to ignoring `undefined` is incorrect

[13:27:53.0993] <Ashley Claymore>
* I'm happy to change my stance on (not) including undefined values 

[13:30:19.0205] <Ashley Claymore>
I was mostly coming from the perspective of how prevalent JSON.stringify is used for keys today, and I hadn't come across dropping undefined as an issue.
whereas the key order and unsupported types were 

[13:32:14.0019] <James M Snell>
yeah, and I can absolutely appreciate that view. I can see it both ways but special casing it just *feels* more incorrect. Going to have to stew on it for a bit

[13:33:34.0950] <Ashley Claymore>
> <@gibson042:matrix.org> and `NaN !== NaN`

Yea but they are equal in a Set

[13:34:11.0169] <Ashley Claymore>
It would be catastrophic to defining interning a key that has NaN within it to always mint a new key 

[13:34:16.0298] <Ashley Claymore>
Just so they are not equal 

[13:34:43.0064] <Ashley Claymore>
so NaN in a key must be equal 

[13:35:10.0249] <Richard Gibson>
yes, Set and Map both have opinionated non-configurable key normalization that doesn't correspond with `===`

[13:35:17.0096] <Ashley Claymore>
And throwing would be a real shame. If that's the only case we throw on 

[13:35:32.0593] <bakkot>
honestly that specific example is fine for me, I don't care _that_ much about equality for keys containing NaN

[13:35:53.0590] <Ashley Claymore>
> <@gibson042:matrix.org> yes, Set and Map both have opinionated non-configurable key normalization that doesn't correspond with `===`

This is why it is related to interning.
the interning will define the equality  in Map and Set 

[13:36:21.0534] <bakkot>
I do think it's important that `Composite({ x: -0 }) === Composite({ x: 0 })`, which implies one of not interning, a full walk despite interning, normalization, or throwing

[13:36:35.0815] <bakkot>
I think we should rule out the "full walk despite interning" option, and throwing feels bad

[13:36:51.0954] <bakkot>
* I do think it's important that `Composite({ x: -0 }) === Composite({ x: 0 })`, which implies one of: not interning, a full walk for `===` despite interning, normalization, or throwing

[13:39:38.0297] <Ashley Claymore>
https://acutmore.github.io/record-tuple-laboratory/ I made this 4 years ago.
we just keep having this same exact conversation 

[13:42:15.0328] <Richard Gibson>
this is probably the crux: I _don't_ think it's important that `Composite({ x: -0 }) === Composite({ x: 0 })`, and haven't determined whether or not I'm opposed to it. `Composite` is in some sense key normalization that sits outside of Map and Set, and normalizing primitives into equivalence classes adds a lot of friction to recovery where the distinction matters (which tends to matter rarely, but not never)

[13:42:19.0510] <Richard Gibson>
we keep having the conversation because no proposal has ever made it far enough for a satisfactory resolution

[13:43:55.0781] <ljharb>
fwiw i fail to understand any unordered use cases; in all cases i'd want to preserve the order

[13:44:20.0392] <Ashley Claymore>
there is no order here, it's key value pairs


[13:44:40.0344] <ljharb>
* fwiw i fail to understand any unordered use cases; in all cases i'd want to preserve the order (talking about async helpers, not composite)

[13:44:43.0227] <Ashley Claymore>
And the keys are sorted (no symbol keys allowed)

[13:45:03.0007] <Ashley Claymore>
ah sorry 

[13:45:19.0435] <Ashley Claymore>
* ah sorry, though you were talking Keys 

[13:46:01.0680] <Ashley Claymore>
Fair.
this is why I wanted to resolve the interning question first.
so that we can have a discussion on -0 with that decision already made 

[13:46:06.0697] <Ashley Claymore>
as they are related 

[13:46:08.0190] <Richard Gibson>
there are tons of cases where order doesn't matter. For a simple one: perform some operation on every file in a directory

[13:46:29.0588] <bakkot>
I definitely agree that you usually you want to preserve order. but there are cases where you might not: any time you use `.some()` on the result, or `.reduce((a, b) => a+b)`, or similar. in those cases it literally does not matter what order things resolve in, and so you just want to get the answer asap

[13:47:12.0703] <bakkot>
(well, quibbles around commutativity of floating point addition aside)

[13:47:15.0612] <James M Snell>
sorry, still stuck on the `undefined` bit here... one example case where ignoring `undefined` causes.. weirdness

```
const err1 = new Error('A', { cause: undefined });
const err2 = new Error('A', { });
const comp1 = new Composite(err1);
const comp2 = new Composite(err2);
comp1 === comp2;
```

These are definitively two separate sources, but they end up resulting in the same Composite? That feels... off. When we're talking about options bags / object literals that are passed in, it seems ok, but for stuff like this... treating them as equivalent feels ... wrong

[13:47:57.0093] <James M Snell>
Btw, I'm intentionally ignoring `stack` in that

[13:48:12.0960] <James M Snell>
* Btw, I'm intentionally ignoring `stack` in that as it's just a quick example 

[13:50:13.0446] <bakkot>
or, say you to fetch a bunch of files, but the files have associated filenames, like [{ name, url }, { name, url }]; you ultimately want a `Map` from `name` to `url` and you know by construction that the names are unique. so you do

```
let result = new Map;
let iter = items.toAsync().map(async ({ name, url }) => [name, await fetch(url)]).buffer(3); // 3 fetches at once
for (let [name, file] of iter) {
  result[name] = file
}
```

[13:50:35.0290] <bakkot>
* or, say you to fetch a bunch of files, but the files have associated filenames, like \[{ name, url }, { name, url }\]; you ultimately want a `Map` from `name` to `url` and you know by construction that the names are unique. so you do

```
let result = new Map;
let iter = items.toAsync()
  .map(async ({ name, url }) => [name, await fetch(url)])
  .buffer(3); // 3 fetches at once
for (let [name, file] of iter) {
  result[name] = file
}
```

[13:51:43.0912] <bakkot>
* or, say you to fetch a bunch of files, but the files have associated filenames, like \[{ name, url }, { name, url }\]; you ultimately want a `Map` from `name` to `url` and you know by construction that the names are unique. so you do

```
let result = new Map;
let iter = items.toAsync()
  .map(async ({ name, url }) => [name, await fetch(url)])
  .buffered(3); // 3 fetches at once
for (let [name, file] of iter) {
  result[name] = file
}
```

[13:52:59.0779] <bakkot>
you can get results strictly faster with unordered `map`/`buffered` in that example, because then you are always fetching three results at once; when order is enforced, you can't drain later results from the buffer until earlier ones have settled, so you're often blocked waiting for earlier ones to settle. there's a nice animation of this in actually on https://github.com/tc39/proposal-unordered-async-iterator-helpers

[13:55:25.0899] <bakkot>
* or, say you want to fetch a bunch of files, but the files have associated filenames, like \[{ name, url }, { name, url }\]; you ultimately want a `Map` from `name` to `url` and you know by construction that the names are unique. so you do

```
let result = new Map;
let iter = items.toAsync()
  .map(async ({ name, url }) => [name, await fetch(url)])
  .buffered(3); // 3 fetches at once
for await (let [name, file] of iter) {
  result[name] = file
}
```

[13:56:44.0957] <hax (HE Shi-Jun)>
so I expect [1, x?, 2] could give u [1, 2] if x is undefined 😉

[13:58:44.0966] <Richard Gibson>
they don't seem very related to me, but I nonetheless support resolving interning vs. not first and would have done it the same way

[14:05:40.0226] <ljharb>
ok but that's a single operation on a directory. if i had a list of the files, absolutely the order should be preserved

[14:25:35.0500] <Richard Gibson>
counting occurrences of a particular search term is not a use case in which order matters

[14:26:11.0552] <ljharb>
ok - in any of the cases where you can discard order, does it matter if order isn't preserved?

[14:26:14.0810] <ljharb>
* ok - in any of the cases where you can discard order, does it matter if order is preserved?

[14:27:08.0278] <ljharb>
* ok - in any of the cases where you can discard order, does it matter if order is preserved? (like, could you use the order-preserving ones and it'd be the same)

[14:27:35.0374] <bakkot>
depends on whether you care about how long things take

[14:28:16.0618] <bakkot>
you will generally get the same result, it'll just take longer

[14:28:26.0541] <bakkot>
* you will ~always get the same result, it'll just take longer

[14:28:33.0750] <ljharb>
why would that be relevant? preserving order doesn't mean the operations can't all be done in parallel/cocurrently

[14:28:37.0073] <ljharb>
* why would that be relevant? preserving order doesn't mean the operations can't all be done in parallel/concurrently

[14:28:46.0539] <ljharb>
it just means the final results need to be in order

[14:28:58.0575] <ljharb>
eg the way Promise.all works

[14:29:01.0353] <bakkot>
generally speaking you want to have a bounded degree of concurrency and also a bounded buffer size

[14:29:44.0220] <bakkot>
if you drop one of those requirements then yes you can just do everything at once and keep it all in memory

[14:32:24.0469] <ljharb>
the final result has to be all in memory anyways tho, right? so why can't that be built up out of order (in the proper order) as results settle?

[14:33:53.0621] <bakkot>
no, there are lots of cases where the final result does not have to all be in memory at once?

[14:37:06.0576] <ljharb>
ah k because it's an async iterator

[14:38:07.0015] <ljharb>
ok so with the unordered one, it's just a totally nondeterministic order in the result?

[14:38:31.0273] <bakkot>
yup

[16:31:20.0520] <Richard Gibson>
here's an example of exactly that pattern, which I think of as a work pool: https://gist.github.com/gibson042/d8ab425d133400959bc5bb875898a97b#file-github-fixer-user-js-L1023-L1029

concurrency ramps up to the specified `capacity` as quickly as possible by `take()`ing input iteration results from `source`, output iteration results are yielded as quickly as possible (_as [index, result] pairs in case the consumer wants to restore ordering_), and new inputs are `take()`n immediately thereafter until `source` is exhausted


2026-07-22
[22:35:53.0941] <bakkot>
stray thought: we've long said that the ratio of readers to writers of code is high, so we should prioritize code being easy to understand over code being easy to write. LLMs make that dramatically more true: for a lot of code the ratio is now literally infinite, i.e., there is no one who wrote it, and just people who are reading it. so it is more important than ever that code be easy to understand.

this is to say, I am increasingly convinced that `if (let x = ...) { } else { x }` should be an early error, so that this code cannot be written, and thus the only cases that any reader of code will ever encounter are completely unambiguous.

[04:30:59.0369] <rkirsling>
even as somebody vehemently opposed to LLMs I agree with your conclusion :)

[08:13:30.0244] <Michael Ficarra>
the original sin was allowing people to write `0.1` instead of its real value `0.1000000000000000055511151231257827021181583404541015625` and pretend like it's 0.1

[08:14:04.0453] <nicolo-ribaudo>
Why don't we start writing a lint rule for that

[08:14:07.0496] <Justin Ridgewell>
Doesn't this mean the AI's naive assumption is the most natural? I see this and assume the X is available within both.

I think what's increasingly confusing is only `using`'s scope. Just ban it. If you want to auto dispose, do it within the `if` body. `if (let x  = …) { using _ = x; … } else { x }`.

[08:14:11.0164] <nicolo-ribaudo>
With autofix

[08:14:22.0307] <Justin Ridgewell>
* Doesn't this mean the AI's naive assumption is the most natural? I see this and assume the `x` is available within both.

I think what's increasingly confusing is only `using`'s scope. Just ban it. If you want to auto dispose, do it within the `if` body. `if (let x  = …) { using _ = x; … } else { x }`.

[08:14:23.0628] <Michael Ficarra>
I literally wrote one for eslint like 10 years ago

[08:14:27.0322] <Michael Ficarra>
it was rejected

[08:14:35.0156] <nicolo-ribaudo>
:/

[08:15:04.0629] <peetk>
but what if you want to condition on something on the disposable

[08:15:13.0602] <Michael Ficarra>
@nicolo-ribaudo:matrix.org https://github.com/eslint/eslint/issues/8447

[08:15:51.0064] <Justin Ridgewell>
The disposable is `x`. `if (let x = …; x.isOpen()) {}`

[08:16:15.0696] <bakkot>
... no? Nothing I wrote had anything to do with _the AI's_ assumption. Also I think you missed my point: some people think it will mean one thing, some people another. We can argue about which is "most natural", but if you simply don't ever encounter an `x` in the `else` then it doesn't matter which assumption you'd make.

[08:16:45.0727] <Michael Ficarra>
it was my last attempt to contribute to eslint lol https://github.com/eslint/eslint/issues?q=sort%3Aupdated-desc%20is%3Aissue%20state%3Aclosed%20author%3Amichaelficarra

[08:17:29.0148] <peetk>
oh i see. i think that's (a lot) worse than having to do `{ let x = ...; if ... else ... }` in the cases where you want `x` in the `else`

[08:18:31.0201] <Justin Ridgewell>
The AI wrote `x` because it assumes it's available, which I share when reading it.

[08:18:53.0215] <Justin Ridgewell>
Isn't that the whole point of this proposal?

[08:20:22.0943] <peetk>
@bakkot has several times made the argument that without the `using`, this proposal is maybe not even worth it given how easy it is to desugar. no `else` + `using` gives this proposal a very strong use case where it's not so easy to desugar yourself.

[08:20:50.0857] <peetk>
* bakkot has several times made the argument that without the `using`, this proposal is maybe not even worth it given how easy it is to desugar. no `else` + `using` gives this proposal a very strong use case where it's not so easy to desugar yourself.

[08:22:23.0049] <Justin Ridgewell>
I think `using` is the source of it's worse semantics.

[08:22:26.0165] <Michael Ficarra>
The AI may have written `x` to intentionally read from an outer `x` because it knows how the feature works.

[08:22:29.0147] <Justin Ridgewell>
* I think `using` is the source of its worse semantics.

[08:22:41.0624] <bakkot>
Maybe it was trying to refer to the `x` from the outer scope; you can't tell just from that snippet. But who cares? It will immediately discover that this doesn't work and write something else. (Or, much more likely, _if_ we ban it then it will already be aware that it is not legal and will not write it in the first place.) And so by the time a human actually reads it the human will not see the problematic construct and so will not encounter a situation where their intuition may differ from the AI's intent.

[08:23:15.0183] <peetk>
can you elaborate on this

[08:23:50.0464] <bakkot>
My point is, reasoning from "what is more natural" doesn't work all that well, because clearly people have different intuitions, but by simply banning it _people never need to be exposed to the fact that their intuitions differ in the first place_ (except in the increasingly-rare case that they're attempting to write the code, not just read it, and are not fully aware of how it works)

[08:24:27.0193] <bakkot>
I feel like you are not engaging with this argument at all

[08:24:30.0337] <Justin Ridgewell>
^ this entire discussion. `x` should be available within the `else {}`. The only justification for it not, is that we want the `using` scope to end with the `if`'s .

[08:24:58.0419] <Justin Ridgewell>
If there is no disposal, there's no need to remove its binding from the scope.

[08:25:03.0439] <bakkot>
No! there is another _excellent_ justification for it to be an error, which is that some people assume it wouldn't be available, and that makes it inevitably confusing for a lot of people to read

[08:25:25.0388] <bakkot>
* No! there is another _excellent_ justification for it to be an error, which is that some people assume it wouldn't be available (i.e., would read through to the outer scope), and that makes it inevitably confusing for a lot of people to read

[08:25:45.0651] <peetk>
can you explain why you want this proposal? is saving 5 keystrokes worth it?

[08:26:19.0559] <Justin Ridgewell>
Based on the need for `using` to dispose? This isn't an issue with without it, who would care if the `x` is available or not?

[08:26:33.0055] <Justin Ridgewell>
It's increasingly not worth it.

[08:27:06.0965] <ljharb>
i mean, if it were available in the else, i'd use it to determine what kind of falsy value it was, no `using` involved

[08:27:39.0675] <peetk>
but you could do that without this proposal very easily

[08:27:49.0937] <ljharb>
sure, by defining it outside the if

[08:28:02.0828] <Justin Ridgewell>
That's exactly my reasoning. `if (let x = …; x.isFile()) { } else if (x.isDir()) {} else {}`.

[08:28:26.0530] <James M Snell>
at this point, I'm rather leaning towards *not* adding this because of the complexities and issues raised that seem to outweigh the syntactic benefit

[08:28:33.0452] <peetk>
yea but again why not just pop that let x outside the `if`

[08:28:35.0025] <Michael Ficarra>
@jridgewell:matrix.org there's no use in defining that in the if conditional though

[08:28:39.0171] <bakkot>
no, not based on the need for `using` to dispose, just based on literally looking at it and/or familiarity with languages like Rust and Swift

[08:29:03.0106] <bakkot>
did you see Lea's polls? there lots of people with this intuition https://github.com/LeaVerou/blog/discussions/139

[08:29:43.0751] <bakkot>
you can say their intuition is wrong, which, fine, whatever, they are wrong and bad. but we can still avoid making their lives harder, at _very_ low cost, simply by making it an error

[08:29:47.0087] <Justin Ridgewell>
I am looking at it? It should be available within `else`.

[08:29:53.0094] <bakkot>
yes

[08:29:59.0737] <bakkot>
I recognize that this is your intuition

[08:30:06.0675] <bakkot>
I would like you to acknowledge that other people have a different intuition

[08:30:14.0810] <bakkot>
not based on reasoning from `using`

[08:31:23.0128] <Justin Ridgewell>
I agree. This proposal only opens a confusing core case, with the goal of making `using` nicer in an `if`. I don't think that's worth it.

[08:32:09.0127] <Justin Ridgewell>
I do. But if they don't want to use the `x`, why would the write it in the `else`? Why would it ever refer to an outer scope when there's a inner scope that defines it?

[08:33:55.0868] <bakkot>
I am concerned primarily about people _reading_ code, not people writing code

[08:34:26.0002] <bakkot>
so they probably wouldn't! but if other people or AIs do, because they share your intuition or have learned that it works that way, they will encounter that, and be confused

[08:34:34.0147] <Justin Ridgewell>
> Why would it ever refer to an outer scope when there's a inner scope that defines it?

I think that applies whether you're reading or writing.

[08:34:59.0697] <bakkot>
if it is not legal to write, then no, it does not apply

[08:35:20.0100] <Michael Ficarra>
isn't it a catch-22? won't more devs use it as a primitive?

[08:35:30.0496] <rkirsling>
if a new binding were meant to apply in an `else` then it would be presumably defined next to the else, just like catch

[08:35:50.0878] <ljharb>
also bigint was never going to be used by double digits of devs, it was designed intentionally not to be. if it was "Integer", however, it would have been

[08:35:52.0376] <nicolo-ribaudo>
Yeah I think if it was a primitive devs would start defaulting to decimals

[08:36:00.0816] <nicolo-ribaudo>
Like they default to let instead of var, even if var is faster

[08:36:11.0491] <Michael Ficarra>
I agree with ACE, I would love a primitive, but an API is good enough as long as our path to an eventual primitive is clear.

[08:37:29.0073] <Richard Gibson>
is this a claim that the *spelling* of built-in function `BigInt` made the difference? Which isn't even encountered with `n`-suffix syntax?

[08:37:44.0615] <ljharb>
no, the spelling reflects the semantics, which is what made the difference

[08:37:52.0727] <Caio Lima>
In almost all languages I mentioned, Decimal is exposed through an Object-like way.

[08:37:55.0791] <eemeli>
[@aclaymore:matrix.org](https://matrix.to/#/@aclaymore:matrix.org) My question from the end of the queue: What benefit would you get from a spec-defined object Decimal?

[08:38:14.0335] <ljharb>
* no, the spelling reflects the semantics, which is what made the difference. the lack of free coercion between integer numbers and BigInt is related.

[08:38:37.0761] <Ashley Claymore>
Because having a standard is very useful. Is that not why we are here :D

[08:38:41.0801] <nicolo-ribaudo>
That libraries you depend on know how to deal with it rather than having to convert my decimal type to something that those libraries understand

[08:39:12.0603] <Richard Gibson>
each bigint literally is an arbitrary-precision integer number

[08:39:30.0619] <Michael Ficarra>
people always forget about the value of data interchange!

[08:39:45.0473] <Richard Gibson>
oh, you mean integer Number... n/m

[08:39:54.0767] <Richard Gibson>
* oh, you mean integral Number... n/m

[08:40:01.0023] <eemeli>
Isn't that problem solved by Amount?

[08:40:04.0526] <ljharb>
yes. and, you can't use bigints interchangeably in places that expect integer numbers.

[08:40:15.0696] <Caio Lima>
keith_miller: I think your question on queue were related to this, right?

[08:40:39.0476] <ljharb>
if bigint was designed differently, Number would be only used for non-integers today.

[08:40:58.0995] <ljharb>
* if bigint was designed differently, Number would be ~only~ primarily used for non-integers today in new code.

[08:42:27.0444] <keith_miller>
Would it? I'm not convinced. It'd probably be much slower unless BigInt were actually int32_t

[08:42:47.0489] <ljharb>
most people don't care about performance in that way ime.

[08:43:24.0458] <Richard Gibson>
I would expect int64 fast-paths

[08:43:56.0456] <keith_miller>
You'll never see int64 fast-paths because there's not enough bits in registers

[08:44:05.0645] <keith_miller>
Until we have 128 bit registers

[08:44:10.0897] <ljharb>
int32 fast paths then

[08:44:17.0490] <Olivier Flückiger>
that is exactly the problem.... that there will be this expectation...

[08:44:32.0362] <Olivier Flückiger>
without a guarantee

[08:44:37.0402] <Richard Gibson>
isn't this contradicted by the other languages that went that way?

[08:44:38.0599] <keith_miller>
Still not going to be any faster than a double unless it wraps around on overflow

[08:44:52.0642] <ljharb>
it doesn't have to be faster

[08:45:00.0131] <Caio Lima>
Well, one of the motivations of adding Decimal to the language is that it's being designed in a way that it can be potentially more performant than user-space Decimals. One point regarding data transportation here is that if there's a Decimal into the language, you can think in applications written in WASM where the transportation is essentially a pointer exchange.

[08:45:03.0534] <keith_miller>
They have types so they don't have to do boxing

[08:45:25.0571] <ljharb>
sure, that also applies to arbitrary integers/bigint tho

[08:45:40.0109] <keith_miller>
* They have static types so they don't have to do boxing

[08:47:04.0779] <keith_miller>
That's probably not going to happen. At the wasm layer it's going to be an externref. Wasm doesn't and probably will not in the foreseeable future have BigInts.

[08:47:38.0092] <ljharb>
does that mean it wouldn't happen with decimal either?

[08:47:39.0027] <Richard Gibson>
Python types are not static

[08:50:37.0900] <keith_miller>
AFAIK, Python does not inline arbitrary 64-bit integers even on 64-bit platforms

[08:51:06.0052] <keith_miller>
By pigeon hole principle it's just not possible

[08:55:49.0724] <bakkot>
this would be a reason not to do it; adding something which people actually use a lot but which is slower than the thing they'd otherwise do is _worse_ for the world than adding one that people don't use a lot

[08:56:39.0378] <ljharb>
although i think way more people would use a decimal primitive, i think enough would use a decimal object that your statement applies to both

[08:57:05.0651] <bakkot>
if people start using decimal for things that don't need it, that would be bad, yes

[08:57:23.0858] <bakkot>
however I am more optimistic; languages like Java have decimal and people only use it in places that actually warrant one, IME

[08:58:02.0377] <bakkot>
(well, most people only use it in places that warrant. there's always someone.)

[08:58:38.0796] <eemeli>
Queue is stuck?

[08:59:20.0267] <Michael Ficarra>
chairs are sleeping 🛌

[08:59:42.0972] <peetk>
ljharb can you elaborate on this: "I don't think that the object-based API solves it and I think it needs to be a primitive in order to solve it" (pulled from the notes so may not be word perfect) what aspect of the problem statement isn't solved by the object-based API?

[09:00:42.0443] <ljharb>
oh i'm sure the problem statement is currently carefully worded so the object ones technically solves it, but i see the problem statement as broad enough that it doesn't

[09:00:48.0791] <Caio Lima>
Regarding this question, module reasons we already talked, why do other languages provide a Decimal into their language? From Python's PEP (https://peps.python.org/pep-0327/) the motivation started to be a building block to implement Money, but they also find that it would be beneficial to have that type for other use-cases as well. I see that interactions with DB records having a Decimal type is quite valuable for applications where JS is being widely used (mostly server-side JS here). Would Amount be capable of interacting with this DB data interaction case, for example?

[09:01:05.0496] <ljharb>
math and numbers is too important and too basic to be objects.

[09:01:33.0109] <Michael Ficarra>
"math is basic" -- Jordan Harband, Fields medalist

[09:01:50.0508] <ljharb>
basic !== easy :-p

[09:01:53.0458] <peetk>
what do you see as the problem statement?

[09:02:04.0451] <Ashley Claymore>
Objects if anything can be more powerful than primitives

[09:02:31.0250] <ljharb>
i want to do normal math. with operators. by typing mostly numbers.

[09:02:51.0020] <Ashley Claymore>
Numbers are not being removed

[09:03:01.0382] <ljharb>
`Number` isn't normal math

[09:03:15.0197] <Ashley Claymore>
base10 is no more normal than base2 in maths

[09:03:19.0124] <peetk>
isn't that just a distinct problem?

[09:03:58.0430] <ljharb>
tell that to my fingers :-)

[09:04:01.0551] <bakkot>
if you are thinking of this as "I want to use it in in everyday life for normal things that I currently use Numbers for", we should not add it because it would be very bad if people start doing that

[09:04:12.0320] <ljharb>
why?

[09:04:21.0554] <bakkot>
because it will be much slower

[09:04:22.0169] <ljharb>
that is absolutely the long term goal i want

[09:04:28.0905] <bakkot>
then we should absolutely not do this

[09:04:31.0453] <Ashley Claymore>
Some of us care about performance

[09:04:38.0727] <Ashley Claymore>
* Some of us care about performance :D

[09:04:57.0999] <Michael Ficarra>
lots of machines have decimal128 instructions!

[09:05:17.0716] <ljharb>
i fail to understand why, for current common Number usages, it'd be meaningfully slower

[09:05:28.0371] <Ashley Claymore>
because we have to box in JS

[09:05:28.0654] <Caio Lima>
But this is mostly about expressivity and syntax ergonomics than the problem being solved 

[09:05:29.0285] <peetk>
Bloomberg has a clear need we've articulated and we are content that the Object API would solve it. you're objecting because you have a different problem that this doesn't solve? is that correct?

[09:05:33.0043] <ljharb>
if it's something i can run a trivial transpilation over and make fast, then browsers can just do that

[09:05:52.0106] <shu>
oh yeah like what

[09:06:03.0970] <ljharb>
i also think they're aspects of the same problem. the performance motivation was just contradicted above, so what more remains to prefer "not a library"?

[09:06:08.0110] <Caio Lima>
There's lack of hardware to use decimals instead of Numbers.

[09:06:09.0748] <Michael Ficarra>
some!

[09:06:15.0914] <ljharb>
yes, and those are important concerns

[09:06:17.0718] <shu>
uh huh

[09:06:36.0007] <ljharb>
sounds like that's the problem that needs solving then

[09:06:45.0820] <peetk>
there is value is standardizing on a format

[09:06:55.0207] <peetk>
* there is value in standardizing on a format

[09:06:58.0856] <bakkot>
no, to be clear, native can be more performant than a library, just not as performant as a Number

[09:07:10.0587] <ljharb>
sure. but we generally don't add something to the language merely to have a coherence point

[09:07:13.0040] <bakkot>
performance is a totally valid reason to do this if, and only if, it is replacing libraries and not Numbers

[09:07:42.0169] <ljharb>
since a library can be written in wasm, you're saying native would still be faster than that?

[09:07:49.0986] <ljharb>
* since a library can be written in wasm, you're saying native would still be faster than that? (like, guaranteed)

[09:08:25.0313] <peetk>
i would argue that this is a good enough reason on its own. this is a standards body!

[09:08:49.0038] <peetk>
if it were in the language you could trust that every library will handle them

[09:08:50.0208] <ljharb>
standards bodies don't exist just to churn out standards for standardization's sake tho

[09:09:07.0973] <ljharb>
* standards bodies don't exist just to churn out standards for standardization's sake tho (ideally)

[09:09:14.0554] <peetk>
do you think we're doing this for fun

[09:09:16.0808] <bakkot>
depends on the hardware and whether wasm ever gets native decimal operators and how smart the JIT is, so no I can't guarantee it, but typically yes.

[09:11:11.0463] <keith_miller>
Devs can do it and send it to browsers :)

[09:11:41.0820] <Caio Lima>
But still I have the question, why is it not common that Decimals aren't primitives on most of the languages that provides them in their STL? There should be a reason for that common design choice.

[09:12:21.0655] <ljharb>
why would that be better, to push the cost to servers that won't all do it rather than making the entire web faster by doing it in-browser?

[09:13:01.0110] <ljharb>
i can't speak to other languages, but JS is unique in many ways, and we have a lot of idioms that are "weird" when attempted in other languages.

[09:13:29.0671] <shu>
you think it's faster for the entire web to do something every page instead of once ahead of time?

[09:13:38.0207] <keith_miller>
It would be faster on the server

[09:13:44.0656] <ljharb>
touché

[09:13:45.0967] <Caio Lima>
From my studies, the reason is rooted on the case that Decimal should have their specific usage, while Nubmer (float64) is preferred for everything else.

[09:15:09.0025] <ljharb>
"should" is pretty load-bearing there

[09:15:28.0066] <ljharb>
certainly the performance/hardware argument holds, for the current time

[09:15:43.0995] <ljharb>
but in the fullness of time we'll have hardware where it no longer does. at that point why "should"?

[09:15:46.0684] <Caio Lima>
That's why I asked if "does the use cases we have are compelling enough?" If we think the usage is too niche, I'd like to hear a block concern on that. 

[09:15:49.0640] <bakkot>
is "bushel" an actual unit or just an example of a thing you could have an Amount of but that is probably not a real unit

[09:16:19.0610] <ljharb>
that's what i've been saying the entire life of this proposal - that if it's not going to be able to replace Number, its use case is imo too niche to add to the language

[09:16:33.0202] <dminor>
There's four pecks in a bushel

[09:16:36.0987] <ljharb>
> In the U.S., it is legally defined as 2,150.42 cubic inches or roughly 1.25 cubic feet (about 35.24 liters)

[09:16:47.0875] <bakkot>
:|

[09:16:54.0337] <peetk>
so asking again: Bloomberg has a clear need we've articulated and we are content that the object API solves it. are you a) objecting because you have a different problem that this doesn't solve? or b) you believe we are wrong that it would solve our problem? or c) something else?

[09:16:58.0190] <ljharb>
* > In the U.S., it is legally defined as 2,150.42 cubic inches or roughly 1.25 cubic feet (about 35.24 liters)
> the U.K. bushel is slightly larger, defined as 2,219.36 cubic inches (about 36.38 liters)

[09:17:48.0985] <ljharb>
i believe it would solve bloomberg's problem. i don't think "bloomberg's problem" is broad enough to warrant addition to the language on its own, and i guess i have a different problem this both doesn't solve and makes less likely to be solveable.

[09:18:45.0249] <ljharb>
either way i think it's clear we shouldn't have done BigInt

[09:18:57.0262] <peetk>
ok so you object to stage 1 even.

[09:19:00.0860] <ljharb>
* either way i think it's clear we shouldn't have done BigInt (which was also posited as solving a bloomberg problem)

[09:19:10.0063] <Richard Gibson>
it's an actual unit, and included in https://github.com/unicode-org/cldr/blob/main/common/supplemental/units.xml . "peck" is a related unit (four per bushel) that is not.

[09:19:15.0396] <keith_miller>
That doesn't seem like a stage 1 objection

[09:19:27.0299] <ljharb>
it was not my understanding that the stage 1 problem statement was so constrained so as to preclude the problem i wanted to solve

[09:19:55.0007] <Richard Gibson>
* "bushel" an actual unit, and included in https://github.com/unicode-org/cldr/blob/main/common/supplemental/units.xml . "peck" is a related unit (four per bushel) that is not.

[09:19:55.0883] <keith_miller>
* ~That doesn't seem like a stage 1 objection~

[09:19:58.0907] <ljharb>
also yeah i think it's still very much a stage 2 objection; it's fine to explore something narrow

[09:20:01.0399] <Richard Gibson>
* "bushel" is an actual unit, and included in https://github.com/unicode-org/cldr/blob/main/common/supplemental/units.xml . "peck" is a related unit (four per bushel) that is not.

[09:20:04.0064] <ljharb>
* also yeah i think it's still very much a stage 2 objection; it's fine to explore something narrow (which is stage 1)

[09:20:12.0859] <keith_miller>
Oh Matrix doesn't do markdown

[09:20:42.0052] <Michael Ficarra>
~it doesn't?~

[09:20:46.0178] <Michael Ficarra>
hmm

[09:21:58.0478] <ptomato>
<del>you can do it with `<del>`</del>

[09:22:14.0826] <ljharb>
also what got stage 1 was "BigDecimal" in 2020, and i think the proposal has evolved a lot over the years without reestablishing consensus (because it's not super important to do that within stage 1)

[09:22:19.0438] <ptomato>
<del>use `<del>`</del>

[09:24:14.0730] <peetk>
so if we re-brought a new proposal with the problem statement "we want a native means for doing precise decimal math so we can have a standard interchange format + perf benefits" you would block stage 1?

[09:24:17.0761] <ljharb>
but yeah i'm just not interested in the language having a non-primitive way to do math, and i've made that quite clear every time this has come up

[09:25:36.0585] <peetk>
* ljharb: so if we re-brought a new proposal with the problem statement "we want a native means for doing precise decimal math so we can have a standard interchange format + perf benefits" you would block stage 1?

[09:25:43.0093] <ljharb>
* but yeah i'm just not interested in the language having a non-primitive way to do generic math, and i've made that quite clear every time this has come up

[09:25:44.0109] <bakkot>
other people clearly do want that

[09:25:52.0370] <ljharb>
clearly

[09:26:18.0100] <ljharb>
there's a ton of things people clearly want, that aren't in the language because other delegates anti-want it

[09:26:28.0003] <eemeli>
Performance is explicitly not a use case or motivation for Decimal, right?

[09:26:46.0747] <ljharb>
* there's a ton of things people clearly want, that aren't in the language because other delegates anti-want it (including decimal primitives)

[09:26:58.0941] <mgaudet>
(Sorry, just a dumb side question: What does this interchange format actually end up looking like in practice? e.g. what does a JSON string with a Decimal value look like in practice)

[09:27:20.0064] <ljharb>
without JSON changing, i assume a string or some kind of custom object

[09:27:24.0344] <eemeli>
A JSON number, I presume?

[09:27:38.0011] <ljharb>
ah, yeah that'd work too, with the raw json stuff

[09:27:57.0039] <dminor>
That was my understanding, but it seems to keep coming up

[09:28:22.0848] <peetk>
when i say "interchange" i mean between JS libraries (although there would presumably be a choice made about how to serialize to JSON)

[09:28:57.0533] <ljharb>
why would such interchange be meaningfully better than "as a numeric string"?

[09:28:59.0768] <eemeli>
Number not being able to represent all JSON numbers is a motivation for Amount.

[09:31:04.0711] <eemeli>
That makes me think that if we end up with a JSON.parse options bag for the reviver, that should maybe include an option for how JSON numbers are represented.

[09:32:56.0094] <bakkot>
someone suggested that IIRC

[09:34:27.0446] <Chengzhong Wu>
strings can represent many things but it doesn't mean it is meaningfully optimal. This is why we need structured, semantic classes and objects

[09:35:04.0955] <Clément Pit-Claudel>


[Representative sample](https://blakeembrey.com/posts/2024-09-web-redos/) of the issue: Express.js/[path-to-regex](https://github.com/pillarjs/path-to-regexp) (8.6k stars on github) removing regex support entirely, due to ReDoS:

> For version 8, I've removed regex features. I know this is a huge pain for users of the library and apologize in advance.

[09:36:44.0980] <Ashley Claymore>
It is not the primary motivation.
But it is a motivation.

[09:36:45.0597] <mgaudet>
(to be clear, I'm not against the problem statement or anything here but one aspect that continues to confuse me is what interchange will actually look like, _especially_ across systems)

[09:37:21.0727] <peetk>
ljharb: i really hate to be a bother but do you mind answering my question at the top of this thread? genuinely i'm really trying to understand your objection

[09:37:52.0282] <ljharb>
i did answer it - https://matrix.to/#/!WgJwmjBNZEXhJnXHXw:matrix.org/$WZd9anGsMD1fcVnfxU9nLxWF47250NKbLa65aPfXHyY?via=matrix.org&via=mozilla.org&via=igalia.com

[09:38:24.0984] <peetk>
so that's a "yes" to "would you block my restatement of the problem for stage 1 if we brought it as a new proposal"?

[09:38:35.0693] <ljharb>
yes. "perf benefits" has been repeatedly debunked by browsers, and re "standard interchange format" i haven't been convinced that it'd be meaningfully better than "numeric strings" as an interchange format

[09:38:49.0076] <ljharb>
and i'm very disinterested in having a number system in the language that doesn't use primitives

[09:40:45.0277] <peetk>
is one delegate not being interested in something sufficient reason to block? (as opposed to, one delegate believing the proposal is harmful e.g.)

[09:40:58.0233] <Ashley Claymore>
In as much as, it's always a nice addition when there is the potential to not download a library and also to implement something natively.

[09:41:07.0814] <ljharb>
by saying "disinterested" and "antiwant" i'm saying that i think it would be harmful to have an object-based number system in the language

[09:41:23.0852] <Ashley Claymore>
One thing I'd really love to do is compare pure JS,Wasm,native implementations of Decimal128

[09:41:31.0789] <peetk>
thanks, i think it really helps to avoid euphemism in these contexts

[09:41:42.0822] <Ashley Claymore>
just to see what the relative overheads are

[09:42:04.0721] <Ashley Claymore>
* One thing I'd really love to do is compare pure JS/Wasm/Native implementations of Decimal128

[09:43:10.0545] <bakkot>
I can't think of way that subquadratic-superlinear matching comes up with regexes fwiw

[09:43:39.0327] <nicolo-ribaudo>
Good that the size of the regexp is usually constant and not dynamic

[09:43:59.0894] <peetk>
can you elaborate on what you think the harm would be?

[09:44:05.0295] <Richard Gibson>
counted quantifiers, plausibly

[09:44:06.0913] <nicolo-ribaudo>
* Good that the size of the regexp is usually constant for a given program and not dynamic depending on outside factors like user input or fs

[09:44:57.0623] <Richard Gibson>
* bounded-repetition quantifiers, plausibly

[09:46:22.0417] <nicolo-ribaudo>
Can we always run the two algorithms in parallel on two threads, and race them?

[09:59:59.0240] <bakkot>
oo generator is a fun idea

[10:05:18.0255] <bakkot>
I agree with Mark that timing channels are bad but strongly disagree that this means we must not add any new ones, they just need an adequate justification that I think there we have here 

[10:08:12.0049] <Aurèle Barrière>
Thank you for the exciting discussion! Have a good lunch

[10:09:33.0452] <Olivier Flückiger>
Btw. I do like the proposal, but am just not sure about the feasibility (or rather complexity vs. problem tradeoff).

[10:09:54.0783] <Olivier Flückiger>
It might be one of the proposals where early prototyping in actual engines would be really valuable for the discussion

[10:14:02.0476] <Richard Gibson>
I don't think it is justified here, especially since the resource budget *could* be deterministic

[10:14:45.0158] <Michael Ficarra>
TC53 would like to have a word

[10:16:39.0723] <Steve Hicks>
What about a way to interrupt a regex eval? That would push things like timing considerations outside but still allow libraries to provide a "safe eval with timeout" or whatever

[10:17:29.0551] <Richard Gibson>
yeah, that's space that would be opened up by a generator

[10:17:57.0962] <keith_miller>
Are the problematic regexes for these CVEs know to be risk sites before the CVE arrives?

[10:18:18.0036] <keith_miller>
* Are the problematic regexes for these CVEs known to be risk sites before the CVE arrives?

[10:18:28.0912] <bakkot>
often the CVE takes the form of "I was looking at your regex and noticed it could blow up"

[10:18:37.0958] <bakkot>
they are not usually known to the author to be risk sites

[10:18:47.0071] <bakkot>
but also they are not usually found by active exploitation

[10:19:02.0978] <keith_miller>
So, it sounds like any solution effectively has to be the default to be useful here?

[10:19:33.0004] <Steve Hicks>
generators can be awkward to use - a cancel token might be more ergonomic

[10:19:36.0505] <bakkot>
I think the existence of this feature would make people think about it more when writing, in a way they currently do not

[10:19:37.0110] <keith_miller>
* So, it sounds like any solution effectively has to be the default to be to solve the CVE problem here?

[10:19:57.0017] <bakkot>
cancel token only works if it's async which is impractical for many uses

[10:31:08.0756] <Caio Lima>
On summary, should we include discussions that happened after the timeslot, or just what got discussed on plenary?

[10:35:40.0176] <eemeli>
I would presume that referring to discussions on this channel in the summary would be fine.

[10:38:59.0052] <Lea Verou>
How would that look like?

[10:39:20.0417] <waldemar>
Rust's regex implementation is not linear in the size of the regex. Things like quantifiers and Unicode character classes cause problems. To prevent denial of service attacks via user-specified regular expressions, the engine includes an analogue of a timeout or resource limit that aborts on some regular expressions.

[10:43:59.0023] <bakkot>
looks like they document this as being linear in the size of the regex "where the size of the regex includes the expansion of all counted repetitions". there are finitely many character classes so those are definitionally at most constant size although the factor can of course be large


[10:44:26.0681] <Lea Verou>
The generator version would also let authors turn it into async, or use `scheduler.yield()` to yield to the event loop etc. It's not just a decision about allowing vs aborting. It could also be UX mitigation, i.e. "I still want the answer, but this is taking too long so I need to plan accordingly and communicate this to the user"

[10:45:06.0630] <Lea Verou>
* The generator version would also let authors turn it into async, or use `scheduler.yield()` to yield to the event loop etc. It's not just about allowing vs aborting, it's also about _handling_. It could also be UX mitigation, i.e. "I still want the answer, but this is taking too long so I need to plan accordingly and communicate this to the user"

[10:45:24.0166] <Lea Verou>
* The generator version would also let authors turn it into async, or use `scheduler.yield()` to yield to the event loop etc. It's not just about allowing vs aborting, it's also about _handling_. It could also be used for UX mitigation, i.e. "I still want the answer, but this is taking too long so I need to plan accordingly and communicate this to the user"

[10:46:47.0438] <Lea Verou>
It's not just about vulnerabilities either, there are well-intentioned inputs (for either a user-generated regex or the matched string) that be slow to match 

[10:46:52.0694] <Lea Verou>
* It's not just about vulnerabilities either, there are well-intentioned inputs (for either a user-generated regex or the matched string) that can be slow to match 

[10:47:41.0049] <Lea Verou>
I agree that generators are awkward to use, but that's something we should solve separately :)

[10:59:02.0467] <waldemar>
Yes, "a{9}{9}{9}{9}{9}{9}{9}" counts as a 4782969-character regular expression in Rust.

[11:25:44.0588] <Michael Ficarra>
it's almost like we should have done private symbols in the first place...

[11:26:58.0255] <peetk>
`export for` in general would be interesting

[11:27:33.0300] <ptomato>
side note, there's a slightly easier way to export module-scoped access to private fields than what was in the slide (but still I'd prefer private declarations)
```js
let myField;
class MyClass {
  #myField;
  // ...
  static #exportedMyField(instance) {
    return instance.#myField;
  }
  static {
    myField = MyClass.#exportedMyField;
  }
}
```

[11:33:22.0799] <ptomato>
(well, I guess my way is more verbose, so easier is subjective, but it doesn't require the classes to be nested)

[11:33:34.0710] <Justin Ridgewell>
This is a mistake we made when adding class fields. It would have been much cleaner if we required `class { private #x = 1 }` to declare in the private field. Then `class { #x = 2 }` could just refer to the outer.

[11:34:11.0152] <ljharb>
literally anything could work eg `{ gimme #x: 3 }`

[11:34:29.0097] <ljharb>
i would have objected to the spelling "private" but i would have preferred some keyword, yes

[11:36:09.0391] <eemeli>
Csv we extend the timebox given that we're ahead of schedule?

[11:36:19.0054] <eemeli>
* Can we extend the timebox given that we're ahead of schedule?

[11:36:38.0049] <Michael Ficarra>
I think Kevin might prefer to spend the time on unblocking async iterator helpers

[11:37:16.0017] <James M Snell>
V8 (I don't know about other runtimes) have had the private symbol registry for a while.. e.g. `Symbol::ForAPI(...)` in the embedder API. Would much prefer an approach like that with the ability to restrict private symbols from being iterated using getOwnPropertySymbols ... So... something like `const x = Symbol.private('foo');` ... and private symbols don't enumerate but otherwise act just like other symbols

[11:37:35.0652] <Chris de Almeida>
we still have consensus items to get to, so it would be better to instead do a continuation later if possible

[11:37:53.0573] <James M Snell>
Basically think we can address the use case without any new syntax

[11:38:07.0111] <Chris de Almeida>
if the queue was not so large, sure

[11:38:43.0684] <ljharb>
we can, but the ergonomics would be worse - so why?

[11:38:53.0267] <ljharb>
(we can do both in either order, also)

[11:38:59.0645] <Michael Ficarra>
This being a global registry defeats the visibility restriction, right?

[11:39:04.0708] <ljharb>
although the "no new way to add a private symbol to an object" makes it trickier

[11:39:26.0976] <James M Snell>
No, `ForAPI(...)` is a global registry that is not exposed to user code

[11:39:36.0922] <James M Snell>
it's distinct from `Symbol.for(...)`

[11:39:50.0626] <Michael Ficarra>
guess it was the other way around

[11:40:00.0050] <ljharb>
i don't think it'd be better to have it be string-based tho

[11:40:14.0351] <ljharb>
two totally different files could make a private symbol "foo" and shouldn't be able to access each other's data

[11:40:22.0674] <ljharb>
and without syntax, it couldn't be lexical

[11:40:29.0136] <ljharb>
* and without syntax, it couldn't be lexical (which is why private fields are syntax0

[11:40:30.0808] <ljharb>
* and without syntax, it couldn't be lexical (which is why private fields are syntax)

[11:40:36.0710] <James M Snell>
`Symbol.private('...')` would not be able to give you from a registry... just keying off the same basic idea as ForAPI in that it's not accessible to users unless specifically handed to them

[11:41:18.0716] <James M Snell>
In any case, I'm +1 on Stage 1 but I think the approach should be fundamentally different than what is being proposed

[11:41:32.0657] <ljharb>
what's wrong with using syntax tho?

[11:41:33.0814] <Chengzhong Wu>
there are two methods to get a private symbol in V8 https://github.com/v8/v8/blob/3105a7cb526e310f2cf996769ba430a99619258a/include/v8-object.h#L53-L68 . Basically the equivalent to `Symbol(...)` and `Symbol.for(...)`

[11:41:34.0211] <Michael Ficarra>
it's already stage one 😅

[11:41:40.0201] <ljharb>
it could still become private symbols later

[11:42:14.0824] <James M Snell>
Typical issues with introducing new syntax, etc,etc... can be solved with API rather than syntax

[11:42:28.0635] <ljharb>
"confining it to lexical scope" requires syntax tho

[11:42:59.0040] <ljharb>
* "confining it to lexical scope" requires syntax tho. and that's a feature of this proposal imo, that you *can't* hand it out

[11:43:30.0179] <Michael Ficarra>
"a thing that is exactly like private fields but reified" 😍😍😍😍😍

[11:43:40.0694] <keith_miller>
Olivier Flückiger: "what does private #hello get you couldn't get from const hello = Symbol("hello")" It's sugar around that with defining everything non-enumerable

[11:43:45.0319] <ljharb>
eemeli: yes

[11:44:29.0078] <Olivier Flückiger>
I think it's more. private also does not show up getOwnProperty

[11:44:55.0553] <ljharb>
all symbols are public

[11:45:15.0485] <keith_miller>
That's because there's not an identifier? If you don't have the symbol then you can't `getOwnProperty`

[11:45:33.0433] <keith_miller>
Maybe there's some thing I'm missing from private fields

[11:45:51.0479] <ljharb>
`Object.getOwnPropertySymbols` exists

[11:45:59.0922] <ljharb>
so all symbols on an object are always reachable

[11:46:04.0455] <ljharb>
(also Reflect.ownKeys0

[11:46:05.0405] <keith_miller>
Does that give you non-enumerable?

[11:46:07.0270] <ljharb>
* (also Reflect.ownKeys)

[11:46:07.0862] <keith_miller>
Oh ok

[11:46:08.0350] <ljharb>
yes

[11:46:19.0588] <ljharb>
enumerability of symbols only impacts object spread/Object.assign

[11:46:30.0045] <Steve Hicks>
FWIW, not having "final" (classes and methods) also makes invariants impossible in nearly any case where you're dealing with classes.

[11:46:37.0155] <ljharb>
James M Snell: no, it does not ignore enumerable false

[11:46:46.0351] <Caio Lima>
Can't Proxies leak symbols on access? Or do proxies skips non-enumerable?

[11:46:54.0576] <ljharb>
not as of the introduction of private fields.

[11:47:00.0196] <ljharb>
proxies leak them also, yes.

[11:47:05.0225] <ljharb>
all symbols are fully public in every case full stop

[11:47:24.0741] <ljharb>
* James M Snell: no, gOPS does not ignore enumerable false

[11:47:25.0066] <keith_miller>
Well with export it it's not since a module can't decide who can import it

[11:47:39.0555] <keith_miller>
* Well with export it it's not very private since a module can't decide who can import it

[11:47:55.0067] <ljharb>
exporting it is nonviable, i have a later topic about that

[11:49:02.0685] <keith_miller>
It also seems like it's conflating two goals for private to export it

[11:49:03.0817] <James M Snell>
```
> s = Symbol()
Symbol()
> m = {}
{}
> Object.defineProperty(m, s, { value: 1, enumerable: false })
{}
> Object.getOwnPropertySymbols(m)
[ Symbol() ]
> Object.getOwnPropertyDescriptor(m, s)
{ value: 1, writable: false, enumerable: false, configurable: false }
> m[Object.getOwnPropertySymbols(m)[0]]
1
```

[11:49:28.0199] <ljharb>
line 8 shows that it's there, yes

[11:49:36.0975] <ljharb>
(also Reflect.ownKeys includes non-enumerable symbols also)

[11:49:45.0114] <James M Snell>
That's what I'm referring to

[11:50:03.0507] <ljharb>
that snippet demonstrates that Object.getOwnPropertySymbols includes nonenumerable symbols

[11:50:18.0768] <James M Snell>
Yes, that's what I'm saying

[11:50:21.0964] <ljharb>
ah ok

[11:50:26.0908] <ljharb>
your queue item implied the opposite?

[11:50:36.0689] <ljharb>
* your queue item implied the opposite? maybe i misread

[11:51:12.0023] <James M Snell>
maybe I misworded... the point being, we need private symbols that do NOT show up in enumeration so they can actually be private

[11:51:34.0024] <ljharb>
ok agreed :-)

[11:52:36.0694] <ptomato>
I think private declarations as Kevin is proposing would be better than symbol property descriptors with `enumerableNoReallyIMeanItThisTime: true`

[11:52:52.0953] <James M Snell>
If we eventually have syntax for making symbols easier, then cool, but we can address this with API ... `Symbol.private(...)` return a regular Symbol that is truly not enumerated

[11:53:16.0946] <ljharb>
then you'd be able to add it to arbitrary existing objects. and that's an anti-goal

[11:53:32.0708] <Olivier Flückiger>
I think there are arguments for both. a private symbol is a more primitive concept that you could use to build this and also many other things with it

[11:53:46.0050] <James M Snell>
would require an additional slot on `Symbol` and a tweak to getOwnPropertySymbols/R.ownKeys

[11:53:58.0451] <ptomato>
why would we add that additional primitive concept when we already have the concept of private fields

[11:54:06.0017] <Caio Lima>
How would it work on scope? Every call of Symol.private("foo") return a different Symbol?

[11:54:08.0881] <James M Snell>
I don't see this as a problem. We do this in node.js cases and it works just fine

[11:54:11.0265] <bakkot>
eemeli: `private #x;` introduces a `#x` into the nested scope, such that you can refer to it in that scope. So does `class { #x }`. that's already how it works. 

[11:54:15.0115] <Michael Ficarra>
FYI: whenever we end an agenda items with a non-empty queue, the queue is captured in the `Log` tab (press 3 in TCQ)

[11:54:18.0365] <ljharb>
that's the point. without being syntax, it can't work

[11:54:30.0903] <James M Snell>
Yes, just like `Symbol(...)`

[11:54:32.0512] <bakkot>
the `private #x` syntax is new, but the "introduce a `#x` you can refer to in that scope" is just how private fields work

[11:54:45.0933] <ljharb>
"works just fine" is subjective, and doesn't mean it'd work when everyone has access to the feature

[11:54:50.0581] <James M Snell>
only difference is it doesn't show up in enumerations at all

[11:54:56.0652] <Olivier Flückiger>
huh, `#private hello` is literally `const hello = Symbol.private()`

[11:55:02.0791] <Olivier Flückiger>
nothing else

[11:55:10.0440] <ljharb>
it'd still leak with proxies though

[11:55:21.0533] <Chengzhong Wu>
it can not do `obj.foo = #hello`

[11:55:37.0080] <ljharb>
* it'd still leak with proxies though, including with `privateSymbol in arbitraryObject`

[11:55:41.0134] <Olivier Flückiger>
ok, so it's a untouchable object?

[11:55:52.0675] <ljharb>
it's not an object, it's an unreified PropertyKey

[11:55:52.0950] <Chengzhong Wu>
like, not a first class value

[11:56:00.0562] <ljharb>
* it's not an object, it's an unreified PropertyKey - a PrivateName, which is already in the spec

[11:56:02.0415] <Chengzhong Wu>
* like, not a first class value, is it?

[11:56:05.0196] <ljharb>
no

[11:56:16.0890] <ljharb>
* it'd still leak with proxies though, including with `privateSymbol in arbitraryObject`, which imo makes it a bit of a nonstarter

[11:56:38.0194] <Olivier Flückiger>
that is the def. of a non-firstclass. it's a unique value but you can only refer to it in some places

[11:56:47.0436] <James M Snell>
I guess I'm less concerned with proxies and less concerned with making it fool proof... would prefer we just pave existing path rather than invent new syntax. Just me tho

[11:57:08.0178] <ljharb>
i get that. but "making it foolproof" is the reason private fields are already so valuable

[11:57:09.0636] <bakkot>
if you read the slide it had `export { #foo } for 'importer.js'` so you would be deciding who can import it for exactly that reason

[11:57:24.0953] <ljharb>
(and why we're better than every other language because we actually have private state that can't be runtime-introspected)

[11:57:45.0770] <Olivier Flückiger>
meta comment: I think our timeboxes feel way to small this time.

[11:57:55.0582] <ljharb>
* (and why we're better than every other language because we actually have private state that can't be runtime-introspected, whereas every other language has "_DO_NOT_USE_THIS_OR_BE_FIRED")

[11:58:05.0915] <Michael Ficarra>
I think it was only KG's timeboxes that were way too small

[11:58:07.0563] <Caio Lima>
There's also the early error of used but not defined that private fields brings. With Symbols it needs to become a runtime error.

[11:58:26.0757] <bakkot>
I did not expect this one to be controversial

[11:58:34.0951] <Chris de Almeida>
yeah, but we still always want to capture it because chairs often drain the queue before moving on.  (cognitive momentum to TCQ Legendary Edition (LE))

[11:58:40.0855] <bakkot>
I should have started with a recap of "what are private fields and why do we have them"

[11:58:46.0319] <bakkot>
because it seems like there is a lot of confusion about those questions

[11:58:46.0678] <Chris de Almeida>
* yeah, but we still always want to capture it because chairs often drain the queue before moving on.  (cognitive momentum from TCQ Legendary Edition (LE))

[11:59:12.0707] <Michael Ficarra>
hopefully you're at least using the copy queue button

[11:59:24.0706] <Chris de Almeida>
I mean..  _I_ am

[11:59:52.0111] <Chengzhong Wu>
is there a copy queue button?

[11:59:52.0527] <ljharb>
for one, i think it would make it much more likely we'll never have proper numerical math primitives, and that would be an incalculable harm

[12:00:08.0912] <Michael Ficarra>
chairs have all sorts of features, which includes a copy queue button, yes

[12:00:13.0690] <Lea Verou>
What I was going to say, in case it triggers any useful ideas:

Similar concerns as eemeli . Without this being first-class there are too many open questions about how these behave. E.g. `Object.assign()`, `Object.defineProperty()`, exports etc. 

But the motivation seems clear, many authors already use symbols that way and the fact that they are exposed via getOwnPropertySymbols() is a surprise to some. But like eemeli , I do wonder if we need a whole new primitive for this however.

First, is lexical scope actually part of the requirements? Or just the ability to assign properties that are not visible to the outside unless explicitly shared, essentially like a more ergonomic version of the weakmap pattern?

Seeing the slides my mental model was that this looks a lot like symbols with
(a) more ergonomic syntax, that 
(b) cannot be accessed via the usual ways of accessing symbols

I wonder if it would be expressed in those terms, then it becomes predictable and the semantics fall directly out of that. Exports work, etc.
With a new descriptor, it could be completely decoupled from syntax and even be applied to string properties too. Then `#foo` is only syntactic sugar to create and register such symbols that automatically have the right descriptor (just like symbols are automatically `enumerable: false`)

[12:00:22.0157] <bakkot>
the point is, we already have those. that is already what private fields are

[12:00:29.0523] <bakkot>
this proposal does not change that, at all, in any way

[12:00:56.0497] <Olivier Flückiger>
really? how would we catch
```
private #foo
class NotFoo {}
f = new NotFoo()
f.#foo
```

[12:01:14.0293] <ljharb>
obv that'd be a runtime error

[12:01:31.0589] <eemeli>
My problem with this is that it forces me to think of `#x` as a thing, while in the current code I can think of just the `#` as syntax for "private", and `x` as the field name.

[12:02:04.0654] <ljharb>
why as a thing? it's just like a variable

[12:02:12.0463] <ljharb>
does `var x;` make you think of `x` as a thing?

[12:02:18.0062] <ljharb>
* does `let x;` make you think of `x` as a thing?

[12:02:43.0576] <Caio Lima>
You declared `#foo` there. you can't just have `this.#foo` without a declaration.

[12:03:08.0877] <Olivier Flückiger>
same with `const foo = Symbol()`

[12:03:14.0784] <eemeli>
Yeah, `x` there is something that is a first class thing, and that's completely fine.

[12:03:24.0155] <bakkot>
did `#x in foo` not already do that?

[12:03:35.0360] <ljharb>
no it's not. `x` is a second-class thing that refers to a first-class value

[12:03:36.0444] <Justin Ridgewell>
^ Yah, that'll runtime error when because `NotFoo` doesn't declare the field.

[12:03:59.0917] <ljharb>
* no it's not. `x` is a second-class thing that refers to a first-class value. you can't pass around the binding such that `x = somethingElse` automatically exposes `somethingElse`

[12:04:11.0281] <Justin Ridgewell>
* ^ Yah, that'll runtime error when you access `f.#foo` because `NotFoo` doesn't declare the field.

[12:04:38.0866] <ljharb>
* no it's not. `x` is a second-class thing that refers to a first-class value. you can't pass around the binding such that `x = somethingElse` automatically exposes `somethingElse` to the holder of `x`

[12:04:44.0851] <Olivier Flückiger>
exactly, and that is why this is not fully syntactic and not fully first-class either

[12:04:55.0479] <ljharb>
it's not fully syntactic in class fields already

[12:05:08.0101] <ljharb>
`static is(x) { return #foo in x; }` is a runtime error, already

[12:05:20.0697] <Caio Lima>
Not even with let declarations, given TDZ

[12:05:26.0838] <ljharb>
* `static get(x) { return x.#foo; }` is a runtime error, already

[12:05:31.0294] <ljharb>
* `static get(x) { return x.#foo; }` is a runtime error when it doesn't have the field, already

[12:05:38.0929] <bakkot>
I should really write up the spec text; I think it may help clarify the extent to which this is not adding any new kinds of thing at all on top of how private fields already work

[12:05:40.0708] <ljharb>
(and const and super)

[12:06:26.0164] <Michael Ficarra>
I don't think anyone reads the proposal spec text. Even this meeting, I feel like I was the only one who actually read the spec text for numerous agenda items.

[12:07:05.0081] <eemeli>
Here too the `#` parses as syntax in my head. This is of course a rather subjective opinion.

[12:07:16.0980] <bakkot>
it... is syntax?

[12:07:33.0469] <ljharb>
the presence of the `#` should indicate it's syntax yes. same in all of kevin's slides examples

[12:07:40.0151] <Michael Ficarra>
lol wait what would a `#` be other than syntax?

[12:08:34.0169] <James M Snell>
fwiw, I 100% agree with things like `{ [#foo]: 1 }` to declare a private in a non-class object... I'm only having a problem with the approach `private #foo; class Foo { [#foo] = 1 }; let f= f = new Foo(); console.lof(f.#foo)` ... I think that should use a `Symbol.private(...)` type approach instead... treat them differently, not as regular private fields. But again, just one opinion. I can be swayed and shouldn't be considered blocking.

[12:08:49.0968] <James M Snell>
* fwiw, I 100% agree with things like `{ [#foo]: 1 }` to declare a private in a non-class object... I'm only having a problem with the approach `private #foo; class Foo { [#foo] = 1 }; let f= f = new Foo(); console.log(f.#foo)` ... I think that should use a `Symbol.private(...)` type approach instead... treat them differently, not as regular private fields. But again, just one opinion. I can be swayed and shouldn't be considered blocking.

[12:09:11.0215] <bakkot>
I am very strongly opposed to having a different kind of private field on top of ones we already have

[12:09:23.0256] <Justin Ridgewell>
I think if we need to involve Symbols, this proposal will die

[12:09:32.0313] <Justin Ridgewell>
Any reification into values will kill it

[12:10:22.0957] <eemeli>
The place where it smells to me like too much of a new thing is specifically `private #x`, which reads like the declaration of `#x` as something novel.

[12:12:08.0051] <Olivier Flückiger>
btw. I am also not blocking here. I think the main thing I trip over is that it's kind of a novel way of doing it, so I would like to see a bit of a discussion why it can't be done some "traditional" way, e.g. friend classes or private symbols, etc.. I understand there are probably technical problems with all of them. but it should be discussed.

[12:12:38.0459] <Justin Ridgewell>
This is the mistake. It should have always been `private #x` to declare within a scope, even for class fields.

[12:13:10.0340] <Justin Ridgewell>
Then it wouldn't be different that I've declared in my function scope vs my class scope.

[12:13:12.0778] <bakkot>
I did try to push for that at the last minute

[12:13:16.0972] <bakkot>
but it didn't go through, alas

[12:14:05.0707] <Justin Ridgewell>
We could try it again. Redefine `private #x; class { #x = 1 }` to refer to the outer field. You need `class { private #x = 1 }` to get a different inner field.

[12:14:26.0769] <Justin Ridgewell>
There is a change in semantics for nested classes that use the same, but honestly who is doing that?

[12:14:45.0647] <bakkot>
problem is that `class { #x = 1; static { class { #x = 2 } }` already shadows rather than being a reference to the outer

[12:15:01.0563] <bakkot>
fair, it is possible we could get away with changing it

[12:15:25.0991] <bakkot>
that said it does feel weird that `#x = 1` would sometimes be a declaration and sometimes just a reference, so I don't love it

[12:15:42.0115] <ljharb>
i think there should be a way to declare it

[12:15:51.0484] <ljharb>
and even if we can't change classes, i think we should make that new way work in class bodies too

[12:16:01.0364] <ljharb>
* and even if we can't change classes, i think we should make that new way work in class bodies too (and i still don't like the spelling "private")

[12:16:17.0564] <Lea Verou>
Why?

[12:17:08.0988] <bakkot>
for one thing, that would expand the scope dramatically

[12:17:17.0602] <bakkot>
and I don't think it warrants such a large change

[12:17:22.0044] <Justin Ridgewell>
Because reification breaks hard private requirement. We required only syntax to ensure it works correctly.

[12:17:26.0510] <eemeli>
Would it work to allow for `class { protected #x }`, so that `class { #y }` would effectively become sugar for `class { private #y }`?

[12:17:58.0015] <bakkot>
it doesn't _necessarily_ but it opens up a bunch of annoying questions like "can proxies intercept it"

[12:18:17.0681] <bakkot>
and some possible answers to those questions would break privacy

[12:18:21.0386] <eemeli>
Where "protected" here would mean that the field is available in the lexical context or whatever.

[12:18:22.0164] <Justin Ridgewell>
`protected` here wouldn't work like every other language, though. You can't late declare a class to refer to the `#x`, it would have to be lexical.

[12:18:24.0594] <Lea Verou>
How so?

[12:18:44.0045] <eemeli>
Some other word then.

[12:18:55.0081] <ljharb>
no, "protected" is wholly inappropriate for javascript

[12:19:06.0647] <Justin Ridgewell>
This is what I dealt with when I tried for Private Symbols. It's a very difficult question to address.

[12:19:09.0680] <ljharb>
"protected" (and "private") implies "access levels" which isn't how JS classes work

[12:19:35.0827] <bakkot>
I'm not totally sure what the suggestion is. `class { #y }` is pretty much already sugar for `class { private #y }` in the proposal as it stands

[12:20:17.0591] <Lea Verou>
Ah, right. Darn. 

[12:20:22.0788] <Olivier Flückiger>
I guess if we wanted symmetry we could also use `#foo` as the declaration in any script

[12:20:31.0937] <Olivier Flückiger>
* I guess if we wanted symmetry we could also use `#foo` as the declaration in any scope

[12:20:59.0639] <eemeli>
Suggestion is for the visibility declaration to be a part of the field declaration, not wholly separate from it.

[12:21:41.0188] <Lea Verou>
I suppose making these invisible to proxy traps would be a no-go?

[12:22:14.0317] <Justin Ridgewell>
It breaks membrane transparency when the value is intentionally passed between graphs.

[12:22:24.0630] <ljharb>
(the only intended use case for proxies, iow)

[12:22:54.0748] <bakkot>
sorry, I still don't understand; can you spell it out a little more?

[12:23:16.0610] <Justin Ridgewell>
I had like a 100 slide presentation trying to cover all the cases for how to do it. It becomes incredibly complex.

[12:23:52.0933] <bakkot>
also _we settled the question already_

[12:23:55.0146] <Michael Ficarra>
@sffc:mozilla.org me an JHD

[12:24:10.0463] <Lea Verou>
In the sprit of "code only sees these if it has a reference to them" what if you could supply a list of private symbols you have references to when creating the proxy, and any others are invisible to it and just automatically reflect?

[12:24:24.0579] <Justin Ridgewell>
https://docs.google.com/presentation/d/1HWM_pREmpz7QN9VrNeFt5T-x6CfKBL06pXhiQcxgfgU/edit?slide=id.g487eecbd48_0_123#slide=id.g487eecbd48_0_123 is the slidedeck _after_ I pared it back.

[12:24:54.0217] <Michael Ficarra>
@ljharb:matrix.org ooh good reminder, I almost forgot

[12:25:24.0331] <Justin Ridgewell>
* https://docs.google.com/presentation/d/1HWM_pREmpz7QN9VrNeFt5T-x6CfKBL06pXhiQcxgfgU/edit?slide=id.gc6f73a04f_0_0#slide=id.gc6f73a04f_0_0 is the slidedeck _after_ I pared it back.

[12:29:38.0916] <Justin Ridgewell>
* There is a change in semantics for nested classes that redeclares the same private, but honestly who is doing that?

[12:30:09.0449] <eemeli>
I think it'd be weird to have this: 
```
private #x
class A { static #x }
class B { static #x }
```
Where the visibility of `A.#x` and `B.#x` are both affected by the `private` declaration. It would be much clearer if the visibility of `A.#x` and `B.#x` were defined separately, with syntax immediately adjacent to their definitions as class fields.

[12:32:28.0746] <bakkot>
ah. I agree that we should not have the visibility of `A.#x` or `B.#x` affected by the `private` declaration and am not proposing it that it would

[12:32:30.0421] <eemeli>
So maybe this? 
```
class A { static outer #x }
class B { static outer #x }
```

[12:33:59.0299] <bakkot>
yeah, you'd need something like that to refer to the outer field

[12:34:22.0420] <bakkot>
I had it as `class A { static [#x] }` but this seems to have been confusing so probably some sort of keyword

[12:34:26.0139] <bakkot>
`outer` or `with` or something

[12:34:54.0524] <rbuckton>
One of these days I should propose `Symbol.geti`/`seti` for property get/set inversion. Originally considered for reified Slice/Index-from-end, it could also be used to allow `WeakMap` as a key using element access/computed property names. 

[12:38:14.0398] <Steve Hicks>
If this is an error
```
private #x;
class A { #x; [#x]; }
```
then it's not a big stretch to my mind for
```
private #x;
class A { #x; }
```
to also be an error.  In which case, why do we need another keyword, just use `#x` as-is to refer to the outer scope.  (admittedly, this is a bit of a straw man, since I don't think the first block should be an error, and I'd rather get more clarity on what exactly the scoping and shadowing rules are)

[12:39:19.0032] <Justin Ridgewell>
mgaudet: You're causing an echo

[12:39:51.0359] <Steve Hicks>
In particular
```
private #x;
{ private #x; }
```
Is this allowed?  It seems equivalent to
```
class Outer {
  #x;
  static { class Inner { #x; } }
}
```
which is definitely allowed currently.

[12:41:08.0762] <bakkot>
the error in the first case happens at _runtime_, when instantiating the class because it will try to install the inner field twice

[12:41:19.0932] <bakkot>
the `private #x` doesn't have anything to do with it in this case

[12:41:42.0849] <bakkot>
`class A { #x; [#x] }` is an error for the same reason

[12:42:11.0401] <Steve Hicks>
I guess what I'm saying is - why is that "the same" field?  One is #x and the other is [#x], aren't these completely different things?

[12:42:37.0151] <bakkot>
the same reason that `a.b` and `a['b']` are the same field?

[12:43:34.0664] <Justin Ridgewell>
You're redeclared an inner `#x`. The same way `let x; { let x; obj[x] }` refers to the inner x.

[12:44:01.0685] <peetk>
but there's no string `"#x"` in this case. one is a class-private-field-thing and one is a module-private-field-thing. they (plausibly) are different kinds of things

[12:44:17.0629] <Steve Hicks>
I think we have a different mental model for `private #x`.  What about `private #x; { private #x; }` are these the same thing?

[12:44:44.0261] <bakkot>
no

[12:44:49.0452] <Justin Ridgewell>
No

[12:44:55.0442] <bakkot>
same reason that `let x; { let x; }` are not the same thing

[12:44:59.0014] <Steve Hicks>
So why is it the same when the inner one is a class?

[12:45:02.0724] <Justin Ridgewell>
The same way `let x; { let x }` aren't the same var.

[12:45:07.0949] <bakkot>
or `class A { #x; static { class B { #x } }` for that matter

[12:45:17.0621] <bakkot>
* or `class A { #x; static { class B { #x } } }` for that matter

[12:45:32.0505] <Steve Hicks>
But you're saying that it _is_ the same in `private #x; class A { #x }`?

[12:45:51.0949] <bakkot>
no

[12:45:55.0651] <bakkot>
those are also different fields

[12:45:59.0657] <bakkot>
in literally exactly the same way

[12:46:13.0294] <Steve Hicks>
So why is `private #x; class A { #x; [#x]; }` an error?

[12:46:41.0465] <Justin Ridgewell>
Because the same private name can't be installed on the same object twice.

[12:46:48.0784] <bakkot>
for the same reason that `class A { #x; [#x] }` is an error

[12:46:52.0650] <bakkot>
the `private #x` has nothing to do with this

[12:46:54.0910] <peetk>
but they're not the same lmao

[12:47:06.0735] <peetk>
^

[12:47:08.0560] <bakkot>
it's just that `class A { #x; [#x] }` means "install #x on these instances twice"

[12:47:12.0002] <bakkot>
and that's an error

[12:47:31.0780] <Steve Hicks>
okay so we're interpreting the not-yet-defined `[#x]` syntax differently

[12:47:32.0898] <bakkot>
the `private #x` declaration does not come into play in any way

[12:47:36.0047] <ljharb>
(wouldn't be if one was x and one was y tho)

[12:47:40.0556] <Steve Hicks>
I think that's the disconnect

[12:48:15.0432] <keith_miller>
How much value do we get from shadowing? is it something we'd be willing to kill for `private #x`

[12:48:46.0219] <bakkot>
I get no value from shadowing and would be happy to kill it

[12:48:54.0753] <bakkot>
but we do already have it for classes

[12:48:59.0699] <bakkot>
so I don't see that much value in killing it either

[12:49:02.0023] <keith_miller>
As in `private #x class { #x = 1 }` refers to the outer

[12:49:19.0305] <keith_miller>
Yeah, idk maybe that's confusing

[12:49:43.0906] <bakkot>
oh, I don't love making `#x = 1` sometimes a declaration and sometimes a reference

[12:49:49.0182] <bakkot>
I'd be OK with making that line an error though

[12:49:51.0073] <Steve Hicks>
I guess I don't see `class { #x }` as defining a "thing" #x in the same way that `private #x` does precisely because the latter allows `[#x]` but the former doesn't.

[12:49:53.0021] <Justin Ridgewell>
The double initialization error already exists: 
```
class Base {
    constructor(o) {
        return o;
    }
}
class Sub extends Base {
    #x;
    constructor(o, v) {
        super(o);
        this.#x = v;
    }
}
const o = {};
new Sub(o, 1);
new Sub(o, 2);

VM433:7 Uncaught TypeError: Cannot initialize #x twice on the same object
    at <instance_members_initializer> (<anonymous>:7:5)
    at new Sub (<anonymous>:9:9)
    at <anonymous>:15:1
<instance_members_initializer> @ VM433:7
Sub @ VM433:9
(anonymous) @ VM433:15
```

[12:50:06.0767] <Justin Ridgewell>
This is using the super override trick.

[12:51:29.0682] <bakkot>
sorry, to be clear, they both would allow `[#x`

[12:51:33.0561] <bakkot>
* sorry, to be clear, they both would allow `[#x]`

[12:51:37.0569] <bakkot>
I didn't cover this and I guess I should have

[12:51:40.0561] <Justin Ridgewell>
You never need it in the former case, though? You only need it for classes so that you install the field at declaration.

[12:51:52.0781] <bakkot>
but the point is that `private #x` introduces a name in _exactly_ the same sense that `class { #x }` introduces a name

[12:52:11.0559] <bakkot>
and then we would have a syntax `{ [#x]: 1 }` for referring to _these existing kinds of names_ in object literals

[12:52:14.0557] <Justin Ridgewell>
You mean to allow `private [#x]` at block scope?

[12:52:18.0828] <peetk>
ahhhhh this is what i was missing

[12:52:33.0691] <Justin Ridgewell>
Why would you need to re-refer to the outer?

[12:52:51.0509] <ljharb>
a class could create an object with the private field?

[12:53:10.0119] <bakkot>
specifically I am proposing

```
class A {
  #x = 1;
  staticfactory() {
    return { [#x]: 1 }; // makes objects which are not instances of A but have `#x` anyway
 }
}
```

[12:53:14.0361] <bakkot>
* specifically I am proposing

```
class A {
  #x = 1;
  static factory() {
    return { [#x]: 1 }; // makes objects which are not instances of A but have `#x` anyway
 }
}
```

[12:53:37.0326] <Justin Ridgewell>
Oh, in object scope. Yes, that's allowed.

[12:53:59.0431] <ljharb>
`#x = { [#x]: true }` would also work

[12:54:03.0144] <ljharb>
* `#x = { [#x]: true };` would also work

[12:54:23.0977] <bakkot>
yes although, probably don't

[12:54:43.0629] <ljharb>
oh, definitely don't. it just follows.

