2026-07-02
[07:54:26.0391] <Michael Ficarra>
reminder: editor call in 5 minutes

[08:11:49.0257] <nicolo-ribaudo>
ljharb I was wondering, would it be possible to create a GitHub action that merges PRs "the right way"? So that for those that do not have conflicts, the "ready to merge" label could work automatically without you having to do it manually

[08:35:23.0694] <bakkot>
https://github.com/tc39/ecma262/pull/1799

[09:05:36.0203] <linus>
I added `editor call` to https://github.com/tc39/ecma262/pull/3893 just before the meeting, nothing to discuss other than asking for thoughts and/or reviews

[09:37:32.0394] <Michael Ficarra>
@gibson042:matrix.org I want to make an ecmarkup release pretty soon after the pins bugfix makes it in, so if you also want https://github.com/tc39/ecmarkup/pull/694 to be included, you should remember to update it.

[12:13:28.0741] <Michael Ficarra>
also rebase so it hides all those baseline changes from the diff

[12:14:24.0519] <Richard Gibson>
ok. I'm reviewing the pins PR now

[12:59:44.0451] <Richard Gibson>
review submitted; if the release can wait for tomorrow I think https://github.com/tc39/ecmarkup/pull/694 will be ready

[13:05:56.0950] <Michael Ficarra>
cool, thanks

[13:06:04.0308] <Michael Ficarra>
I'll do it early next week then


2026-07-03
[21:36:19.0328] <ljharb>
> <@nicolo-ribaudo:matrix.org> ljharb I was wondering, would it be possible to create a GitHub action that merges PRs "the right way"? So that for those that do not have conflicts, the "ready to merge" label could work automatically without you having to do it manually

I’m sure we can get that done 


2026-07-06
[13:44:31.0795] <Richard Gibson>
I don't much care about https://github.com/tc39/ecmarkup/pull/694/changes#r3530802437 , but I'd like to resolve it quickly so we can merge the PR and cut an ecmarkup release. So, who would like to weigh in on how `setMultipagePreference` ought to be wired to `#shortcuts-help`?
1. in multipage.js, as a listener on `document` with runtime filtering
2. in multipage.js, as a listener on `#shortcuts-help` that is dynamically added either immediately if that container already exists or in a DOMContentLoaded callback otherwise
3. in Spec.ts as an inline script element accompanying `#shortcuts-help`

[13:45:28.0155] <Richard Gibson>
* I don't much care about https://github.com/tc39/ecmarkup/pull/694/changes#r3530802437 , but I'd like to resolve it quickly so we can merge the PR and cut an ecmarkup release. So, who would like to weigh in on how `setMultipagePreference` ought to be wired to `#shortcuts-help`?

1. in multipage.js, as a listener on `document` with runtime filtering
2. in multipage.js, as a listener on `#shortcuts-help` that is dynamically added immediately if that container already exists or otherwise in a DOMContentLoaded callback
3. in Spec.ts as an inline script element accompanying `#shortcuts-help`

[13:45:38.0265] <Richard Gibson>
* I don't much care about https://github.com/tc39/ecmarkup/pull/694/changes#r3530802437 , but I'd like to resolve it quickly so we can merge the PR and cut an ecmarkup release. So, who would like to weigh in on how `setMultipagePreference` ought to be wired to `#shortcuts-help`?

1. in multipage.js, as a listener on `document` with runtime filtering
2. in multipage.js, as a listener on `#shortcuts-help` that is dynamically added immediately if that container already exists or otherwise in a DOMContentLoaded callback
3. in Spec.ts, as an inline script element accompanying `#shortcuts-help`


2026-07-07
[17:25:47.0324] <Michael Ficarra>
I think it's not a very big deal no matter what we do

[12:33:49.0365] <Richard Gibson>
Michael Ficarra I updated https://github.com/tc39/ecmarkup/pull/696 to keep using the current U+2716 HEAVY MULTIPLICATION X character


2026-07-08
[12:58:38.0520] <ljharb>
fwiw in the medium term humanless publishes won't be possible, so the biblio will need some kind of solution: https://github.blog/changelog/2026-07-08-npm-install-time-security-and-gat-bypass2fa-deprecation/ (OIDC will be the last way to do non-2FA publish, but it'll be killed as well eventually) we'll be able to stage things from CI but a human will still need to approve them

[13:11:45.0175] <ljharb>
* fwiw in the medium term humanless publishes won't be possible, so the biblio will need some kind of solution: https://github.blog/changelog/2026-07-08-npm-install-time-security-and-gat-bypass2fa-deprecation/ (OIDC will be the last way to do non-2FA publish, but non-staged OIDC will be killed as well eventually) we'll be able to stage things from CI but a human will still need to approve them

[13:14:47.0219] <bakkot>
where'd they say non-staged OIDC will be killed

[13:14:51.0603] <bakkot>
* where'd they say non-staged OIDC will be killed?

[13:16:16.0070] <bakkot>
I guess

> plan to move automated publishing to trusted publishing (OIDC) or staged publishing with a human approval step

is grammatically ambiguous; I read that as "to (trusted publishing) or (staged publishing with a human approval step)" rather than "to (trusted publishing or staged publishing) with a human approval step"


2026-07-09
[23:12:44.0948] <ljharb>
it may or may not be in the blog post, but that's very much the plan

[23:12:54.0901] <ljharb>
the first step is indeed the way you read it

[23:13:04.0468] <ljharb>
but once that's shipped, non-staged OIDC is the next to go.

