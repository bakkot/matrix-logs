2026-07-02
[08:02:07.0846] <sffc>
Given there is nothing on the agenda and it is a holiday for many of us in the US, I am deleting the meeting for today


2026-07-10
[17:33:24.0079] <sffc>
I made slides for duration units in Amount and Intl Sequence Units and put them on the TC39 agenda. PTAL: https://tc39.es/proposal-intl-sequence-units/slides/time-units.html

[14:35:06.0340] <Richard Gibson>
thanks, I think they're a good characterization and will be a big help in framing the discussion


2026-07-13
[04:13:05.0929] <eemeli>
Point 6 for excluding is a bit weird, because all of the individual time units are already supported by ECMA-402, and Intl.DurationFormat even uses Intl.NumberFormat internally to format them.

Including open questions on Amount in this context is a bit weird, esp. as the slides are solely about Intl.NumberFormat. I would also disagree with the characterisation that any of the Amount questions are actually open questions; its units support is solely and completely driven by what's in units.xml, and that clearly includes both `time` and year-duration` units.

Otherwise it looks good!

[04:13:19.0803] <eemeli>
* Point 6 for excluding is a bit weird, because all of the individual time units are already supported by ECMA-402, and Intl.DurationFormat even uses Intl.NumberFormat internally to format them.

Including open questions on Amount in this context is a bit weird, esp. as the slides are solely about Intl.NumberFormat. I would also disagree with the characterisation that any of the Amount questions are actually open questions; its units support is solely and completely driven by what's in units.xml, and that clearly includes both `time` and `year-duration\` units.

Otherwise it looks good!

[04:13:26.0035] <eemeli>
* Point 6 for excluding is a bit weird, because all of the individual time units are already supported by ECMA-402, and Intl.DurationFormat even uses Intl.NumberFormat internally to format them.

Including open questions on Amount in this context is a bit weird, esp. as the slides are solely about Intl.NumberFormat. I would also disagree with the characterisation that any of the Amount questions are actually open questions; its units support is solely and completely driven by what's in units.xml, and that clearly includes both `time` and `year-duration` units.

Otherwise it looks good!

[04:14:43.0436] <eemeli>
* Point 6 for excluding is a bit weird, because all of the individual time units are already supported by ECMA-402, and Intl.DurationFormat even uses Intl.NumberFormat internally to format them.

Including open questions on Amount in this context is also a bit weird, esp. as the slides are solely about Intl.NumberFormat. I would also disagree with the characterisation that any of the Amount questions are actually open questions; its units support is solely and completely driven by what's in units.xml, and that clearly includes both `time` and `year-duration` units.

Otherwise it looks good!

[04:14:51.0960] <eemeli>
* Point 6 for excluding is a bit weird, because all of the individual time units are already supported by ECMA-402, and Intl.DurationFormat even uses Intl.NumberFormat internally to format them.

Including open questions on Amount in this context is also a bit weird, esp. as the slides are otherwise solely about Intl.NumberFormat. I would also disagree with the characterisation that any of the Amount questions are actually open questions; its units support is solely and completely driven by what's in units.xml, and that clearly includes both `time` and `year-duration` units.

Otherwise it looks good!


2026-07-15
[17:32:21.0639] <sffc>
I put some topics from Oli on the Numerics agenda

[17:39:36.0302] <sffc>
Thanks for the review!

The agenda item calls out both proposals by name.

I edited Point 6: https://github.com/tc39/proposal-intl-sequence-units/commit/8b6ab9542c6024f5121483d5cdb96e7bbb1dc631

I'll let you voice the comment about "[Amount]'s units support is solely and completely driven by what's in units.xml" in the plenary. I think I agree with you that Amount shouldn't exclude single time units, because it would create a new class of errors. It seems good to be explicit about that in the discussion.

[00:10:09.0076] <Jesse>
unfortunately I'll be away for the Numerics call tomorrow (I'll be on vacation)

[00:11:58.0514] <Jesse>
I'm taking a look at Oli's issue

[01:29:00.0112] <eemeli>
The point 6 update looks good. If you do want to keep the Amount open questions in, could you at least file issue(s) for them in the proposal-amount repo? I can't find anything about the topic there at least yet.

Also, as one more argument for including them, we already support time units for `-per-` units:
```
new Intl.NumberFormat('en', { style: 'unit', unit: 'minute-per-week' }).format(7*24*60)
// "10,080 min/w"
```


2026-07-16
[11:28:20.0731] <Caio Lima>
Is there any policy from TC39 to not use AI to take notes on Numeric meetings? We had this discussion on Modules Harmony meeting a while ago, but the blocker was that we couldn't find any member to host the meeting with such feature enabled, and I'm wondering if this is the case for Numeric calls or if it is something else.

[11:50:59.0751] <sffc>
I don't think there's a TC39-level prohibition against this for mostly-informal working groups like Numerics. Not sure about task groups like TG2. In TG1 plenary, the rules are more strict, mostly driven by individual delegates working under personal and company constraints. It's worth making an item to ask about this in next week's meeting, though, since you get on the hook a lot for taking notes and this would make your workflow easier


2026-07-17
[09:43:36.0550] <Chris de Almeida>
tl;dr -- whatever the meeting participants are comfortable with is fine. if the notes are going to be public, then you must disclose that the notes were AI-generated, and also give folks the opportunity to change or remove anything attributed to them

