[
{"content":{"body":"Regarding [this](https://github.com/tc39/proposal-measure/pull/29#discussion_r2182738311) short conversation between Jesse and myself, I've started to think that the default behaviour of `Amount.p.toString()` should be to return the numeric string with no unit/currency markers, so that e.g. `new Decimal(amount)` would work without any special Amount-detection code.\n\nThe with-marker behaviour could be implemented optionally, e.g. with `.toString({ style: 'long' })` where `style: 'numeric'` would be the default.","format":"org.matrix.custom.html","formatted_body":"<p>Regarding <a href=\"https://github.com/tc39/proposal-measure/pull/29#discussion_r2182738311\">this</a> short conversation between <a href=\"https://matrix.to/#/@jesse:igalia.com\">Jesse</a> and myself, I've started to think that the default behaviour of <code>Amount.p.toString()</code> should be to return the numeric string with no unit/currency markers, so that e.g. <code>new Decimal(amount)</code> would work without any special Amount-detection code.</p>\n<p>The with-marker behaviour could be implemented optionally, e.g. with <code>.toString({ style: 'long' })</code> where <code>style: 'numeric'</code> would be the default.</p>\n","m.mentions":{"user_ids":["@jesse:igalia.com"]},"msgtype":"m.text"},"ts":1751620833109,"senderName":"eemeli","senderId":"@eemeli:mozilla.org","id":"$fThi7ClFZ9XYAOBmLgMSeEqa7aSAxR_iCGsJ4BQl8dQ"}
]