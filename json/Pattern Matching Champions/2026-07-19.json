[
{"content":{"body":"We've been discussing cleaning up and cutting/deferring a number of parts of the proposal due to its complexity and concerns from members of the plenary. We've had a few false starts on putting together a presentation over the past year+, so haven't been able to provide an update.","m.mentions":{"user_ids":["@jackworks:matrix.org"]},"m.relates_to":{"m.in_reply_to":{"event_id":"$OAotgxA1LxXt7WU1VO77nx6iIUdIDSSR6l-BY0zMvdA"}},"msgtype":"m.text"},"ts":1784500339208,"senderName":"rbuckton","senderId":"@rbuckton:matrix.org","id":"$N5VbaRQKiT8B9uj6qQW6jcO6ZWTvdtGgHu0u3r22-dI"},
{"content":{"body":"Is there anyone in the champions group that is familiar with my concerns regarding the Declarations in Conditionals proposal? I've discussed them here and more recently in the comment thread for https://github.com/tc39/proposal-extractors/issues/35, but also when it last went for Stage 2: https://github.com/tc39/notes/blob/main/meetings/2025-11/november-18.md#declarations-in-conditionals-for-stage-2. I know this is short notice, but I may end up unable to attend some of the plenary and may need someone to speak on my behalf. ","m.mentions":{},"msgtype":"m.text"},"ts":1784500622875,"senderName":"rbuckton","senderId":"@rbuckton:matrix.org","id":"$lEA3dY3-NUgUCdkpsWADbhQ2yjbN3elpggDStjD5cpg"},
{"content":{"body":"I read your recent comment, but I have no confidence I can convince others","msgtype":"m.text"},"ts":1784501708935,"senderName":"Jack Works","senderId":"@jackworks:matrix.org","id":"$Rc32efroMQY4XRCJ9XEWJjE1NzNFoEVeW24P95wscYs"},
{"content":{"body":"In November I asked that we postpone advancement until we had discussed cross-cutting concerns with pattern matching, but I don't believe that was ever addressed. In short, there is a fair amount of overlap between what's been proposed for Declarations in Conditionals and Pattern Matching's `is..let` syntax. If Decls in Conditionals advances to stage 2 or 2.7, it will likely force pattern matching into a direction that many of the champions have expressed opposition to. It may mean that we are forced to rely on existing binding/assignment pattern-like semantics such as I documented here: https://gist.github.com/rbuckton/df6ade207eecad4fc94cedc3aae79ceb. This could mean we would be back to needing something like `^` or `@` to designate whether something is a reference or a binding and `[]` syntax would not be exhaustive. Declarations in Conditionals also appears to be leaning towards only exposing the binding in the `if` branch, which is opposite of what we'd previously discussed for `let` patterns due to refactoring concerns.\n\nThis is the progression of the Declarations in Conditionals proposal, as I see it:\n\n```\nif (let x = a; x) ...; // separate declaration and condition have no conflict\nif (let x = a) ...; // no condition (relies on truthiness) has no conflict, but begins the slippery slope\nif (let { x, y } = a) ...; // very much crossing into pattern matching territory though not a concern yet.\nif (let [x, y] = a) ...; // this would likely conflict with pattern matching which wants `[x, y]` to only match `[a, b]` and not `[a]` or `[a, b, c]`.\nif (let Point(x, y) = a) ...; // already suggested by bakkot, in the issue linked above.\n\n// as opposed to `is..let` patterns\nif (a is let x) ...; // covers the first two cases\nif (a is { x: let x, y: let y }) ...; // covers third case\nif (a is { let x, let y }) ...; // shorthand form of previous line\nif (a is [let x, let y]) ...; // covers fourth case, but semantics are stricter\nif (a is Point(let x, let y)) ...; // covers fifth case\n```\n\n","format":"org.matrix.custom.html","formatted_body":"<p>In November I asked that we postpone advancement until we had discussed cross-cutting concerns with pattern matching, but I don't believe that was ever addressed. In short, there is a fair amount of overlap between what's been proposed for Declarations in Conditionals and Pattern Matching's <code>is..let</code> syntax. If Decls in Conditionals advances to stage 2 or 2.7, it will likely force pattern matching into a direction that many of the champions have expressed opposition to. It may mean that we are forced to rely on existing binding/assignment pattern-like semantics such as I documented here: https://gist.github.com/rbuckton/df6ade207eecad4fc94cedc3aae79ceb. This could mean we would be back to needing something like <code>^</code> or <code>@</code> to designate whether something is a reference or a binding and <code>[]</code> syntax would not be exhaustive. Declarations in Conditionals also appears to be leaning towards only exposing the binding in the <code>if</code> branch, which is opposite of what we'd previously discussed for <code>let</code> patterns due to refactoring concerns.</p>\n<p>This is the progression of the Declarations in Conditionals proposal, as I see it:</p>\n<pre><code>if (let x = a; x) ...; // separate declaration and condition have no conflict\nif (let x = a) ...; // no condition (relies on truthiness) has no conflict, but begins the slippery slope\nif (let { x, y } = a) ...; // very much crossing into pattern matching territory though not a concern yet.\nif (let [x, y] = a) ...; // this would likely conflict with pattern matching which wants `[x, y]` to only match `[a, b]` and not `[a]` or `[a, b, c]`.\nif (let Point(x, y) = a) ...; // already suggested by bakkot, in the issue linked above.\n\n// as opposed to `is..let` patterns\nif (a is let x) ...; // covers the first two cases\nif (a is { x: let x, y: let y }) ...; // covers third case\nif (a is { let x, let y }) ...; // shorthand form of previous line\nif (a is [let x, let y]) ...; // covers fourth case, but semantics are stricter\nif (a is Point(let x, let y)) ...; // covers fifth case\n</code></pre>\n","m.mentions":{},"msgtype":"m.text"},"ts":1784501842125,"senderName":"rbuckton","senderId":"@rbuckton:matrix.org","id":"$bml-ILgB2RDe1qAkmzTLTqO9DMH5R6BeQ5SvKKboAIA"}
]