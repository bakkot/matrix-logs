[
{"content":{"body":"We really need to discuss the Declarations in Conditionals proposal, currently on next week's agenda for Stage 2 or 2.7, as I believe it could have ramifications for pattern matching, especially the `is` and `let`-patterns:\n\n```js\n// decls in conditionals\nif (let p = obj.position; p instanceof Point) {\n  ...\n}\n\n// pattern matching (mvp)\nif (obj.position is Point and let p) {\n  ...\n}\n\n// pattern matching (future potential)\nif (obj is Point p) {\n ...\n}\n```\n\nEven if it does not immediately impact pattern matching, I'm concerned it will push the committee towards shoehorning pattern matching into existing destructuring and binding patterns, which is an avenue we've already explored and rejected. For instance, if we support patternless `if (let x = y)`, it is a slippery slope towards `if (let [a, b] = ar)` or `if (let { x, y } = p)`, not unlike `if..let` in Rust. Especially if extractors were to advance independently of pattern matching. \n\nThere's also https://github.com/tc39/proposal-Declarations-in-Conditionals/issues/3, which is suggesting binding semantics in opposition to the semantics for `let` patterns we've discussed previously. This could potentially force us into a specific path for that feature as well.\n\nI think we should be considering the directions the Declarations in Conditionals proposal could take and form a position as to how pattern matching either aligns with or opposes this syntax prior to next week's plenary.\n","format":"org.matrix.custom.html","formatted_body":"<p>We really need to discuss the Declarations in Conditionals proposal, currently on next week's agenda for Stage 2 or 2.7, as I believe it could have ramifications for pattern matching, especially the <code>is</code> and <code>let</code>-patterns:</p>\n<pre><code class=\"language-js\">// decls in conditionals\nif (let p = obj.position; p instanceof Point) {\n  ...\n}\n\n// pattern matching (mvp)\nif (obj.position is Point and let p) {\n  ...\n}\n\n// pattern matching (future potential)\nif (obj is Point p) {\n ...\n}\n</code></pre>\n<p>Even if it does not immediately impact pattern matching, I'm concerned it will push the committee towards shoehorning pattern matching into existing destructuring and binding patterns, which is an avenue we've already explored and rejected. For instance, if we support patternless <code>if (let x = y)</code>, it is a slippery slope towards <code>if (let [a, b] = ar)</code> or <code>if (let { x, y } = p)</code>, not unlike <code>if..let</code> in Rust. Especially if extractors were to advance independently of pattern matching.</p>\n<p>There's also https://github.com/tc39/proposal-Declarations-in-Conditionals/issues/3, which is suggesting binding semantics in opposition to the semantics for <code>let</code> patterns we've discussed previously. This could potentially force us into a specific path for that feature as well.</p>\n<p>I think we should be considering the directions the Declarations in Conditionals proposal could take and form a position as to how pattern matching either aligns with or opposes this syntax prior to next week's plenary.</p>\n","m.mentions":{},"msgtype":"m.text"},"ts":1783984911044,"senderName":"rbuckton","senderId":"@rbuckton:matrix.org","id":"$oPYF7JEmbT5k3Q4_J1LwqlUd2Ont70ZLduK64nUGIks"},
{"content":{"body":"We also need to coordinate a new meeting schedule for regular discussions and get champions actively involved in the proposal again. It's been far too quiet","m.mentions":{},"msgtype":"m.text"},"ts":1783985046626,"senderName":"rbuckton","senderId":"@rbuckton:matrix.org","id":"$MhhVoaIVCmXLeHvoKmB7liue0XZrjf17dLgWwEzWMVg"},
{"content":{"body":"yeah, we should find a new meeting time (or reconfirm the existing one)","m.mentions":{},"msgtype":"m.text"},"ts":1783986349529,"senderName":"ljharb","senderId":"@ljharb:matrix.org","id":"$eMGQeRjAjVV9s93TzJfkYaFC7ORL91l9juEAT0ZzI5g"},
{"content":{"body":"but also that jumping straight to 2.7 is a nonstarter imo, it's too fast for something like this","m.mentions":{},"msgtype":"m.text"},"ts":1783986364703,"senderName":"ljharb","senderId":"@ljharb:matrix.org","id":"$c-ak4vIXWfE56SuInhr6hKOP-JOL22a6br1YTqM1z8c"}
]