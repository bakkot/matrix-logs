[
{"content":{"body":"I'm not sure if this is the best place for this, but I'm in the process of implementing source phase imports and ran into a question about syntax errors for import source declarations: Give code like `import source form \"./module.mjs\";` (`from` misspelled `form`) should the SyntaxError reflect parsing an `import source` form, or an `import` form? That is, is the `from` keyword missing at `form` or at `\"./module.mjs\"`?","format":"org.matrix.custom.html","formatted_body":"I'm not sure if this is the best place for this, but I'm in the process of implementing source phase imports and ran into a question about syntax errors for import source declarations: Give code like <code>import source form \"./module.mjs\";</code> (<code>from</code> misspelled <code>form</code>) should the SyntaxError reflect parsing an <code>import source</code> form, or an <code>import</code> form? That is, is the <code>from</code> keyword missing at <code>form</code> or at <code>\"./module.mjs\"</code>?","m.mentions":{},"msgtype":"m.text"},"ts":1770405099173,"senderName":"dminor","senderId":"@dminor:mozilla.org","id":"$669MXozleAdylbIJ4UD3IL2BWhsxNNtbe2e96kNgoPg"},
{"content":{"body":"Interesting question - on first glance I'd say to me it seems most similar to `import form 'x'` which says unexpected string in Node.js for me. So I think \"module.mjs\" being the unexpected string that should be the from keyword makes sense.","format":"org.matrix.custom.html","formatted_body":"Interesting question - on first glance I'd say to me it seems most similar to <code>import form 'x'</code> which says unexpected string in Node.js for me. So I think \"module.mjs\" being the unexpected string that should be the from keyword makes sense.","m.mentions":{"user_ids":["@dminor:mozilla.org"]},"m.relates_to":{"m.in_reply_to":{"event_id":"$669MXozleAdylbIJ4UD3IL2BWhsxNNtbe2e96kNgoPg"}},"msgtype":"m.text"},"ts":1770405431089,"senderName":"guybedford","senderId":"@guybedford:matrix.org","id":"$LdZOosT0LSFquextmbIkvQAdgC_bljqEDUgHTf3oqsI"}
]