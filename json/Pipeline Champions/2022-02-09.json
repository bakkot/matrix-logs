[
{"content":{"body":"A Babel contributor has opened a pull request for adding an optional pipe operator spelled as `?>` to Babel. https://github.com/babel/babel/pull/14255 ","format":"org.matrix.custom.html","formatted_body":"A Babel contributor has opened a pull request for adding an optional pipe operator spelled as <code>?&gt;</code> to Babel. https://github.com/babel/babel/pull/14255","msgtype":"m.text"},"ts":1644417513389,"senderName":"jschoi","senderId":"@jschoi:matrix.org","id":"$_K3iPEo5Xp_XvfzWgLWYuNZX43UqWefACN_QY0_elCE"},
{"content":{"body":"only a little shame we can't do this:`if (val ?> true) { ... `,  need to `if (val != null)` but some projects force always strict equality checks so then have to do `if ((val ?? null) !== null)`","format":"org.matrix.custom.html","formatted_body":"only a little shame we can't do this:<code>if (val ?&gt; true) { ... </code>,  need to <code>if (val != null)</code> but some projects force always strict equality checks so then have to do <code>if ((val ?? null) !== null)</code>","msgtype":"m.text","org.matrix.msc1767.message":[{"body":"only a little shame we can't do this:`if (val ?> true) { ... `,  need to `if (val != null)` but some projects force always strict equality checks so then have to do `if ((val ?? null) !== null)`","mimetype":"text/plain"},{"body":"only a little shame we can't do this:<code>if (val ?&gt; true) { ... </code>,  need to <code>if (val != null)</code> but some projects force always strict equality checks so then have to do <code>if ((val ?? null) !== null)</code>","mimetype":"text/html"}]},"ts":1644418248065,"senderName":"Ashley Claymore","senderId":"@aclaymore:matrix.org","id":"$Aa6m7tgquqOmOYIt_cQMJmFxJbvtPspgXdKbVUnymdE"}
]