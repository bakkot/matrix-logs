[
{"content":{"body":"Back in 2021, when `Object.hasOwn()` raced all the way to Stage 4 in four months, was something like `Object.getOwn()` considered at all? I can't find any reference to such in the notes or the proposal repo.\n\nI'm currently needing to write this repeatedly in a project, and it feels a bit repetitive:\n```\nlet x = Object.hasOwn(obj, name) ? obj[name] : undefined;\n```","format":"org.matrix.custom.html","formatted_body":"<p>Back in 2021, when <code>Object.hasOwn()</code> raced all the way to Stage 4 in four months, was something like <code>Object.getOwn()</code> considered at all? I can't find any reference to such in the notes or the proposal repo.</p>\n<p>I'm currently needing to write this repeatedly in a project, and it feels a bit repetitive:</p>\n<pre><code>let x = Object.hasOwn(obj, name) ? obj[name] : undefined;\n</code></pre>\n","m.mentions":{},"msgtype":"m.text"},"ts":1756804631812,"senderName":"eemeli","senderId":"@eemeli:mozilla.org","id":"$56afBWq5L1YYfQF0PJd3IopiOvBNgoG2HWOiMhTnURg"}
]