[
{"content":{"body":"Delegates! I have posted a comment on the reflector regarding information still missing from the notes. Please do check it and/or your github inboxes","m.mentions":{},"msgtype":"m.text"},"ts":1785513223898,"senderName":"Aki","senderId":"@akirose:matrix.org","id":"$mcbPg5vmGey4Nulj5aFZgoOTRVpfWkMZCtpFjsP3Irc"},
{"content":{"body":"For the `export defer` proposal, as promised two meetings ago we're also adding the \"filter\" support (`import { a, b } as ns from \"x\"`) to dynamic import, by doing `import(\"x\", { filter: [\"a\", \"b\"] })`.\n\nShould that `filter` be an array-like or an iterable? It'd be nice if we could agree on some guidance to put in the normative conventions","format":"org.matrix.custom.html","formatted_body":"<p>For the <code>export defer</code> proposal, as promised two meetings ago we're also adding the \"filter\" support (<code>import { a, b } as ns from \"x\"</code>) to dynamic import, by doing <code>import(\"x\", { filter: [\"a\", \"b\"] })</code>.</p>\n<p>Should that <code>filter</code> be an array-like or an iterable? It'd be nice if we could agree on some guidance to put in the normative conventions</p>\n","m.mentions":{},"msgtype":"m.text"},"ts":1785531921869,"senderName":"nicolo-ribaudo","senderId":"@nicolo-ribaudo:matrix.org","id":"$sxjUAgiNHVQifcYZXLHoiG0hcc1y0ewujznNiUJu03I"},
{"content":{"body":"* For the `export defer` proposal, as promised two meetings ago we're also adding the \"filter\" support (`import { a, b } as ns from \"x\"`) to dynamic import, by doing `import(\"x\", { filter: [\"a\", \"b\"] })`.\n\nShould that `filter` be an array, an array-like or an iterable? It'd be nice if we could agree on some guidance to put in the normative conventions","format":"org.matrix.custom.html","formatted_body":"* <p>For the <code>export defer</code> proposal, as promised two meetings ago we're also adding the \"filter\" support (<code>import { a, b } as ns from \"x\"</code>) to dynamic import, by doing <code>import(\"x\", { filter: [\"a\", \"b\"] })</code>.</p>\n<p>Should that <code>filter</code> be an array, an array-like or an iterable? It'd be nice if we could agree on some guidance to put in the normative conventions</p>\n","m.mentions":{},"m.new_content":{"body":"For the `export defer` proposal, as promised two meetings ago we're also adding the \"filter\" support (`import { a, b } as ns from \"x\"`) to dynamic import, by doing `import(\"x\", { filter: [\"a\", \"b\"] })`.\n\nShould that `filter` be an array, an array-like or an iterable? It'd be nice if we could agree on some guidance to put in the normative conventions","format":"org.matrix.custom.html","formatted_body":"<p>For the <code>export defer</code> proposal, as promised two meetings ago we're also adding the \"filter\" support (<code>import { a, b } as ns from \"x\"</code>) to dynamic import, by doing <code>import(\"x\", { filter: [\"a\", \"b\"] })</code>.</p>\n<p>Should that <code>filter</code> be an array, an array-like or an iterable? It'd be nice if we could agree on some guidance to put in the normative conventions</p>\n","m.mentions":{},"msgtype":"m.text"},"m.relates_to":{"event_id":"$sxjUAgiNHVQifcYZXLHoiG0hcc1y0ewujznNiUJu03I","rel_type":"m.replace"},"msgtype":"m.text"},"ts":1785531932043,"senderName":"nicolo-ribaudo","senderId":"@nicolo-ribaudo:matrix.org","id":"$3murFQ9sJ-vkkIzLndxHYqrszQTmo2X5wodNJzoUeYk"},
{"content":{"body":"* For the `export defer` proposal, as promised two meetings ago we're also adding the \"filter\" support (`import { a, b } as ns from \"x\"`) to dynamic import, by doing `import(\"x\", { filter: [\"a\", \"b\"] })`.\n\nShould that `filter` be an array-like or an iterable? It'd be nice if we could agree on some guidance to put in the normative conventions","format":"org.matrix.custom.html","formatted_body":"* <p>For the <code>export defer</code> proposal, as promised two meetings ago we're also adding the \"filter\" support (<code>import { a, b } as ns from \"x\"</code>) to dynamic import, by doing <code>import(\"x\", { filter: [\"a\", \"b\"] })</code>.</p>\n<p>Should that <code>filter</code> be an array-like or an iterable? It'd be nice if we could agree on some guidance to put in the normative conventions</p>\n","m.mentions":{},"m.new_content":{"body":"For the `export defer` proposal, as promised two meetings ago we're also adding the \"filter\" support (`import { a, b } as ns from \"x\"`) to dynamic import, by doing `import(\"x\", { filter: [\"a\", \"b\"] })`.\n\nShould that `filter` be an array-like or an iterable? It'd be nice if we could agree on some guidance to put in the normative conventions","format":"org.matrix.custom.html","formatted_body":"<p>For the <code>export defer</code> proposal, as promised two meetings ago we're also adding the \"filter\" support (<code>import { a, b } as ns from \"x\"</code>) to dynamic import, by doing <code>import(\"x\", { filter: [\"a\", \"b\"] })</code>.</p>\n<p>Should that <code>filter</code> be an array-like or an iterable? It'd be nice if we could agree on some guidance to put in the normative conventions</p>\n","m.mentions":{},"msgtype":"m.text"},"m.relates_to":{"event_id":"$sxjUAgiNHVQifcYZXLHoiG0hcc1y0ewujznNiUJu03I","rel_type":"m.replace"},"msgtype":"m.text"},"ts":1785531945316,"senderName":"nicolo-ribaudo","senderId":"@nicolo-ribaudo:matrix.org","id":"$KXM6H25Zo0O_P88sM8XjkEOUqtudyCWioUT4dwHbtWk"}
]