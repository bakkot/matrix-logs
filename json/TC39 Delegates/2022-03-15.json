[
{"content":{"body":"shu: re https://github.com/tc39/ecma262/pull/2646, did you know that TA.prototype.sort _also_ has a per-iteration detachedness check?","format":"org.matrix.custom.html","formatted_body":"<a href=\"https://matrix.to/#/@shuyuguo:matrix.org\">shu</a>: re https://github.com/tc39/ecma262/pull/2646, did you know that TA.prototype.sort _also_ has a per-iteration detachedness check?","msgtype":"m.text","org.matrix.msc1767.message":[{"body":"shu: re https://github.com/tc39/ecma262/pull/2646, did you know that TA.prototype.sort _also_ has a per-iteration detachedness check?","mimetype":"text/plain"},{"body":"<a href=\"https://matrix.to/#/@shuyuguo:matrix.org\">shu</a>: re https://github.com/tc39/ecma262/pull/2646, did you know that TA.prototype.sort _also_ has a per-iteration detachedness check?","mimetype":"text/html"}]},"ts":1647306000913,"senderName":"bakkot","senderId":"@bakkot:matrix.org","id":"$KZUFXRTqLmbIEhLiLpJ_mRLoLnQ8gEL8I-_9LDhQT6Y"},
{"content":{"body":"it doesn't even get you anything because you have to read all of the values out of the buffer before the loop anyway, since `compareFn` can write to the buffer","format":"org.matrix.custom.html","formatted_body":"it doesn't even get you anything because you have to read all of the values out of the buffer before the loop anyway, since <code>compareFn</code> can write to the buffer","msgtype":"m.text","org.matrix.msc1767.message":[{"body":"it doesn't even get you anything because you have to read all of the values out of the buffer before the loop anyway, since `compareFn` can write to the buffer","mimetype":"text/plain"},{"body":"it doesn't even get you anything because you have to read all of the values out of the buffer before the loop anyway, since <code>compareFn</code> can write to the buffer","mimetype":"text/html"}]},"ts":1647306170433,"senderName":"bakkot","senderId":"@bakkot:matrix.org","id":"$lMsmSHVBknRPwvdz3jz9-3SLQCN-V_rpfVAby1ExiiU"},
{"content":{"body":"My guess is this was missed when https://github.com/tc39/ecma262/pull/1585 landed? Before es2021 the order of calls to [get] and `comparefn` was implementation defined. From es6 onwards there has always been that detached check","format":"org.matrix.custom.html","formatted_body":"My guess is this was missed when https://github.com/tc39/ecma262/pull/1585 landed? Before es2021 the order of calls to [get] and <code>comparefn</code> was implementation defined. From es6 onwards there has always been that detached check","msgtype":"m.text","org.matrix.msc1767.message":[{"body":"My guess is this was missed when https://github.com/tc39/ecma262/pull/1585 landed? Before es2021 the order of calls to [get] and `comparefn` was implementation defined. From es6 onwards there has always been that detached check","mimetype":"text/plain"},{"body":"My guess is this was missed when https://github.com/tc39/ecma262/pull/1585 landed? Before es2021 the order of calls to [get] and <code>comparefn</code> was implementation defined. From es6 onwards there has always been that detached check","mimetype":"text/html"}]},"ts":1647344129761,"senderName":"Ashley Claymore","senderId":"@aclaymore:matrix.org","id":"$yQtUR-_9tEKp5nzAw4eHudZOI13nLVNSYWC886cCh0k"},
{"content":{"body":" * My guess is this was missed when https://github.com/tc39/ecma262/pull/1585 landed? Before es2021 the order of calls to [get] and `comparefn` was implementation defined. From es6 onwards there has always been that detached check","format":"org.matrix.custom.html","formatted_body":" * My guess is this was missed when https://github.com/tc39/ecma262/pull/1585 landed? Before es2021 the order of calls to [get] and <code>comparefn</code> was implementation defined. From es6 onwards there has always been that detached check","m.new_content":{"body":"My guess is this was missed when https://github.com/tc39/ecma262/pull/1585 landed? Before es2021 the order of calls to [get] and `comparefn` was implementation defined. From es6 onwards there has always been that detached check","format":"org.matrix.custom.html","formatted_body":"My guess is this was missed when https://github.com/tc39/ecma262/pull/1585 landed? Before es2021 the order of calls to [get] and <code>comparefn</code> was implementation defined. From es6 onwards there has always been that detached check","msgtype":"m.text","org.matrix.msc1767.message":[{"body":"My guess is this was missed when https://github.com/tc39/ecma262/pull/1585 landed? Before es2021 the order of calls to [get] and `comparefn` was implementation defined. From es6 onwards there has always been that detached check","mimetype":"text/plain"},{"body":"My guess is this was missed when https://github.com/tc39/ecma262/pull/1585 landed? Before es2021 the order of calls to [get] and <code>comparefn</code> was implementation defined. From es6 onwards there has always been that detached check","mimetype":"text/html"}]},"m.relates_to":{"event_id":"$yQtUR-_9tEKp5nzAw4eHudZOI13nLVNSYWC886cCh0k","rel_type":"m.replace"},"msgtype":"m.text","org.matrix.msc1767.message":[{"body":" * My guess is this was missed when https://github.com/tc39/ecma262/pull/1585 landed? Before es2021 the order of calls to [get] and `comparefn` was implementation defined. From es6 onwards there has always been that detached check","mimetype":"text/plain"},{"body":" * My guess is this was missed when https://github.com/tc39/ecma262/pull/1585 landed? Before es2021 the order of calls to [get] and <code>comparefn</code> was implementation defined. From es6 onwards there has always been that detached check","mimetype":"text/html"}]},"ts":1647344184254,"senderName":"Ashley Claymore","senderId":"@aclaymore:matrix.org","id":"$pjeQcWN4DNekfvAq4ypmfqP9gchyoe7RVWKxUjauHgk"}
]