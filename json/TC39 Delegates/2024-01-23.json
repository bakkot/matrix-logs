[
{"content":{"body":"does anyone have strong opinions about `Uint8Array.fromHexInto` being on the prototype vs static? It sets bytes in an existing `Uint8Array`. Currently it's static to match TextEncoder's `encodeInto`, which is a very similar method, but it could be prototype instead","format":"org.matrix.custom.html","formatted_body":"does anyone have strong opinions about <code>Uint8Array.fromHexInto</code> being on the prototype vs static? It sets bytes in an existing <code>Uint8Array</code>. Currently it's static to match TextEncoder's <code>encodeInto</code>, which is a very similar method, but it could be prototype instead","m.mentions":{},"msgtype":"m.text"},"ts":1706039371319,"senderName":"bakkot","senderId":"@bakkot:matrix.org","id":"$Yg6tEc3FhQJbvnnr9mjvsuh3pzKhNStQOnakWfZlVns"},
{"content":{"body":" * does anyone have strong opinions about `Uint8Array.fromHexInto` being on the prototype vs static? It sets bytes in an existing `Uint8Array` and returns a `{ read, written }` pair. Currently it's static to match TextEncoder's `encodeInto`, which is a very similar method, but it could be prototype instead","format":"org.matrix.custom.html","formatted_body":" * does anyone have strong opinions about <code>Uint8Array.fromHexInto</code> being on the prototype vs static? It sets bytes in an existing <code>Uint8Array</code> and returns a <code>{ read, written }</code> pair. Currently it's static to match TextEncoder's <code>encodeInto</code>, which is a very similar method, but it could be prototype instead","m.mentions":{},"m.new_content":{"body":"does anyone have strong opinions about `Uint8Array.fromHexInto` being on the prototype vs static? It sets bytes in an existing `Uint8Array` and returns a `{ read, written }` pair. Currently it's static to match TextEncoder's `encodeInto`, which is a very similar method, but it could be prototype instead","format":"org.matrix.custom.html","formatted_body":"does anyone have strong opinions about <code>Uint8Array.fromHexInto</code> being on the prototype vs static? It sets bytes in an existing <code>Uint8Array</code> and returns a <code>{ read, written }</code> pair. Currently it's static to match TextEncoder's <code>encodeInto</code>, which is a very similar method, but it could be prototype instead","m.mentions":{},"msgtype":"m.text"},"m.relates_to":{"event_id":"$Yg6tEc3FhQJbvnnr9mjvsuh3pzKhNStQOnakWfZlVns","rel_type":"m.replace"},"msgtype":"m.text"},"ts":1706039382819,"senderName":"bakkot","senderId":"@bakkot:matrix.org","id":"$8Uksi-b-1xZTM9cTdzeDgB7Q1srk7daiU0FraHv7MNM"},
{"content":{"body":" * does anyone have strong opinions about `Uint8Array.fromBase64Into` being on the prototype vs static? It sets bytes in an existing `Uint8Array` and returns a `{ read, written }` pair. Currently it's static to match TextEncoder's `encodeInto` and the `Uint8Array.fromBase64` method, but it could be prototype instead","format":"org.matrix.custom.html","formatted_body":" * does anyone have strong opinions about <code>Uint8Array.fromBase64Into</code> being on the prototype vs static? It sets bytes in an existing <code>Uint8Array</code> and returns a <code>{ read, written }</code> pair. Currently it's static to match TextEncoder's <code>encodeInto</code> and the <code>Uint8Array.fromBase64</code> method, but it could be prototype instead","m.mentions":{},"m.new_content":{"body":"does anyone have strong opinions about `Uint8Array.fromBase64Into` being on the prototype vs static? It sets bytes in an existing `Uint8Array` and returns a `{ read, written }` pair. Currently it's static to match TextEncoder's `encodeInto` and the `Uint8Array.fromBase64` method, but it could be prototype instead","format":"org.matrix.custom.html","formatted_body":"does anyone have strong opinions about <code>Uint8Array.fromBase64Into</code> being on the prototype vs static? It sets bytes in an existing <code>Uint8Array</code> and returns a <code>{ read, written }</code> pair. Currently it's static to match TextEncoder's <code>encodeInto</code> and the <code>Uint8Array.fromBase64</code> method, but it could be prototype instead","m.mentions":{},"msgtype":"m.text"},"m.relates_to":{"event_id":"$Yg6tEc3FhQJbvnnr9mjvsuh3pzKhNStQOnakWfZlVns","rel_type":"m.replace"},"msgtype":"m.text"},"ts":1706039406009,"senderName":"bakkot","senderId":"@bakkot:matrix.org","id":"$MS-FKjws0sI53-mFQpK9J_GoXXZL8hJDCC2i2V8HLFE"}
]