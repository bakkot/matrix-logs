[
{"content":{"body":"bakkot: it seems like `it.map(f)` and `it.flatMap(f)`, for an `f` that doesn't return an iterator, aren't the same - iow, it seems like a mapper of `x => x` will throw in GetIteratorFlattenable if `x` isn't an Object. was that an intentional decision?","format":"org.matrix.custom.html","formatted_body":"<a href=\"https://matrix.to/#/@bakkot:matrix.org\">bakkot</a>: it seems like <code>it.map(f)</code> and <code>it.flatMap(f)</code>, for an <code>f</code> that doesn't return an iterator, aren't the same - iow, it seems like a mapper of <code>x =&gt; x</code> will throw in GetIteratorFlattenable if <code>x</code> isn't an Object. was that an intentional decision?","msgtype":"m.text","org.matrix.msc1767.message":[{"body":"bakkot: it seems like `it.map(f)` and `it.flatMap(f)`, for an `f` that doesn't return an iterator, aren't the same - iow, it seems like a mapper of `x => x` will throw in GetIteratorFlattenable if `x` isn't an Object. was that an intentional decision?","mimetype":"text/plain"},{"body":"<a href=\"https://matrix.to/#/@bakkot:matrix.org\">bakkot</a>: it seems like <code>it.map(f)</code> and <code>it.flatMap(f)</code>, for an <code>f</code> that doesn't return an iterator, aren't the same - iow, it seems like a mapper of <code>x =&gt; x</code> will throw in GetIteratorFlattenable if <code>x</code> isn't an Object. was that an intentional decision?","mimetype":"text/html"}]},"ts":1670881404865,"senderName":"ljharb","senderId":"@ljharb:matrix.org","id":"$d6Q9MBqaeEDKUt8fjJKkyRSV44g38P0XkrZYaOHCEiI"},
{"content":{"body":"yes","msgtype":"m.text","org.matrix.msc1767.text":"yes"},"ts":1670881620626,"senderName":"bakkot","senderId":"@bakkot:matrix.org","id":"$AhnPN5WLgTLkwMp77Yx-UxdGWSsDl5wV0qNJOPjpVlA"},
{"content":{"body":"https://github.com/tc39/proposal-iterator-helpers/pull/59","msgtype":"m.text","org.matrix.msc1767.text":"https://github.com/tc39/proposal-iterator-helpers/pull/59"},"ts":1670881663921,"senderName":"bakkot","senderId":"@bakkot:matrix.org","id":"$Ap23xA0_DtVaGKE-oxMraPfFSZvhqtRkaDubxk8D79s"}
]