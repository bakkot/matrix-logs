[
{"content":{"body":"what is the process for changing [Editorial Conventions](https://github.com/tc39/ecma262/wiki/Editorial-Conventions)? https://github.com/tc39/ecma262/pull/3832#discussion_r3172092825 highlights some gaps, specifically about how to compare an [extended] mathematical value or bigint against multiple options (*which I think should be `If _x_ = $a or _x_ = $b` when there are two options, and presumably `If _x_ = $a or _x_ = $b or _x_ = …` when there are more*). So I think I'd like the first few bullets of that section to look like\n> * when comparing an alias against a constant or collection of options, the alias should be on the left\n> * when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:\n>   * `=` or `≠` for an extended mathematical value or bigint\n>   * `SameValue(_x_, …)` for an object, non-well-known symbol, or arbitrary language value\n>   * `is` or `is not` for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)\n> * when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise `=`/`≠`/`SameValue` (`_x_ = _option1_ or _x_ = _option2_ or _x_ = …`) but consolidate `is`/`is not` using `either`/`neither` for a pair of options (`_x_ is neither _option1_ or _option2_`) and `one of` for more than that (`_x_ is not of of _option1_, _option2_, or _option3_`)\n> * an alias can also be compared against a number, bigint, or extended mathematical value using `<`, `≤`, `>`, or `≥`\n\nwe should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page","format":"org.matrix.custom.html","formatted_body":"<p>what is the process for changing <a href=\"https://github.com/tc39/ecma262/wiki/Editorial-Conventions\">Editorial Conventions</a>? https://github.com/tc39/ecma262/pull/3832#discussion_r3172092825 highlights some gaps, specifically about how to compare an [extended] mathematical value or bigint against multiple options (<em>which I think should be <code>If _x_ = $a or _x_ = $b</code> when there are two options, and presumably <code>If _x_ = $a or _x_ = $b or _x_ = …</code> when there are more</em>). So I think I'd like the first few bullets of that section to look like</p>\n<blockquote>\n<ul>\n<li>when comparing an alias against a constant or collection of options, the alias should be on the left</li>\n<li>when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:\n<ul>\n<li><code>=</code> or <code>≠</code> for an extended mathematical value or bigint</li>\n<li><code>SameValue(_x_, …)</code> for an object, non-well-known symbol, or arbitrary language value</li>\n<li><code>is</code> or <code>is not</code> for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)</li>\n</ul>\n</li>\n<li>when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise <code>=</code>/<code>≠</code>/<code>SameValue</code> (<code>_x_ = _option1_ or _x_ = _option2_ or _x_ = …</code>) but consolidate <code>is</code>/<code>is not</code> using <code>either</code>/<code>neither</code> for a pair of options (<code>_x_ is neither _option1_ or _option2_</code>) and <code>one of</code> for more than that (<code>_x_ is not of of _option1_, _option2_, or _option3_</code>)</li>\n<li>an alias can also be compared against a number, bigint, or extended mathematical value using <code>&lt;</code>, <code>≤</code>, <code>&gt;</code>, or <code>≥</code></li>\n</ul>\n</blockquote>\n<p>we should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page</p>\n","m.mentions":{},"msgtype":"m.text"},"ts":1777649206647,"senderName":"Richard Gibson","senderId":"@gibson042:matrix.org","id":"$3dGfwzIEW0gOTSojR5jl1alfroNZyOpubwG2H3Jcckg"},
{"content":{"body":"* what is the process for changing [Editorial Conventions](https://github.com/tc39/ecma262/wiki/Editorial-Conventions)? https://github.com/tc39/ecma262/pull/3832#discussion\\_r3172092825 highlights some gaps, specifically about how to compare an \\[extended\\] mathematical value or bigint against multiple options (_which I think should be `If _x_ = $a or _x_ = $b` when there are two options, and presumably `If _x_ = $a or _x_ = $b or _x_ = …` when there are more_). So I think I'd like the first few bullets of that section to look like\n\n> - when comparing an alias against a constant or collection of options, the alias should be on the left\n> - when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:\n\n>     - `_x_ = …` or `_x_ ≠ …` for an extended mathematical value or bigint\n>     - `SameValue(_x_, …)` for an object, non-well-known symbol, or arbitrary language value\n>     - `_x_ is …` or `_x_ is not …` for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)\n> - when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise `=`/`≠`/`SameValue` (`_x_ = _option1_ or _x_ = _option2_ or _x_ = …`) but consolidate `is`/`is not` using `either`/`neither` for a pair of options (`_x_ is neither _option1_ or _option2_`) and `one of` for more than that (`_x_ is not of of _option1_, _option2_, or _option3_`)\n> - an alias can also be compared against a number, bigint, or extended mathematical value using `<`, `≤`, `>`, or `≥`\n\nwe should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page","format":"org.matrix.custom.html","formatted_body":"* <p>what is the process for changing <a href=\"https://github.com/tc39/ecma262/wiki/Editorial-Conventions\">Editorial Conventions</a>? https://github.com/tc39/ecma262/pull/3832#discussion_r3172092825 highlights some gaps, specifically about how to compare an [extended] mathematical value or bigint against multiple options (<em>which I think should be <code>If _x_ = $a or _x_ = $b</code> when there are two options, and presumably <code>If _x_ = $a or _x_ = $b or _x_ = …</code> when there are more</em>). So I think I'd like the first few bullets of that section to look like</p>\n<blockquote>\n<ul>\n<li>when comparing an alias against a constant or collection of options, the alias should be on the left</li>\n<li>when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:</li>\n</ul>\n</blockquote>\n<blockquote>\n<pre><code>- `_x_ = …` or `_x_ ≠ …` for an extended mathematical value or bigint\n- `SameValue(_x_, …)` for an object, non-well-known symbol, or arbitrary language value\n- `_x_ is …` or `_x_ is not …` for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)\n</code></pre>\n<ul>\n<li>when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise <code>=</code>/<code>≠</code>/<code>SameValue</code> (<code>_x_ = _option1_ or _x_ = _option2_ or _x_ = …</code>) but consolidate <code>is</code>/<code>is not</code> using <code>either</code>/<code>neither</code> for a pair of options (<code>_x_ is neither _option1_ or _option2_</code>) and <code>one of</code> for more than that (<code>_x_ is not of of _option1_, _option2_, or _option3_</code>)</li>\n<li>an alias can also be compared against a number, bigint, or extended mathematical value using <code>&lt;</code>, <code>≤</code>, <code>&gt;</code>, or <code>≥</code></li>\n</ul>\n</blockquote>\n<p>we should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page</p>\n","m.mentions":{},"m.new_content":{"body":"what is the process for changing [Editorial Conventions](https://github.com/tc39/ecma262/wiki/Editorial-Conventions)? https://github.com/tc39/ecma262/pull/3832#discussion\\_r3172092825 highlights some gaps, specifically about how to compare an \\[extended\\] mathematical value or bigint against multiple options (_which I think should be `If _x_ = $a or _x_ = $b` when there are two options, and presumably `If _x_ = $a or _x_ = $b or _x_ = …` when there are more_). So I think I'd like the first few bullets of that section to look like\n\n> - when comparing an alias against a constant or collection of options, the alias should be on the left\n> - when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:\n\n>     - `_x_ = …` or `_x_ ≠ …` for an extended mathematical value or bigint\n>     - `SameValue(_x_, …)` for an object, non-well-known symbol, or arbitrary language value\n>     - `_x_ is …` or `_x_ is not …` for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)\n> - when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise `=`/`≠`/`SameValue` (`_x_ = _option1_ or _x_ = _option2_ or _x_ = …`) but consolidate `is`/`is not` using `either`/`neither` for a pair of options (`_x_ is neither _option1_ or _option2_`) and `one of` for more than that (`_x_ is not of of _option1_, _option2_, or _option3_`)\n> - an alias can also be compared against a number, bigint, or extended mathematical value using `<`, `≤`, `>`, or `≥`\n\nwe should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page","format":"org.matrix.custom.html","formatted_body":"<p>what is the process for changing <a href=\"https://github.com/tc39/ecma262/wiki/Editorial-Conventions\">Editorial Conventions</a>? https://github.com/tc39/ecma262/pull/3832#discussion_r3172092825 highlights some gaps, specifically about how to compare an [extended] mathematical value or bigint against multiple options (<em>which I think should be <code>If _x_ = $a or _x_ = $b</code> when there are two options, and presumably <code>If _x_ = $a or _x_ = $b or _x_ = …</code> when there are more</em>). So I think I'd like the first few bullets of that section to look like</p>\n<blockquote>\n<ul>\n<li>when comparing an alias against a constant or collection of options, the alias should be on the left</li>\n<li>when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:</li>\n</ul>\n</blockquote>\n<blockquote>\n<pre><code>- `_x_ = …` or `_x_ ≠ …` for an extended mathematical value or bigint\n- `SameValue(_x_, …)` for an object, non-well-known symbol, or arbitrary language value\n- `_x_ is …` or `_x_ is not …` for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)\n</code></pre>\n<ul>\n<li>when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise <code>=</code>/<code>≠</code>/<code>SameValue</code> (<code>_x_ = _option1_ or _x_ = _option2_ or _x_ = …</code>) but consolidate <code>is</code>/<code>is not</code> using <code>either</code>/<code>neither</code> for a pair of options (<code>_x_ is neither _option1_ or _option2_</code>) and <code>one of</code> for more than that (<code>_x_ is not of of _option1_, _option2_, or _option3_</code>)</li>\n<li>an alias can also be compared against a number, bigint, or extended mathematical value using <code>&lt;</code>, <code>≤</code>, <code>&gt;</code>, or <code>≥</code></li>\n</ul>\n</blockquote>\n<p>we should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page</p>\n","m.mentions":{},"msgtype":"m.text"},"m.relates_to":{"event_id":"$3dGfwzIEW0gOTSojR5jl1alfroNZyOpubwG2H3Jcckg","rel_type":"m.replace"},"msgtype":"m.text"},"ts":1777649661535,"senderName":"Richard Gibson","senderId":"@gibson042:matrix.org","id":"$nD8PvMtgUyOfs2fa4Yys5jBMj-mMAOeqZc6NKQxLfAk"},
{"content":{"body":"* what is the process for changing [Editorial Conventions](https://github.com/tc39/ecma262/wiki/Editorial-Conventions)? https://github.com/tc39/ecma262/pull/3832#discussion\\_r3172092825 highlights some gaps, specifically about how to compare an \\[extended\\] mathematical value or bigint against multiple options (_which I think should be `If _x_ = $a or _x_ = $b` when there are two options, and presumably `If _x_ = $a or _x_ = $b or _x_ = …` when there are more_). So I think I'd like the first few bullets of that section to look like\n\n> - when comparing an alias against a constant or collection of options, the alias should be on the left\n> - when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:\n> - `_x_ = …` or `_x_ ≠ …` for an extended mathematical value or bigint\n> - `SameValue(_x_, …)` for an object, non-well-known symbol, or arbitrary language value\n> - `_x_ is …` or `_x_ is not …` for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)\n> - when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise `=`/`≠`/`SameValue` (`_x_ = _option1_ or _x_ = _option2_ or _x_ = …`) but consolidate `is`/`is not` using `either`/`neither` for a pair of options (`_x_ is neither _option1_ or _option2_`) and `one of` for more than that (`_x_ is not of of _option1_, _option2_, or _option3_`)\n> - an alias can also be compared against a number, bigint, or extended mathematical value using `<`, `≤`, `>`, or `≥`\n\nwe should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page","format":"org.matrix.custom.html","formatted_body":"* <p>what is the process for changing <a href=\"https://github.com/tc39/ecma262/wiki/Editorial-Conventions\">Editorial Conventions</a>? https://github.com/tc39/ecma262/pull/3832#discussion_r3172092825 highlights some gaps, specifically about how to compare an [extended] mathematical value or bigint against multiple options (<em>which I think should be <code>If _x_ = $a or _x_ = $b</code> when there are two options, and presumably <code>If _x_ = $a or _x_ = $b or _x_ = …</code> when there are more</em>). So I think I'd like the first few bullets of that section to look like</p>\n<blockquote>\n<ul>\n<li>when comparing an alias against a constant or collection of options, the alias should be on the left</li>\n<li>when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:</li>\n<li><code>_x_ = …</code> or <code>_x_ ≠ …</code> for an extended mathematical value or bigint</li>\n<li><code>SameValue(_x_, …)</code> for an object, non-well-known symbol, or arbitrary language value</li>\n<li><code>_x_ is …</code> or <code>_x_ is not …</code> for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)</li>\n<li>when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise <code>=</code>/<code>≠</code>/<code>SameValue</code> (<code>_x_ = _option1_ or _x_ = _option2_ or _x_ = …</code>) but consolidate <code>is</code>/<code>is not</code> using <code>either</code>/<code>neither</code> for a pair of options (<code>_x_ is neither _option1_ or _option2_</code>) and <code>one of</code> for more than that (<code>_x_ is not of of _option1_, _option2_, or _option3_</code>)</li>\n<li>an alias can also be compared against a number, bigint, or extended mathematical value using <code>&lt;</code>, <code>≤</code>, <code>&gt;</code>, or <code>≥</code></li>\n</ul>\n</blockquote>\n<p>we should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page</p>\n","m.mentions":{},"m.new_content":{"body":"what is the process for changing [Editorial Conventions](https://github.com/tc39/ecma262/wiki/Editorial-Conventions)? https://github.com/tc39/ecma262/pull/3832#discussion\\_r3172092825 highlights some gaps, specifically about how to compare an \\[extended\\] mathematical value or bigint against multiple options (_which I think should be `If _x_ = $a or _x_ = $b` when there are two options, and presumably `If _x_ = $a or _x_ = $b or _x_ = …` when there are more_). So I think I'd like the first few bullets of that section to look like\n\n> - when comparing an alias against a constant or collection of options, the alias should be on the left\n> - when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:\n> - `_x_ = …` or `_x_ ≠ …` for an extended mathematical value or bigint\n> - `SameValue(_x_, …)` for an object, non-well-known symbol, or arbitrary language value\n> - `_x_ is …` or `_x_ is not …` for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)\n> - when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise `=`/`≠`/`SameValue` (`_x_ = _option1_ or _x_ = _option2_ or _x_ = …`) but consolidate `is`/`is not` using `either`/`neither` for a pair of options (`_x_ is neither _option1_ or _option2_`) and `one of` for more than that (`_x_ is not of of _option1_, _option2_, or _option3_`)\n> - an alias can also be compared against a number, bigint, or extended mathematical value using `<`, `≤`, `>`, or `≥`\n\nwe should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page","format":"org.matrix.custom.html","formatted_body":"<p>what is the process for changing <a href=\"https://github.com/tc39/ecma262/wiki/Editorial-Conventions\">Editorial Conventions</a>? https://github.com/tc39/ecma262/pull/3832#discussion_r3172092825 highlights some gaps, specifically about how to compare an [extended] mathematical value or bigint against multiple options (<em>which I think should be <code>If _x_ = $a or _x_ = $b</code> when there are two options, and presumably <code>If _x_ = $a or _x_ = $b or _x_ = …</code> when there are more</em>). So I think I'd like the first few bullets of that section to look like</p>\n<blockquote>\n<ul>\n<li>when comparing an alias against a constant or collection of options, the alias should be on the left</li>\n<li>when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:</li>\n<li><code>_x_ = …</code> or <code>_x_ ≠ …</code> for an extended mathematical value or bigint</li>\n<li><code>SameValue(_x_, …)</code> for an object, non-well-known symbol, or arbitrary language value</li>\n<li><code>_x_ is …</code> or <code>_x_ is not …</code> for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)</li>\n<li>when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise <code>=</code>/<code>≠</code>/<code>SameValue</code> (<code>_x_ = _option1_ or _x_ = _option2_ or _x_ = …</code>) but consolidate <code>is</code>/<code>is not</code> using <code>either</code>/<code>neither</code> for a pair of options (<code>_x_ is neither _option1_ or _option2_</code>) and <code>one of</code> for more than that (<code>_x_ is not of of _option1_, _option2_, or _option3_</code>)</li>\n<li>an alias can also be compared against a number, bigint, or extended mathematical value using <code>&lt;</code>, <code>≤</code>, <code>&gt;</code>, or <code>≥</code></li>\n</ul>\n</blockquote>\n<p>we should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page</p>\n","m.mentions":{},"msgtype":"m.text"},"m.relates_to":{"event_id":"$3dGfwzIEW0gOTSojR5jl1alfroNZyOpubwG2H3Jcckg","rel_type":"m.replace"},"msgtype":"m.text"},"ts":1777649676254,"senderName":"Richard Gibson","senderId":"@gibson042:matrix.org","id":"$b6pro71DuEVOJgGTqrOcffZskBYkcv_CJ_wKToyc5nY"},
{"content":{"body":"* what is the process for changing [Editorial Conventions](https://github.com/tc39/ecma262/wiki/Editorial-Conventions)? https://github.com/tc39/ecma262/pull/3832#discussion\\_r3172092825 highlights some gaps, specifically about how to compare an \\[extended\\] mathematical value or bigint against multiple options (_which I think should be `If _x_ = $a or _x_ = $b` when there are two options, and presumably `If _x_ = $a or _x_ = $b or _x_ = …` when there are more_). So I think I'd like the first few bullets of that section to look like\n\n> * when comparing an alias against a constant or collection of options, the alias should be on the left\n> * when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:\n>   * `_x_ = …` or `_x_ ≠ …` for an extended mathematical value or bigint\n>   * `SameValue(_x_, …)` for an object, non-well-known symbol, or arbitrary language value\n>   * `_x_ is …` or `_x_ is not …` for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)\n> * when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise `=`/`≠`/`SameValue` (`_x_ = _option1_ or _x_ = _option2_ or _x_ = …`) but consolidate `is`/`is not` using `either`/`neither` for a pair of options (`_x_ is neither _option1_ or _option2_`) and `one of` for more than that (`_x_ is not of of _option1_, _option2_, or _option3_`)\n> * an alias can also be compared against a number, bigint, or extended mathematical value using `<`, `≤`, `>`, or `≥`\n\nwe should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page","format":"org.matrix.custom.html","formatted_body":"* <p>what is the process for changing <a href=\"https://github.com/tc39/ecma262/wiki/Editorial-Conventions\">Editorial Conventions</a>? https://github.com/tc39/ecma262/pull/3832#discussion_r3172092825 highlights some gaps, specifically about how to compare an [extended] mathematical value or bigint against multiple options (<em>which I think should be <code>If _x_ = $a or _x_ = $b</code> when there are two options, and presumably <code>If _x_ = $a or _x_ = $b or _x_ = …</code> when there are more</em>). So I think I'd like the first few bullets of that section to look like</p>\n<blockquote>\n<ul>\n<li>when comparing an alias against a constant or collection of options, the alias should be on the left</li>\n<li>when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:\n<ul>\n<li><code>_x_ = …</code> or <code>_x_ ≠ …</code> for an extended mathematical value or bigint</li>\n<li><code>SameValue(_x_, …)</code> for an object, non-well-known symbol, or arbitrary language value</li>\n<li><code>_x_ is …</code> or <code>_x_ is not …</code> for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)</li>\n</ul>\n</li>\n<li>when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise <code>=</code>/<code>≠</code>/<code>SameValue</code> (<code>_x_ = _option1_ or _x_ = _option2_ or _x_ = …</code>) but consolidate <code>is</code>/<code>is not</code> using <code>either</code>/<code>neither</code> for a pair of options (<code>_x_ is neither _option1_ or _option2_</code>) and <code>one of</code> for more than that (<code>_x_ is not of of _option1_, _option2_, or _option3_</code>)</li>\n<li>an alias can also be compared against a number, bigint, or extended mathematical value using <code>&lt;</code>, <code>≤</code>, <code>&gt;</code>, or <code>≥</code></li>\n</ul>\n</blockquote>\n<p>we should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page</p>\n","m.mentions":{},"m.new_content":{"body":"what is the process for changing [Editorial Conventions](https://github.com/tc39/ecma262/wiki/Editorial-Conventions)? https://github.com/tc39/ecma262/pull/3832#discussion\\_r3172092825 highlights some gaps, specifically about how to compare an \\[extended\\] mathematical value or bigint against multiple options (_which I think should be `If _x_ = $a or _x_ = $b` when there are two options, and presumably `If _x_ = $a or _x_ = $b or _x_ = …` when there are more_). So I think I'd like the first few bullets of that section to look like\n\n> * when comparing an alias against a constant or collection of options, the alias should be on the left\n> * when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:\n>   * `_x_ = …` or `_x_ ≠ …` for an extended mathematical value or bigint\n>   * `SameValue(_x_, …)` for an object, non-well-known symbol, or arbitrary language value\n>   * `_x_ is …` or `_x_ is not …` for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)\n> * when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise `=`/`≠`/`SameValue` (`_x_ = _option1_ or _x_ = _option2_ or _x_ = …`) but consolidate `is`/`is not` using `either`/`neither` for a pair of options (`_x_ is neither _option1_ or _option2_`) and `one of` for more than that (`_x_ is not of of _option1_, _option2_, or _option3_`)\n> * an alias can also be compared against a number, bigint, or extended mathematical value using `<`, `≤`, `>`, or `≥`\n\nwe should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page","format":"org.matrix.custom.html","formatted_body":"<p>what is the process for changing <a href=\"https://github.com/tc39/ecma262/wiki/Editorial-Conventions\">Editorial Conventions</a>? https://github.com/tc39/ecma262/pull/3832#discussion_r3172092825 highlights some gaps, specifically about how to compare an [extended] mathematical value or bigint against multiple options (<em>which I think should be <code>If _x_ = $a or _x_ = $b</code> when there are two options, and presumably <code>If _x_ = $a or _x_ = $b or _x_ = …</code> when there are more</em>). So I think I'd like the first few bullets of that section to look like</p>\n<blockquote>\n<ul>\n<li>when comparing an alias against a constant or collection of options, the alias should be on the left</li>\n<li>when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:\n<ul>\n<li><code>_x_ = …</code> or <code>_x_ ≠ …</code> for an extended mathematical value or bigint</li>\n<li><code>SameValue(_x_, …)</code> for an object, non-well-known symbol, or arbitrary language value</li>\n<li><code>_x_ is …</code> or <code>_x_ is not …</code> for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)</li>\n</ul>\n</li>\n<li>when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise <code>=</code>/<code>≠</code>/<code>SameValue</code> (<code>_x_ = _option1_ or _x_ = _option2_ or _x_ = …</code>) but consolidate <code>is</code>/<code>is not</code> using <code>either</code>/<code>neither</code> for a pair of options (<code>_x_ is neither _option1_ or _option2_</code>) and <code>one of</code> for more than that (<code>_x_ is not of of _option1_, _option2_, or _option3_</code>)</li>\n<li>an alias can also be compared against a number, bigint, or extended mathematical value using <code>&lt;</code>, <code>≤</code>, <code>&gt;</code>, or <code>≥</code></li>\n</ul>\n</blockquote>\n<p>we should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page</p>\n","m.mentions":{},"msgtype":"m.text"},"m.relates_to":{"event_id":"$3dGfwzIEW0gOTSojR5jl1alfroNZyOpubwG2H3Jcckg","rel_type":"m.replace"},"msgtype":"m.text"},"ts":1777649701652,"senderName":"Richard Gibson","senderId":"@gibson042:matrix.org","id":"$09owUaEFF_njqcS6IVbB4UJPjkYzlgjQ_BJDg6UaV2E"},
{"content":{"body":"* what is the process for changing [Editorial Conventions](https://github.com/tc39/ecma262/wiki/Editorial-Conventions)? https://github.com/tc39/ecma262/pull/3832#discussion\\_r3172092825 highlights some gaps, specifically about how to compare an \\[extended\\] mathematical value or bigint against multiple options (_which I think should be `If _x_ = $a or _x_ = $b` when there are two options, and presumably `If _x_ = $a or _x_ = $b or _x_ = …` when there are more_). So I think I'd like the first few bullets of that section to look like\n\n> * when comparing an alias against a single value or collection of options, the alias should be on the left\n> * when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:\n\n>    * `_x_ = …` or `_x_ ≠ …` for an extended mathematical value or bigint\n>    * `SameValue(_x_, …)` for an object, non-well-known symbol, or arbitrary language value\n>    * `_x_ is …` or `_x_ is not …` for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)\n> * when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise `=`/`≠`/`SameValue` (`_x_ = _option1_ or _x_ = _option2_ or _x_ = …`) but consolidate `is`/`is not` using `either`/`neither` for a pair of options (`_x_ is neither _option1_ or _option2_`) and `one of` for more than that (`_x_ is not of of _option1_, _option2_, or _option3_`)\n> * an alias can also be compared against a number, bigint, or extended mathematical value using `<`, `≤`, `>`, or `≥`\n\nwe should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page","format":"org.matrix.custom.html","formatted_body":"* <p>what is the process for changing <a href=\"https://github.com/tc39/ecma262/wiki/Editorial-Conventions\">Editorial Conventions</a>? https://github.com/tc39/ecma262/pull/3832#discussion_r3172092825 highlights some gaps, specifically about how to compare an [extended] mathematical value or bigint against multiple options (<em>which I think should be <code>If _x_ = $a or _x_ = $b</code> when there are two options, and presumably <code>If _x_ = $a or _x_ = $b or _x_ = …</code> when there are more</em>). So I think I'd like the first few bullets of that section to look like</p>\n<blockquote>\n<ul>\n<li>when comparing an alias against a single value or collection of options, the alias should be on the left</li>\n<li>when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:</li>\n</ul>\n</blockquote>\n<blockquote>\n<ul>\n<li><code>_x_ = …</code> or <code>_x_ ≠ …</code> for an extended mathematical value or bigint</li>\n<li><code>SameValue(_x_, …)</code> for an object, non-well-known symbol, or arbitrary language value</li>\n<li><code>_x_ is …</code> or <code>_x_ is not …</code> for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)</li>\n<li>when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise <code>=</code>/<code>≠</code>/<code>SameValue</code> (<code>_x_ = _option1_ or _x_ = _option2_ or _x_ = …</code>) but consolidate <code>is</code>/<code>is not</code> using <code>either</code>/<code>neither</code> for a pair of options (<code>_x_ is neither _option1_ or _option2_</code>) and <code>one of</code> for more than that (<code>_x_ is not of of _option1_, _option2_, or _option3_</code>)</li>\n<li>an alias can also be compared against a number, bigint, or extended mathematical value using <code>&lt;</code>, <code>≤</code>, <code>&gt;</code>, or <code>≥</code></li>\n</ul>\n</blockquote>\n<p>we should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page</p>\n","m.mentions":{},"m.new_content":{"body":"what is the process for changing [Editorial Conventions](https://github.com/tc39/ecma262/wiki/Editorial-Conventions)? https://github.com/tc39/ecma262/pull/3832#discussion\\_r3172092825 highlights some gaps, specifically about how to compare an \\[extended\\] mathematical value or bigint against multiple options (_which I think should be `If _x_ = $a or _x_ = $b` when there are two options, and presumably `If _x_ = $a or _x_ = $b or _x_ = …` when there are more_). So I think I'd like the first few bullets of that section to look like\n\n> * when comparing an alias against a single value or collection of options, the alias should be on the left\n> * when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:\n\n>    * `_x_ = …` or `_x_ ≠ …` for an extended mathematical value or bigint\n>    * `SameValue(_x_, …)` for an object, non-well-known symbol, or arbitrary language value\n>    * `_x_ is …` or `_x_ is not …` for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)\n> * when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise `=`/`≠`/`SameValue` (`_x_ = _option1_ or _x_ = _option2_ or _x_ = …`) but consolidate `is`/`is not` using `either`/`neither` for a pair of options (`_x_ is neither _option1_ or _option2_`) and `one of` for more than that (`_x_ is not of of _option1_, _option2_, or _option3_`)\n> * an alias can also be compared against a number, bigint, or extended mathematical value using `<`, `≤`, `>`, or `≥`\n\nwe should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page","format":"org.matrix.custom.html","formatted_body":"<p>what is the process for changing <a href=\"https://github.com/tc39/ecma262/wiki/Editorial-Conventions\">Editorial Conventions</a>? https://github.com/tc39/ecma262/pull/3832#discussion_r3172092825 highlights some gaps, specifically about how to compare an [extended] mathematical value or bigint against multiple options (<em>which I think should be <code>If _x_ = $a or _x_ = $b</code> when there are two options, and presumably <code>If _x_ = $a or _x_ = $b or _x_ = …</code> when there are more</em>). So I think I'd like the first few bullets of that section to look like</p>\n<blockquote>\n<ul>\n<li>when comparing an alias against a single value or collection of options, the alias should be on the left</li>\n<li>when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:</li>\n</ul>\n</blockquote>\n<blockquote>\n<ul>\n<li><code>_x_ = …</code> or <code>_x_ ≠ …</code> for an extended mathematical value or bigint</li>\n<li><code>SameValue(_x_, …)</code> for an object, non-well-known symbol, or arbitrary language value</li>\n<li><code>_x_ is …</code> or <code>_x_ is not …</code> for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)</li>\n<li>when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise <code>=</code>/<code>≠</code>/<code>SameValue</code> (<code>_x_ = _option1_ or _x_ = _option2_ or _x_ = …</code>) but consolidate <code>is</code>/<code>is not</code> using <code>either</code>/<code>neither</code> for a pair of options (<code>_x_ is neither _option1_ or _option2_</code>) and <code>one of</code> for more than that (<code>_x_ is not of of _option1_, _option2_, or _option3_</code>)</li>\n<li>an alias can also be compared against a number, bigint, or extended mathematical value using <code>&lt;</code>, <code>≤</code>, <code>&gt;</code>, or <code>≥</code></li>\n</ul>\n</blockquote>\n<p>we should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page</p>\n","m.mentions":{},"msgtype":"m.text"},"m.relates_to":{"event_id":"$3dGfwzIEW0gOTSojR5jl1alfroNZyOpubwG2H3Jcckg","rel_type":"m.replace"},"msgtype":"m.text"},"ts":1777649794971,"senderName":"Richard Gibson","senderId":"@gibson042:matrix.org","id":"$_r9Ic0am_Ekt4RTzbxCm6tqdHEXcIpz7ARhHhs7PQAs"},
{"content":{"body":"* what is the process for changing [Editorial Conventions](https://github.com/tc39/ecma262/wiki/Editorial-Conventions)? https://github.com/tc39/ecma262/pull/3832#discussion\\_r3172092825 highlights some gaps, specifically about how to compare an \\[extended\\] mathematical value or bigint against multiple options (_which I think should be `If _x_ = $a or _x_ = $b` when there are two options, and presumably `If _x_ = $a or _x_ = $b or _x_ = …` when there are more_). So I think I'd like the first few bullets of that section to look like\n\n> * when comparing an alias against a single value or collection of options, the alias should be on the left\n> * when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:\n\n>   * `_x_ = …` or `_x_ ≠ …` for an extended mathematical value or bigint\n>   * `SameValue(_x_, …)` for an object, non-well-known symbol, or arbitrary language value\n>   * `_x_ is …` or `_x_ is not …` for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)\n> * when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise `=`/`≠`/`SameValue` (`_x_ = _option1_ or _x_ = _option2_ or _x_ = …`) but consolidate `is`/`is not` using `either`/`neither` for a pair of options (`_x_ is neither _option1_ or _option2_`) and `one of` for more than that (`_x_ is not of of _option1_, _option2_, or _option3_`)\n> * an alias can also be compared against a number, bigint, or extended mathematical value using `<`, `≤`, `>`, or `≥`\n\nwe should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page","format":"org.matrix.custom.html","formatted_body":"* <p>what is the process for changing <a href=\"https://github.com/tc39/ecma262/wiki/Editorial-Conventions\">Editorial Conventions</a>? https://github.com/tc39/ecma262/pull/3832#discussion_r3172092825 highlights some gaps, specifically about how to compare an [extended] mathematical value or bigint against multiple options (<em>which I think should be <code>If _x_ = $a or _x_ = $b</code> when there are two options, and presumably <code>If _x_ = $a or _x_ = $b or _x_ = …</code> when there are more</em>). So I think I'd like the first few bullets of that section to look like</p>\n<blockquote>\n<ul>\n<li>when comparing an alias against a single value or collection of options, the alias should be on the left</li>\n<li>when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:</li>\n</ul>\n</blockquote>\n<blockquote>\n<ul>\n<li><code>_x_ = …</code> or <code>_x_ ≠ …</code> for an extended mathematical value or bigint</li>\n<li><code>SameValue(_x_, …)</code> for an object, non-well-known symbol, or arbitrary language value</li>\n<li><code>_x_ is …</code> or <code>_x_ is not …</code> for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)</li>\n<li>when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise <code>=</code>/<code>≠</code>/<code>SameValue</code> (<code>_x_ = _option1_ or _x_ = _option2_ or _x_ = …</code>) but consolidate <code>is</code>/<code>is not</code> using <code>either</code>/<code>neither</code> for a pair of options (<code>_x_ is neither _option1_ or _option2_</code>) and <code>one of</code> for more than that (<code>_x_ is not of of _option1_, _option2_, or _option3_</code>)</li>\n<li>an alias can also be compared against a number, bigint, or extended mathematical value using <code>&lt;</code>, <code>≤</code>, <code>&gt;</code>, or <code>≥</code></li>\n</ul>\n</blockquote>\n<p>we should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page</p>\n","m.mentions":{},"m.new_content":{"body":"what is the process for changing [Editorial Conventions](https://github.com/tc39/ecma262/wiki/Editorial-Conventions)? https://github.com/tc39/ecma262/pull/3832#discussion\\_r3172092825 highlights some gaps, specifically about how to compare an \\[extended\\] mathematical value or bigint against multiple options (_which I think should be `If _x_ = $a or _x_ = $b` when there are two options, and presumably `If _x_ = $a or _x_ = $b or _x_ = …` when there are more_). So I think I'd like the first few bullets of that section to look like\n\n> * when comparing an alias against a single value or collection of options, the alias should be on the left\n> * when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:\n\n>   * `_x_ = …` or `_x_ ≠ …` for an extended mathematical value or bigint\n>   * `SameValue(_x_, …)` for an object, non-well-known symbol, or arbitrary language value\n>   * `_x_ is …` or `_x_ is not …` for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)\n> * when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise `=`/`≠`/`SameValue` (`_x_ = _option1_ or _x_ = _option2_ or _x_ = …`) but consolidate `is`/`is not` using `either`/`neither` for a pair of options (`_x_ is neither _option1_ or _option2_`) and `one of` for more than that (`_x_ is not of of _option1_, _option2_, or _option3_`)\n> * an alias can also be compared against a number, bigint, or extended mathematical value using `<`, `≤`, `>`, or `≥`\n\nwe should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page","format":"org.matrix.custom.html","formatted_body":"<p>what is the process for changing <a href=\"https://github.com/tc39/ecma262/wiki/Editorial-Conventions\">Editorial Conventions</a>? https://github.com/tc39/ecma262/pull/3832#discussion_r3172092825 highlights some gaps, specifically about how to compare an [extended] mathematical value or bigint against multiple options (<em>which I think should be <code>If _x_ = $a or _x_ = $b</code> when there are two options, and presumably <code>If _x_ = $a or _x_ = $b or _x_ = …</code> when there are more</em>). So I think I'd like the first few bullets of that section to look like</p>\n<blockquote>\n<ul>\n<li>when comparing an alias against a single value or collection of options, the alias should be on the left</li>\n<li>when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:</li>\n</ul>\n</blockquote>\n<blockquote>\n<ul>\n<li><code>_x_ = …</code> or <code>_x_ ≠ …</code> for an extended mathematical value or bigint</li>\n<li><code>SameValue(_x_, …)</code> for an object, non-well-known symbol, or arbitrary language value</li>\n<li><code>_x_ is …</code> or <code>_x_ is not …</code> for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)</li>\n<li>when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise <code>=</code>/<code>≠</code>/<code>SameValue</code> (<code>_x_ = _option1_ or _x_ = _option2_ or _x_ = …</code>) but consolidate <code>is</code>/<code>is not</code> using <code>either</code>/<code>neither</code> for a pair of options (<code>_x_ is neither _option1_ or _option2_</code>) and <code>one of</code> for more than that (<code>_x_ is not of of _option1_, _option2_, or _option3_</code>)</li>\n<li>an alias can also be compared against a number, bigint, or extended mathematical value using <code>&lt;</code>, <code>≤</code>, <code>&gt;</code>, or <code>≥</code></li>\n</ul>\n</blockquote>\n<p>we should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page</p>\n","m.mentions":{},"msgtype":"m.text"},"m.relates_to":{"event_id":"$3dGfwzIEW0gOTSojR5jl1alfroNZyOpubwG2H3Jcckg","rel_type":"m.replace"},"msgtype":"m.text"},"ts":1777649810237,"senderName":"Richard Gibson","senderId":"@gibson042:matrix.org","id":"$CI4E7xT0v3g0VoqFfATrfAy6maUq14RCyH4g8wUqKrM"},
{"content":{"body":"what is the process for changing [Editorial Conventions](https://github.com/tc39/ecma262/wiki/Editorial-Conventions)? https://github.com/tc39/ecma262/pull/3832#discussion\\_r3172092825 highlights some gaps, specifically about how to compare an \\[extended\\] mathematical value or bigint against multiple options (_which I think should be `If _x_ = $a or _x_ = $b` when there are two options, and presumably `If _x_ = $a or _x_ = $b or _x_ = …` when there are more_). So I think I'd like the first few bullets of that section to look like\n\n> * when comparing an alias against a single value or collection of options, the alias should be on the left\n> * when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:\n\n>   * `_x_ = …` or `_x_ ≠ …` for an extended mathematical value or bigint\n>   * `SameValue(_x_, …)` for an object, non-well-known symbol, or arbitrary language value\n>   * `_x_ is …` or `_x_ is not …` for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)\n> * when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise `=`/`≠`/`SameValue` (`_x_ = _option1_ or _x_ = _option2_ or _x_ = …`) but consolidate `is`/`is not` using `either`/`neither` for a pair of options (`_x_ is neither _option1_ or _option2_`) and `one of` for more than that (`_x_ is not of of _option1_, _option2_, or _option3_`)\n> * an alias can also be compared against a number, bigint, or extended mathematical value using `<`, `≤`, `>`, or `≥`\n\nwe should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page","format":"org.matrix.custom.html","formatted_body":"<p>what is the process for changing <a href=\"https://github.com/tc39/ecma262/wiki/Editorial-Conventions\">Editorial Conventions</a>? https://github.com/tc39/ecma262/pull/3832#discussion_r3172092825 highlights some gaps, specifically about how to compare an [extended] mathematical value or bigint against multiple options (<em>which I think should be <code>If _x_ = $a or _x_ = $b</code> when there are two options, and presumably <code>If _x_ = $a or _x_ = $b or _x_ = …</code> when there are more</em>). So I think I'd like the first few bullets of that section to look like</p>\n<blockquote>\n<ul>\n<li>when comparing an alias against a single value or collection of options, the alias should be on the left</li>\n<li>when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:</li>\n</ul>\n</blockquote>\n<blockquote>\n<ul>\n<li><code>_x_ = …</code> or <code>_x_ ≠ …</code> for an extended mathematical value or bigint</li>\n<li><code>SameValue(_x_, …)</code> for an object, non-well-known symbol, or arbitrary language value</li>\n<li><code>_x_ is …</code> or <code>_x_ is not …</code> for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)</li>\n<li>when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise <code>=</code>/<code>≠</code>/<code>SameValue</code> (<code>_x_ = _option1_ or _x_ = _option2_ or _x_ = …</code>) but consolidate <code>is</code>/<code>is not</code> using <code>either</code>/<code>neither</code> for a pair of options (<code>_x_ is neither _option1_ or _option2_</code>) and <code>one of</code> for more than that (<code>_x_ is not of of _option1_, _option2_, or _option3_</code>)</li>\n<li>an alias can also be compared against a number, bigint, or extended mathematical value using <code>&lt;</code>, <code>≤</code>, <code>&gt;</code>, or <code>≥</code></li>\n</ul>\n</blockquote>\n<p>we should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page</p>\n","m.mentions":{},"msgtype":"m.text"},"ts":1777649856781,"senderName":"Richard Gibson","senderId":"@gibson042:matrix.org","id":"$8IRtC6sfDL74R5JdhVadYEKIJw8AZoMPEizAA0W1ulI"},
{"content":{"body":"* what is the process for changing [Editorial Conventions](https://github.com/tc39/ecma262/wiki/Editorial-Conventions)? https://github.com/tc39/ecma262/pull/3832#discussion\\_r3172092825 highlights some gaps, specifically about how to compare an \\[extended\\] mathematical value or bigint against multiple options (_which I think should be `If _x_ = $a or _x_ = $b` when there are two options, and presumably `If _x_ = $a or _x_ = $b or _x_ = …` when there are more_). So I think I'd like the first few bullets of that section to look like\n\n> * when comparing an alias against a single value or collection of options, the alias should be on the left\n> * when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:\n>   * `_x_ = …` or `_x_ ≠ …` for an extended mathematical value or bigint\n>   * `SameValue(_x_, …)` for an object, non-well-known symbol, or arbitrary language value\n>   * `_x_ is …` or `_x_ is not …` for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)\n> * when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise `=`/`≠`/`SameValue` (`_x_ = _option1_ or _x_ = _option2_ or _x_ = …`) but consolidate `is`/`is not` using `either`/`neither` for a pair of options (`_x_ is neither _option1_ or _option2_`) and `one of` for more than that (`_x_ is not of of _option1_, _option2_, or _option3_`)\n> * an alias can also be compared against a number, bigint, or extended mathematical value using `<`, `≤`, `>`, or `≥`\n\nwe should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page","format":"org.matrix.custom.html","formatted_body":"* <p>what is the process for changing <a href=\"https://github.com/tc39/ecma262/wiki/Editorial-Conventions\">Editorial Conventions</a>? https://github.com/tc39/ecma262/pull/3832#discussion_r3172092825 highlights some gaps, specifically about how to compare an [extended] mathematical value or bigint against multiple options (<em>which I think should be <code>If _x_ = $a or _x_ = $b</code> when there are two options, and presumably <code>If _x_ = $a or _x_ = $b or _x_ = …</code> when there are more</em>). So I think I'd like the first few bullets of that section to look like</p>\n<blockquote>\n<ul>\n<li>when comparing an alias against a single value or collection of options, the alias should be on the left</li>\n<li>when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:\n<ul>\n<li><code>_x_ = …</code> or <code>_x_ ≠ …</code> for an extended mathematical value or bigint</li>\n<li><code>SameValue(_x_, …)</code> for an object, non-well-known symbol, or arbitrary language value</li>\n<li><code>_x_ is …</code> or <code>_x_ is not …</code> for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)</li>\n</ul>\n</li>\n<li>when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise <code>=</code>/<code>≠</code>/<code>SameValue</code> (<code>_x_ = _option1_ or _x_ = _option2_ or _x_ = …</code>) but consolidate <code>is</code>/<code>is not</code> using <code>either</code>/<code>neither</code> for a pair of options (<code>_x_ is neither _option1_ or _option2_</code>) and <code>one of</code> for more than that (<code>_x_ is not of of _option1_, _option2_, or _option3_</code>)</li>\n<li>an alias can also be compared against a number, bigint, or extended mathematical value using <code>&lt;</code>, <code>≤</code>, <code>&gt;</code>, or <code>≥</code></li>\n</ul>\n</blockquote>\n<p>we should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page</p>\n","m.mentions":{},"m.new_content":{"body":"what is the process for changing [Editorial Conventions](https://github.com/tc39/ecma262/wiki/Editorial-Conventions)? https://github.com/tc39/ecma262/pull/3832#discussion\\_r3172092825 highlights some gaps, specifically about how to compare an \\[extended\\] mathematical value or bigint against multiple options (_which I think should be `If _x_ = $a or _x_ = $b` when there are two options, and presumably `If _x_ = $a or _x_ = $b or _x_ = …` when there are more_). So I think I'd like the first few bullets of that section to look like\n\n> * when comparing an alias against a single value or collection of options, the alias should be on the left\n> * when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:\n>   * `_x_ = …` or `_x_ ≠ …` for an extended mathematical value or bigint\n>   * `SameValue(_x_, …)` for an object, non-well-known symbol, or arbitrary language value\n>   * `_x_ is …` or `_x_ is not …` for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)\n> * when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise `=`/`≠`/`SameValue` (`_x_ = _option1_ or _x_ = _option2_ or _x_ = …`) but consolidate `is`/`is not` using `either`/`neither` for a pair of options (`_x_ is neither _option1_ or _option2_`) and `one of` for more than that (`_x_ is not of of _option1_, _option2_, or _option3_`)\n> * an alias can also be compared against a number, bigint, or extended mathematical value using `<`, `≤`, `>`, or `≥`\n\nwe should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page","format":"org.matrix.custom.html","formatted_body":"<p>what is the process for changing <a href=\"https://github.com/tc39/ecma262/wiki/Editorial-Conventions\">Editorial Conventions</a>? https://github.com/tc39/ecma262/pull/3832#discussion_r3172092825 highlights some gaps, specifically about how to compare an [extended] mathematical value or bigint against multiple options (<em>which I think should be <code>If _x_ = $a or _x_ = $b</code> when there are two options, and presumably <code>If _x_ = $a or _x_ = $b or _x_ = …</code> when there are more</em>). So I think I'd like the first few bullets of that section to look like</p>\n<blockquote>\n<ul>\n<li>when comparing an alias against a single value or collection of options, the alias should be on the left</li>\n<li>when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:\n<ul>\n<li><code>_x_ = …</code> or <code>_x_ ≠ …</code> for an extended mathematical value or bigint</li>\n<li><code>SameValue(_x_, …)</code> for an object, non-well-known symbol, or arbitrary language value</li>\n<li><code>_x_ is …</code> or <code>_x_ is not …</code> for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)</li>\n</ul>\n</li>\n<li>when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise <code>=</code>/<code>≠</code>/<code>SameValue</code> (<code>_x_ = _option1_ or _x_ = _option2_ or _x_ = …</code>) but consolidate <code>is</code>/<code>is not</code> using <code>either</code>/<code>neither</code> for a pair of options (<code>_x_ is neither _option1_ or _option2_</code>) and <code>one of</code> for more than that (<code>_x_ is not of of _option1_, _option2_, or _option3_</code>)</li>\n<li>an alias can also be compared against a number, bigint, or extended mathematical value using <code>&lt;</code>, <code>≤</code>, <code>&gt;</code>, or <code>≥</code></li>\n</ul>\n</blockquote>\n<p>we should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page</p>\n","m.mentions":{},"msgtype":"m.text"},"m.relates_to":{"event_id":"$8IRtC6sfDL74R5JdhVadYEKIJw8AZoMPEizAA0W1ulI","rel_type":"m.replace"},"msgtype":"m.text"},"ts":1777649881756,"senderName":"Richard Gibson","senderId":"@gibson042:matrix.org","id":"$canofDnhKZ6goCccNWqcEdoTzRlmWw23el7lnJT_po8"},
{"content":{"body":"* what is the process for changing [Editorial Conventions](https://github.com/tc39/ecma262/wiki/Editorial-Conventions)? https://github.com/tc39/ecma262/pull/3832#discussion\\_r3172092825 highlights some gaps, specifically about how to compare an \\[extended\\] mathematical value or bigint against multiple options (_which I think should be `If _x_ = $a or _x_ = $b` when there are two options, and presumably `If _x_ = $a or _x_ = $b or _x_ = …` when there are more_). So I think I'd like the first few bullets of that section to look like\n\n> * when comparing an alias against a single value or multiple options, the alias should be on the left\n> * when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:\n>   * `_x_ = …` or `_x_ ≠ …` for an extended mathematical value or bigint\n>   * `SameValue(_x_, …)` for an object, non-well-known symbol, or arbitrary language value\n>   * `_x_ is …` or `_x_ is not …` for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)\n> * when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise `=`/`≠`/`SameValue` (`_x_ = _option1_ or _x_ = _option2_ or _x_ = …`) but consolidate `is`/`is not` using `either`/`neither` for a pair of options (`_x_ is neither _option1_ or _option2_`) and `one of` for more than that (`_x_ is not of of _option1_, _option2_, or _option3_`)\n> * an alias can also be compared against a number, bigint, or extended mathematical value using `<`, `≤`, `>`, or `≥`\n\nwe should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page","format":"org.matrix.custom.html","formatted_body":"* <p>what is the process for changing <a href=\"https://github.com/tc39/ecma262/wiki/Editorial-Conventions\">Editorial Conventions</a>? https://github.com/tc39/ecma262/pull/3832#discussion_r3172092825 highlights some gaps, specifically about how to compare an [extended] mathematical value or bigint against multiple options (<em>which I think should be <code>If _x_ = $a or _x_ = $b</code> when there are two options, and presumably <code>If _x_ = $a or _x_ = $b or _x_ = …</code> when there are more</em>). So I think I'd like the first few bullets of that section to look like</p>\n<blockquote>\n<ul>\n<li>when comparing an alias against a single value or multiple options, the alias should be on the left</li>\n<li>when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:\n<ul>\n<li><code>_x_ = …</code> or <code>_x_ ≠ …</code> for an extended mathematical value or bigint</li>\n<li><code>SameValue(_x_, …)</code> for an object, non-well-known symbol, or arbitrary language value</li>\n<li><code>_x_ is …</code> or <code>_x_ is not …</code> for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)</li>\n</ul>\n</li>\n<li>when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise <code>=</code>/<code>≠</code>/<code>SameValue</code> (<code>_x_ = _option1_ or _x_ = _option2_ or _x_ = …</code>) but consolidate <code>is</code>/<code>is not</code> using <code>either</code>/<code>neither</code> for a pair of options (<code>_x_ is neither _option1_ or _option2_</code>) and <code>one of</code> for more than that (<code>_x_ is not of of _option1_, _option2_, or _option3_</code>)</li>\n<li>an alias can also be compared against a number, bigint, or extended mathematical value using <code>&lt;</code>, <code>≤</code>, <code>&gt;</code>, or <code>≥</code></li>\n</ul>\n</blockquote>\n<p>we should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page</p>\n","m.mentions":{},"m.new_content":{"body":"what is the process for changing [Editorial Conventions](https://github.com/tc39/ecma262/wiki/Editorial-Conventions)? https://github.com/tc39/ecma262/pull/3832#discussion\\_r3172092825 highlights some gaps, specifically about how to compare an \\[extended\\] mathematical value or bigint against multiple options (_which I think should be `If _x_ = $a or _x_ = $b` when there are two options, and presumably `If _x_ = $a or _x_ = $b or _x_ = …` when there are more_). So I think I'd like the first few bullets of that section to look like\n\n> * when comparing an alias against a single value or multiple options, the alias should be on the left\n> * when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:\n>   * `_x_ = …` or `_x_ ≠ …` for an extended mathematical value or bigint\n>   * `SameValue(_x_, …)` for an object, non-well-known symbol, or arbitrary language value\n>   * `_x_ is …` or `_x_ is not …` for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)\n> * when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise `=`/`≠`/`SameValue` (`_x_ = _option1_ or _x_ = _option2_ or _x_ = …`) but consolidate `is`/`is not` using `either`/`neither` for a pair of options (`_x_ is neither _option1_ or _option2_`) and `one of` for more than that (`_x_ is not of of _option1_, _option2_, or _option3_`)\n> * an alias can also be compared against a number, bigint, or extended mathematical value using `<`, `≤`, `>`, or `≥`\n\nwe should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page","format":"org.matrix.custom.html","formatted_body":"<p>what is the process for changing <a href=\"https://github.com/tc39/ecma262/wiki/Editorial-Conventions\">Editorial Conventions</a>? https://github.com/tc39/ecma262/pull/3832#discussion_r3172092825 highlights some gaps, specifically about how to compare an [extended] mathematical value or bigint against multiple options (<em>which I think should be <code>If _x_ = $a or _x_ = $b</code> when there are two options, and presumably <code>If _x_ = $a or _x_ = $b or _x_ = …</code> when there are more</em>). So I think I'd like the first few bullets of that section to look like</p>\n<blockquote>\n<ul>\n<li>when comparing an alias against a single value or multiple options, the alias should be on the left</li>\n<li>when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:\n<ul>\n<li><code>_x_ = …</code> or <code>_x_ ≠ …</code> for an extended mathematical value or bigint</li>\n<li><code>SameValue(_x_, …)</code> for an object, non-well-known symbol, or arbitrary language value</li>\n<li><code>_x_ is …</code> or <code>_x_ is not …</code> for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)</li>\n</ul>\n</li>\n<li>when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise <code>=</code>/<code>≠</code>/<code>SameValue</code> (<code>_x_ = _option1_ or _x_ = _option2_ or _x_ = …</code>) but consolidate <code>is</code>/<code>is not</code> using <code>either</code>/<code>neither</code> for a pair of options (<code>_x_ is neither _option1_ or _option2_</code>) and <code>one of</code> for more than that (<code>_x_ is not of of _option1_, _option2_, or _option3_</code>)</li>\n<li>an alias can also be compared against a number, bigint, or extended mathematical value using <code>&lt;</code>, <code>≤</code>, <code>&gt;</code>, or <code>≥</code></li>\n</ul>\n</blockquote>\n<p>we should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page</p>\n","m.mentions":{},"msgtype":"m.text"},"m.relates_to":{"event_id":"$8IRtC6sfDL74R5JdhVadYEKIJw8AZoMPEizAA0W1ulI","rel_type":"m.replace"},"msgtype":"m.text"},"ts":1777649929352,"senderName":"Richard Gibson","senderId":"@gibson042:matrix.org","id":"$FgsZ1SyubJc7MwxJCmmdA_gGC2Ac7TP3Rr2Zh-M5yd8"},
{"content":{"body":"* what is the process for changing [Editorial Conventions](https://github.com/tc39/ecma262/wiki/Editorial-Conventions)? https://github.com/tc39/ecma262/pull/3832#discussion\\_r3172092825 highlights some gaps, specifically about how to compare an \\[extended\\] mathematical value or bigint against multiple options (_which I think should be `If _x_ = $a or _x_ = $b` when there are two options, and presumably `If _x_ = $a or _x_ = $b or _x_ = …` when there are more_). So I think I'd like the first few bullets of that section to look like\n\n> * when comparing an alias against a single value or multiple options, the alias should be on the left\n> * when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:\n>   * `_x_ = …` or `_x_ ≠ …` for an extended mathematical value or bigint\n>   * `SameValue(_x_, …)` for an object, non-well-known symbol, or arbitrary language value\n>   * `_x_ is …` or `_x_ is not …` for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)\n> * when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise `=`/`≠`/`SameValue` (`_x_ = _option1_ or _x_ = _option2_ or _x_ = …`) but consolidate `is`/`is not` using `either`/`neither` for a pair of options (`_x_ is neither _option1_ nor _option2_`) and `one of` for more than that (`_x_ is not of of _option1_, _option2_, or _option3_`)\n> * an alias can also be compared against a number, bigint, or extended mathematical value using `<`, `≤`, `>`, or `≥`\n\nwe should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page","format":"org.matrix.custom.html","formatted_body":"* <p>what is the process for changing <a href=\"https://github.com/tc39/ecma262/wiki/Editorial-Conventions\">Editorial Conventions</a>? https://github.com/tc39/ecma262/pull/3832#discussion_r3172092825 highlights some gaps, specifically about how to compare an [extended] mathematical value or bigint against multiple options (<em>which I think should be <code>If _x_ = $a or _x_ = $b</code> when there are two options, and presumably <code>If _x_ = $a or _x_ = $b or _x_ = …</code> when there are more</em>). So I think I'd like the first few bullets of that section to look like</p>\n<blockquote>\n<ul>\n<li>when comparing an alias against a single value or multiple options, the alias should be on the left</li>\n<li>when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:\n<ul>\n<li><code>_x_ = …</code> or <code>_x_ ≠ …</code> for an extended mathematical value or bigint</li>\n<li><code>SameValue(_x_, …)</code> for an object, non-well-known symbol, or arbitrary language value</li>\n<li><code>_x_ is …</code> or <code>_x_ is not …</code> for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)</li>\n</ul>\n</li>\n<li>when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise <code>=</code>/<code>≠</code>/<code>SameValue</code> (<code>_x_ = _option1_ or _x_ = _option2_ or _x_ = …</code>) but consolidate <code>is</code>/<code>is not</code> using <code>either</code>/<code>neither</code> for a pair of options (<code>_x_ is neither _option1_ nor _option2_</code>) and <code>one of</code> for more than that (<code>_x_ is not of of _option1_, _option2_, or _option3_</code>)</li>\n<li>an alias can also be compared against a number, bigint, or extended mathematical value using <code>&lt;</code>, <code>≤</code>, <code>&gt;</code>, or <code>≥</code></li>\n</ul>\n</blockquote>\n<p>we should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page</p>\n","m.mentions":{},"m.new_content":{"body":"what is the process for changing [Editorial Conventions](https://github.com/tc39/ecma262/wiki/Editorial-Conventions)? https://github.com/tc39/ecma262/pull/3832#discussion\\_r3172092825 highlights some gaps, specifically about how to compare an \\[extended\\] mathematical value or bigint against multiple options (_which I think should be `If _x_ = $a or _x_ = $b` when there are two options, and presumably `If _x_ = $a or _x_ = $b or _x_ = …` when there are more_). So I think I'd like the first few bullets of that section to look like\n\n> * when comparing an alias against a single value or multiple options, the alias should be on the left\n> * when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:\n>   * `_x_ = …` or `_x_ ≠ …` for an extended mathematical value or bigint\n>   * `SameValue(_x_, …)` for an object, non-well-known symbol, or arbitrary language value\n>   * `_x_ is …` or `_x_ is not …` for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)\n> * when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise `=`/`≠`/`SameValue` (`_x_ = _option1_ or _x_ = _option2_ or _x_ = …`) but consolidate `is`/`is not` using `either`/`neither` for a pair of options (`_x_ is neither _option1_ nor _option2_`) and `one of` for more than that (`_x_ is not of of _option1_, _option2_, or _option3_`)\n> * an alias can also be compared against a number, bigint, or extended mathematical value using `<`, `≤`, `>`, or `≥`\n\nwe should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page","format":"org.matrix.custom.html","formatted_body":"<p>what is the process for changing <a href=\"https://github.com/tc39/ecma262/wiki/Editorial-Conventions\">Editorial Conventions</a>? https://github.com/tc39/ecma262/pull/3832#discussion_r3172092825 highlights some gaps, specifically about how to compare an [extended] mathematical value or bigint against multiple options (<em>which I think should be <code>If _x_ = $a or _x_ = $b</code> when there are two options, and presumably <code>If _x_ = $a or _x_ = $b or _x_ = …</code> when there are more</em>). So I think I'd like the first few bullets of that section to look like</p>\n<blockquote>\n<ul>\n<li>when comparing an alias against a single value or multiple options, the alias should be on the left</li>\n<li>when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:\n<ul>\n<li><code>_x_ = …</code> or <code>_x_ ≠ …</code> for an extended mathematical value or bigint</li>\n<li><code>SameValue(_x_, …)</code> for an object, non-well-known symbol, or arbitrary language value</li>\n<li><code>_x_ is …</code> or <code>_x_ is not …</code> for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)</li>\n</ul>\n</li>\n<li>when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise <code>=</code>/<code>≠</code>/<code>SameValue</code> (<code>_x_ = _option1_ or _x_ = _option2_ or _x_ = …</code>) but consolidate <code>is</code>/<code>is not</code> using <code>either</code>/<code>neither</code> for a pair of options (<code>_x_ is neither _option1_ nor _option2_</code>) and <code>one of</code> for more than that (<code>_x_ is not of of _option1_, _option2_, or _option3_</code>)</li>\n<li>an alias can also be compared against a number, bigint, or extended mathematical value using <code>&lt;</code>, <code>≤</code>, <code>&gt;</code>, or <code>≥</code></li>\n</ul>\n</blockquote>\n<p>we should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page</p>\n","m.mentions":{},"msgtype":"m.text"},"m.relates_to":{"event_id":"$8IRtC6sfDL74R5JdhVadYEKIJw8AZoMPEizAA0W1ulI","rel_type":"m.replace"},"msgtype":"m.text"},"ts":1777650020239,"senderName":"Richard Gibson","senderId":"@gibson042:matrix.org","id":"$nr-VOV1SmNExe2ff0MFqbZCyTvIwRb6hv76UcaC1_ks"},
{"content":{"body":"* what is the process for changing [Editorial Conventions](https://github.com/tc39/ecma262/wiki/Editorial-Conventions)? https://github.com/tc39/ecma262/pull/3832#discussion\\_r3172092825 highlights some gaps, specifically about how to compare an \\[extended\\] mathematical value or bigint against multiple options (_which I think should be `If _x_ = $a or _x_ = $b` when there are two options, and presumably `If _x_ = $a or _x_ = $b or _x_ = …` when there are more_). So I think I'd like the first few bullets of that section to look like\n\n> * when comparing an alias against a single value or multiple options, the alias should be on the left\n> * when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:\n>   * `_x_ = …` or `_x_ ≠ …` for an extended mathematical value or bigint\n>   * `SameValue(_x_, …)` for an object, non-well-known symbol, or arbitrary language value\n>   * `_x_ is …` or `_x_ is not …` for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)\n> * when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise `=`/`≠`/`SameValue` (`_x_ = _option1_ or _x_ = _option2_ or _x_ = …`) but consolidate `is`/`is not` using `either…or`/`neither…nor` for a pair of options (`_x_ is neither _option1_ nor _option2_`) and `one of` for more than that (`_x_ is not of of _option1_, _option2_, or _option3_`)\n> * an alias can also be compared against a number, bigint, or extended mathematical value using `<`, `≤`, `>`, or `≥`\n\nwe should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page","format":"org.matrix.custom.html","formatted_body":"* <p>what is the process for changing <a href=\"https://github.com/tc39/ecma262/wiki/Editorial-Conventions\">Editorial Conventions</a>? https://github.com/tc39/ecma262/pull/3832#discussion_r3172092825 highlights some gaps, specifically about how to compare an [extended] mathematical value or bigint against multiple options (<em>which I think should be <code>If _x_ = $a or _x_ = $b</code> when there are two options, and presumably <code>If _x_ = $a or _x_ = $b or _x_ = …</code> when there are more</em>). So I think I'd like the first few bullets of that section to look like</p>\n<blockquote>\n<ul>\n<li>when comparing an alias against a single value or multiple options, the alias should be on the left</li>\n<li>when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:\n<ul>\n<li><code>_x_ = …</code> or <code>_x_ ≠ …</code> for an extended mathematical value or bigint</li>\n<li><code>SameValue(_x_, …)</code> for an object, non-well-known symbol, or arbitrary language value</li>\n<li><code>_x_ is …</code> or <code>_x_ is not …</code> for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)</li>\n</ul>\n</li>\n<li>when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise <code>=</code>/<code>≠</code>/<code>SameValue</code> (<code>_x_ = _option1_ or _x_ = _option2_ or _x_ = …</code>) but consolidate <code>is</code>/<code>is not</code> using <code>either…or</code>/<code>neither…nor</code> for a pair of options (<code>_x_ is neither _option1_ nor _option2_</code>) and <code>one of</code> for more than that (<code>_x_ is not of of _option1_, _option2_, or _option3_</code>)</li>\n<li>an alias can also be compared against a number, bigint, or extended mathematical value using <code>&lt;</code>, <code>≤</code>, <code>&gt;</code>, or <code>≥</code></li>\n</ul>\n</blockquote>\n<p>we should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page</p>\n","m.mentions":{},"m.new_content":{"body":"what is the process for changing [Editorial Conventions](https://github.com/tc39/ecma262/wiki/Editorial-Conventions)? https://github.com/tc39/ecma262/pull/3832#discussion\\_r3172092825 highlights some gaps, specifically about how to compare an \\[extended\\] mathematical value or bigint against multiple options (_which I think should be `If _x_ = $a or _x_ = $b` when there are two options, and presumably `If _x_ = $a or _x_ = $b or _x_ = …` when there are more_). So I think I'd like the first few bullets of that section to look like\n\n> * when comparing an alias against a single value or multiple options, the alias should be on the left\n> * when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:\n>   * `_x_ = …` or `_x_ ≠ …` for an extended mathematical value or bigint\n>   * `SameValue(_x_, …)` for an object, non-well-known symbol, or arbitrary language value\n>   * `_x_ is …` or `_x_ is not …` for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)\n> * when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise `=`/`≠`/`SameValue` (`_x_ = _option1_ or _x_ = _option2_ or _x_ = …`) but consolidate `is`/`is not` using `either…or`/`neither…nor` for a pair of options (`_x_ is neither _option1_ nor _option2_`) and `one of` for more than that (`_x_ is not of of _option1_, _option2_, or _option3_`)\n> * an alias can also be compared against a number, bigint, or extended mathematical value using `<`, `≤`, `>`, or `≥`\n\nwe should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page","format":"org.matrix.custom.html","formatted_body":"<p>what is the process for changing <a href=\"https://github.com/tc39/ecma262/wiki/Editorial-Conventions\">Editorial Conventions</a>? https://github.com/tc39/ecma262/pull/3832#discussion_r3172092825 highlights some gaps, specifically about how to compare an [extended] mathematical value or bigint against multiple options (<em>which I think should be <code>If _x_ = $a or _x_ = $b</code> when there are two options, and presumably <code>If _x_ = $a or _x_ = $b or _x_ = …</code> when there are more</em>). So I think I'd like the first few bullets of that section to look like</p>\n<blockquote>\n<ul>\n<li>when comparing an alias against a single value or multiple options, the alias should be on the left</li>\n<li>when comparing an alias for equality or inequality against a single value, the preferred form depends upon the type of that value:\n<ul>\n<li><code>_x_ = …</code> or <code>_x_ ≠ …</code> for an extended mathematical value or bigint</li>\n<li><code>SameValue(_x_, …)</code> for an object, non-well-known symbol, or arbitrary language value</li>\n<li><code>_x_ is …</code> or <code>_x_ is not …</code> for anything else (boolean, number, string, well-known symbol, null, undefined, spec enum, etc.)</li>\n</ul>\n</li>\n<li>when comparing an alias for equality or inequality against multiple options, preserve explicit pairwise <code>=</code>/<code>≠</code>/<code>SameValue</code> (<code>_x_ = _option1_ or _x_ = _option2_ or _x_ = …</code>) but consolidate <code>is</code>/<code>is not</code> using <code>either…or</code>/<code>neither…nor</code> for a pair of options (<code>_x_ is neither _option1_ nor _option2_</code>) and <code>one of</code> for more than that (<code>_x_ is not of of _option1_, _option2_, or _option3_</code>)</li>\n<li>an alias can also be compared against a number, bigint, or extended mathematical value using <code>&lt;</code>, <code>≤</code>, <code>&gt;</code>, or <code>≥</code></li>\n</ul>\n</blockquote>\n<p>we should talk about migrating Editorial Conventions into the spec repository itself, but in the meantime I'd just like to clarify the above guidance but don't want to impose excessive churn by just unilaterally editing the GitHub wiki page</p>\n","m.mentions":{},"msgtype":"m.text"},"m.relates_to":{"event_id":"$8IRtC6sfDL74R5JdhVadYEKIJw8AZoMPEizAA0W1ulI","rel_type":"m.replace"},"msgtype":"m.text"},"ts":1777650077250,"senderName":"Richard Gibson","senderId":"@gibson042:matrix.org","id":"$ipPseB63ZDSHkha_uTO-IRjmnuvwgIkZbMCHC9nMM9E"},
{"content":{"body":"we usually just get agreement in an editor call and then make the change","m.mentions":{},"msgtype":"m.text"},"ts":1777651318148,"senderName":"Michael Ficarra","senderId":"@michaelficarra:matrix.org","id":"$cnV4x5-9XHUpc_IR32le1KV4JyIj5GYYuwYK7jZau_Q"},
{"content":{"body":"then I guess I should open an issue on the repository so there's something to reference and comment on, right?","m.mentions":{},"msgtype":"m.text"},"ts":1777651401975,"senderName":"Richard Gibson","senderId":"@gibson042:matrix.org","id":"$jynGiiHcgsZjKQEORCdUQqdUXmsLAHuKatkwGQpYKzQ"}
]