[
{"content":{"body":"I don't much care about https://github.com/tc39/ecmarkup/pull/694/changes#r3530802437 , but I'd like to resolve it quickly so we can merge the PR and cut an ecmarkup release. So, who would like to weigh in on how `setMultipagePreference` ought to be wired to `#shortcuts-help`?\n1. in multipage.js, as a listener on `document` with runtime filtering\n2. in multipage.js, as a listener on `#shortcuts-help` that is dynamically added either immediately if that container already exists or in a DOMContentLoaded callback otherwise\n3. in Spec.ts as an inline script element accompanying `#shortcuts-help`","format":"org.matrix.custom.html","formatted_body":"<p>I don't much care about https://github.com/tc39/ecmarkup/pull/694/changes#r3530802437 , but I'd like to resolve it quickly so we can merge the PR and cut an ecmarkup release. So, who would like to weigh in on how <code>setMultipagePreference</code> ought to be wired to <code>#shortcuts-help</code>?</p>\n<ol>\n<li>in multipage.js, as a listener on <code>document</code> with runtime filtering</li>\n<li>in multipage.js, as a listener on <code>#shortcuts-help</code> that is dynamically added either immediately if that container already exists or in a DOMContentLoaded callback otherwise</li>\n<li>in Spec.ts as an inline script element accompanying <code>#shortcuts-help</code></li>\n</ol>\n","m.mentions":{},"msgtype":"m.text"},"ts":1783370671795,"senderName":"Richard Gibson","senderId":"@gibson042:matrix.org","id":"$yOQMGgEm3FwHir_qWTAWi1mUSyETlmVmEGiDowbCwlI"},
{"content":{"body":"* I don't much care about https://github.com/tc39/ecmarkup/pull/694/changes#r3530802437 , but I'd like to resolve it quickly so we can merge the PR and cut an ecmarkup release. So, who would like to weigh in on how `setMultipagePreference` ought to be wired to `#shortcuts-help`?\n\n1. in multipage.js, as a listener on `document` with runtime filtering\n2. in multipage.js, as a listener on `#shortcuts-help` that is dynamically added immediately if that container already exists or otherwise in a DOMContentLoaded callback\n3. in Spec.ts as an inline script element accompanying `#shortcuts-help`","format":"org.matrix.custom.html","formatted_body":"* <p>I don't much care about https://github.com/tc39/ecmarkup/pull/694/changes#r3530802437 , but I'd like to resolve it quickly so we can merge the PR and cut an ecmarkup release. So, who would like to weigh in on how <code>setMultipagePreference</code> ought to be wired to <code>#shortcuts-help</code>?</p>\n<ol>\n<li>in multipage.js, as a listener on <code>document</code> with runtime filtering</li>\n<li>in multipage.js, as a listener on <code>#shortcuts-help</code> that is dynamically added immediately if that container already exists or otherwise in a DOMContentLoaded callback</li>\n<li>in Spec.ts as an inline script element accompanying <code>#shortcuts-help</code></li>\n</ol>\n","m.mentions":{},"m.new_content":{"body":"I don't much care about https://github.com/tc39/ecmarkup/pull/694/changes#r3530802437 , but I'd like to resolve it quickly so we can merge the PR and cut an ecmarkup release. So, who would like to weigh in on how `setMultipagePreference` ought to be wired to `#shortcuts-help`?\n\n1. in multipage.js, as a listener on `document` with runtime filtering\n2. in multipage.js, as a listener on `#shortcuts-help` that is dynamically added immediately if that container already exists or otherwise in a DOMContentLoaded callback\n3. in Spec.ts as an inline script element accompanying `#shortcuts-help`","format":"org.matrix.custom.html","formatted_body":"<p>I don't much care about https://github.com/tc39/ecmarkup/pull/694/changes#r3530802437 , but I'd like to resolve it quickly so we can merge the PR and cut an ecmarkup release. So, who would like to weigh in on how <code>setMultipagePreference</code> ought to be wired to <code>#shortcuts-help</code>?</p>\n<ol>\n<li>in multipage.js, as a listener on <code>document</code> with runtime filtering</li>\n<li>in multipage.js, as a listener on <code>#shortcuts-help</code> that is dynamically added immediately if that container already exists or otherwise in a DOMContentLoaded callback</li>\n<li>in Spec.ts as an inline script element accompanying <code>#shortcuts-help</code></li>\n</ol>\n","m.mentions":{},"msgtype":"m.text"},"m.relates_to":{"event_id":"$yOQMGgEm3FwHir_qWTAWi1mUSyETlmVmEGiDowbCwlI","rel_type":"m.replace"},"msgtype":"m.text"},"ts":1783370728155,"senderName":"Richard Gibson","senderId":"@gibson042:matrix.org","id":"$9jYewPkP1XHGZhEF54q3YO8Uey-kwJFiMJvdwxTERd8"},
{"content":{"body":"* I don't much care about https://github.com/tc39/ecmarkup/pull/694/changes#r3530802437 , but I'd like to resolve it quickly so we can merge the PR and cut an ecmarkup release. So, who would like to weigh in on how `setMultipagePreference` ought to be wired to `#shortcuts-help`?\n\n1. in multipage.js, as a listener on `document` with runtime filtering\n2. in multipage.js, as a listener on `#shortcuts-help` that is dynamically added immediately if that container already exists or otherwise in a DOMContentLoaded callback\n3. in Spec.ts, as an inline script element accompanying `#shortcuts-help`","format":"org.matrix.custom.html","formatted_body":"* <p>I don't much care about https://github.com/tc39/ecmarkup/pull/694/changes#r3530802437 , but I'd like to resolve it quickly so we can merge the PR and cut an ecmarkup release. So, who would like to weigh in on how <code>setMultipagePreference</code> ought to be wired to <code>#shortcuts-help</code>?</p>\n<ol>\n<li>in multipage.js, as a listener on <code>document</code> with runtime filtering</li>\n<li>in multipage.js, as a listener on <code>#shortcuts-help</code> that is dynamically added immediately if that container already exists or otherwise in a DOMContentLoaded callback</li>\n<li>in Spec.ts, as an inline script element accompanying <code>#shortcuts-help</code></li>\n</ol>\n","m.mentions":{},"m.new_content":{"body":"I don't much care about https://github.com/tc39/ecmarkup/pull/694/changes#r3530802437 , but I'd like to resolve it quickly so we can merge the PR and cut an ecmarkup release. So, who would like to weigh in on how `setMultipagePreference` ought to be wired to `#shortcuts-help`?\n\n1. in multipage.js, as a listener on `document` with runtime filtering\n2. in multipage.js, as a listener on `#shortcuts-help` that is dynamically added immediately if that container already exists or otherwise in a DOMContentLoaded callback\n3. in Spec.ts, as an inline script element accompanying `#shortcuts-help`","format":"org.matrix.custom.html","formatted_body":"<p>I don't much care about https://github.com/tc39/ecmarkup/pull/694/changes#r3530802437 , but I'd like to resolve it quickly so we can merge the PR and cut an ecmarkup release. So, who would like to weigh in on how <code>setMultipagePreference</code> ought to be wired to <code>#shortcuts-help</code>?</p>\n<ol>\n<li>in multipage.js, as a listener on <code>document</code> with runtime filtering</li>\n<li>in multipage.js, as a listener on <code>#shortcuts-help</code> that is dynamically added immediately if that container already exists or otherwise in a DOMContentLoaded callback</li>\n<li>in Spec.ts, as an inline script element accompanying <code>#shortcuts-help</code></li>\n</ol>\n","m.mentions":{},"msgtype":"m.text"},"m.relates_to":{"event_id":"$yOQMGgEm3FwHir_qWTAWi1mUSyETlmVmEGiDowbCwlI","rel_type":"m.replace"},"msgtype":"m.text"},"ts":1783370738265,"senderName":"Richard Gibson","senderId":"@gibson042:matrix.org","id":"$X9cechpnHEl4ibTAJ3UXg6XOpKNefxsZhHh313kd190"}
]