[
{"content":{"body":"I would also think so because applying mappings is associative:\n\n```\nbundle.js -(a)-> foo.min.js -(b)-> foo.js -(c)-> foo.ts\n```\n\nWhere a, b and c are source maps. Then applying a mapping for an arbitrary location would look something like:\n\n```\nmap(a, pos_bundle) = pos_min\nmap(b, pos_min) = pos_js\nmap(c, pos_js) = pos_ts\n\nand a composed map: map(xy, pos) = map(x, map(y, pos))\n```\n\nI'd think that the following should hold?\n\n```\nmap(c, map(ab, pos_bundle) = map(bc, map(a, pos_bundle))\n```","format":"org.matrix.custom.html","formatted_body":"<p>I would also think so because applying mappings is associative:</p>\n<pre><code>bundle.js -(a)-&gt; foo.min.js -(b)-&gt; foo.js -(c)-&gt; foo.ts\n</code></pre>\n<p>Where a, b and c are source maps. Then applying a mapping for an arbitrary location would look something like:</p>\n<pre><code>map(a, pos_bundle) = pos_min\nmap(b, pos_min) = pos_js\nmap(c, pos_js) = pos_ts\n\nand a composed map: map(xy, pos) = map(x, map(y, pos))\n</code></pre>\n<p>I'd think that the following should hold?</p>\n<pre><code>map(c, map(ab, pos_bundle) = map(bc, map(a, pos_bundle))\n</code></pre>\n","m.mentions":{},"msgtype":"m.text"},"ts":1765434657125,"senderName":"Simon Zünd","senderId":"@szuend:matrix.org","id":"$WTAUBEVzmYuyRLa_VPdiWKX_fFBGxV1AXreKEsWLKYE"},
{"content":{"body":"* I would also think so because applying mappings is associative:\n\n```\nbundle.js -(a)-> foo.min.js -(b)-> foo.js -(c)-> foo.ts\n```\n\nWhere a, b and c are source maps. Then applying a mapping for an arbitrary location would look something like:\n\n```\nmap(a, pos_bundle) = pos_min\nmap(b, pos_min) = pos_js\nmap(c, pos_js) = pos_ts\n\nand a composed map: map(xy, pos) = map(y, map(x, pos))\n```\n\nI'd think that the following should hold?\n\n```\nmap(c, map(ab, pos_bundle) = map(bc, map(a, pos_bundle))\n```","format":"org.matrix.custom.html","formatted_body":"* <p>I would also think so because applying mappings is associative:</p>\n<pre><code>bundle.js -(a)-&gt; foo.min.js -(b)-&gt; foo.js -(c)-&gt; foo.ts\n</code></pre>\n<p>Where a, b and c are source maps. Then applying a mapping for an arbitrary location would look something like:</p>\n<pre><code>map(a, pos_bundle) = pos_min\nmap(b, pos_min) = pos_js\nmap(c, pos_js) = pos_ts\n\nand a composed map: map(xy, pos) = map(y, map(x, pos))\n</code></pre>\n<p>I'd think that the following should hold?</p>\n<pre><code>map(c, map(ab, pos_bundle) = map(bc, map(a, pos_bundle))\n</code></pre>\n","m.mentions":{},"m.new_content":{"body":"I would also think so because applying mappings is associative:\n\n```\nbundle.js -(a)-> foo.min.js -(b)-> foo.js -(c)-> foo.ts\n```\n\nWhere a, b and c are source maps. Then applying a mapping for an arbitrary location would look something like:\n\n```\nmap(a, pos_bundle) = pos_min\nmap(b, pos_min) = pos_js\nmap(c, pos_js) = pos_ts\n\nand a composed map: map(xy, pos) = map(y, map(x, pos))\n```\n\nI'd think that the following should hold?\n\n```\nmap(c, map(ab, pos_bundle) = map(bc, map(a, pos_bundle))\n```","format":"org.matrix.custom.html","formatted_body":"<p>I would also think so because applying mappings is associative:</p>\n<pre><code>bundle.js -(a)-&gt; foo.min.js -(b)-&gt; foo.js -(c)-&gt; foo.ts\n</code></pre>\n<p>Where a, b and c are source maps. Then applying a mapping for an arbitrary location would look something like:</p>\n<pre><code>map(a, pos_bundle) = pos_min\nmap(b, pos_min) = pos_js\nmap(c, pos_js) = pos_ts\n\nand a composed map: map(xy, pos) = map(y, map(x, pos))\n</code></pre>\n<p>I'd think that the following should hold?</p>\n<pre><code>map(c, map(ab, pos_bundle) = map(bc, map(a, pos_bundle))\n</code></pre>\n","m.mentions":{},"msgtype":"m.text"},"m.relates_to":{"event_id":"$WTAUBEVzmYuyRLa_VPdiWKX_fFBGxV1AXreKEsWLKYE","rel_type":"m.replace"},"msgtype":"m.text"},"ts":1765436894318,"senderName":"Simon Zünd","senderId":"@szuend:matrix.org","id":"$0D9CFCZwdmnxyz2VjHDS5nr8KWJswdwpLfNw88eMrDk"},
{"content":{"body":"It seems a desirable invariant. How about we just assert it (in the spec, or whatever authoritative docs we have), test for it, and then see if any awkward counter-examples come up? ","msgtype":"m.text"},"ts":1765457645813,"senderName":"Rob Palmer","senderId":"@robpalme:matrix.org","id":"$w4i0zqYia4PW-HmcMatF0x7eqoL2zCOm4fokV-MkAd8"}
]