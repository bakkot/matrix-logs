[
{"content":{"body":"I think I've asked this before, but when do we use `abort these steps` vs `return`? I tend to use `abort these steps` where returning a value wouldn't make sense, eg in a queued task, or in parallel. Is that… right?","format":"org.matrix.custom.html","formatted_body":"I think I've asked this before, but when do we use <code>abort these steps</code> vs <code>return</code>? I tend to use <code>abort these steps</code> where returning a value wouldn't make sense, eg in a queued task, or in parallel. Is that… right?","msgtype":"m.text","org.matrix.msc1767.message":[{"body":"I think I've asked this before, but when do we use `abort these steps` vs `return`? I tend to use `abort these steps` where returning a value wouldn't make sense, eg in a queued task, or in parallel. Is that… right?","mimetype":"text/plain"},{"body":"I think I've asked this before, but when do we use <code>abort these steps</code> vs <code>return</code>? I tend to use <code>abort these steps</code> where returning a value wouldn't make sense, eg in a queued task, or in parallel. Is that… right?","mimetype":"text/html"}]},"ts":1659596652735,"senderName":"Jake Archibald","senderId":"@jakea:matrix.org","id":"$70ZASvebuGmK6VBtLrePB5g6wXd1XelW9sKrx8UZw6I"}
]