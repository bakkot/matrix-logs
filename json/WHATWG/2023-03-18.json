[
{"content":{"body":"asuth: zcorpan: if Gecko has any feedback on https://github.com/whatwg/fs/pull/96 Monday would be a good time to chime in, Austin and I are pretty close to calling it done","format":"org.matrix.custom.html","formatted_body":"<a href=\"https://matrix.to/#/@asuth:mozilla.org\">asuth</a>: <a href=\"https://matrix.to/#/@zcorpan:mozilla.org\">zcorpan</a>: if Gecko has any feedback on https://github.com/whatwg/fs/pull/96 Monday would be a good time to chime in, Austin and I are pretty close to calling it done","msgtype":"m.text"},"ts":1679125005180,"senderName":"annevk","senderId":"@annevk:matrix.org","id":"$FNXrz-Ryu1zhliXE709X5f01VAB2wsv96NKzXyc_8sI"},
{"content":{"body":"The Fetch standard [states](https://fetch.spec.whatwg.org/#credentials) the following\n\n> Credentials are HTTP cookies, TLS client certificates, and authentication entries (for HTTP authentication).\n\nand it also [defines](https://fetch.spec.whatwg.org/#authentication-entry) authentication entries as follows:\n\n> An authentication entry and a proxy-authentication entry are tuples of username, password, and realm, used for HTTP authentication and HTTP proxy authentication, and associated with one or more requests.\n\nHowever, unless I'm missing something, not all authentication entries count as credentials in the sense used by the Fetch standard. For instance, `Basic Zm9vOmJhcgo=` does count as credential and, if present, will be automatically attached by the browser to credentialed requests issued by the client; but `Bearer whatever` doesn't count as credential in that sense.\n\nIs the Fetch standard accurate in that respect? What am I missing? And if only some authentication entries count as credentials, is there some authoritative list of them?","format":"org.matrix.custom.html","formatted_body":"<p>The Fetch standard <a href=\"https://fetch.spec.whatwg.org/#credentials\">states</a> the following</p>\n<blockquote>\n<p>Credentials are HTTP cookies, TLS client certificates, and authentication entries (for HTTP authentication).</p>\n</blockquote>\n<p>and it also <a href=\"https://fetch.spec.whatwg.org/#authentication-entry\">defines</a> authentication entries as follows:</p>\n<blockquote>\n<p>An authentication entry and a proxy-authentication entry are tuples of username, password, and realm, used for HTTP authentication and HTTP proxy authentication, and associated with one or more requests.</p>\n</blockquote>\n<p>However, unless I'm missing something, not all authentication entries count as credentials in the sense used by the Fetch standard. For instance, <code>Basic Zm9vOmJhcgo=</code> does count as credential and, if present, will be automatically attached by the browser to credentialed requests issued by the client; but <code>Bearer whatever</code> doesn't count as credential in that sense.</p>\n<p>Is the Fetch standard accurate in that respect? What am I missing? And if only some authentication entries count as credentials, is there some authoritative list of them?</p>\n","msgtype":"m.text"},"ts":1679142535624,"senderName":"jub0bs","senderId":"@jub0bs:matrix.org","id":"$LyomzZl1e5ampRAtQnLAW3S_3O9RC5KA3ftLBADDA-Y"},
{"content":{"body":" * The Fetch standard [states](https://fetch.spec.whatwg.org/#credentials) the following\n\n> Credentials are HTTP cookies, TLS client certificates, and authentication entries (for HTTP authentication).\n\nand it also [defines](https://fetch.spec.whatwg.org/#authentication-entry) authentication entries as follows:\n\n> An authentication entry and a proxy-authentication entry are tuples of username, password, and realm, used for HTTP authentication and HTTP proxy authentication, and associated with one or more requests.\n\nHowever, unless I'm missing something, not all authentication entries count as credentials in the sense used by the Fetch standard. For instance, `Basic Zm9vOmJhcgo=` does count as credential and, if present, will be automatically attached by the browser to _credentialed_ requests issued by the client; but `Bearer whatever` doesn't count as credential in that sense.\n\nIs the Fetch standard accurate in that respect? What am I missing? And if only some authentication entries count as credentials, is there some authoritative list of them?","format":"org.matrix.custom.html","formatted_body":" * <p>The Fetch standard <a href=\"https://fetch.spec.whatwg.org/#credentials\">states</a> the following</p>\n<blockquote>\n<p>Credentials are HTTP cookies, TLS client certificates, and authentication entries (for HTTP authentication).</p>\n</blockquote>\n<p>and it also <a href=\"https://fetch.spec.whatwg.org/#authentication-entry\">defines</a> authentication entries as follows:</p>\n<blockquote>\n<p>An authentication entry and a proxy-authentication entry are tuples of username, password, and realm, used for HTTP authentication and HTTP proxy authentication, and associated with one or more requests.</p>\n</blockquote>\n<p>However, unless I'm missing something, not all authentication entries count as credentials in the sense used by the Fetch standard. For instance, <code>Basic Zm9vOmJhcgo=</code> does count as credential and, if present, will be automatically attached by the browser to <em>credentialed</em> requests issued by the client; but <code>Bearer whatever</code> doesn't count as credential in that sense.</p>\n<p>Is the Fetch standard accurate in that respect? What am I missing? And if only some authentication entries count as credentials, is there some authoritative list of them?</p>\n","m.new_content":{"body":"The Fetch standard [states](https://fetch.spec.whatwg.org/#credentials) the following\n\n> Credentials are HTTP cookies, TLS client certificates, and authentication entries (for HTTP authentication).\n\nand it also [defines](https://fetch.spec.whatwg.org/#authentication-entry) authentication entries as follows:\n\n> An authentication entry and a proxy-authentication entry are tuples of username, password, and realm, used for HTTP authentication and HTTP proxy authentication, and associated with one or more requests.\n\nHowever, unless I'm missing something, not all authentication entries count as credentials in the sense used by the Fetch standard. For instance, `Basic Zm9vOmJhcgo=` does count as credential and, if present, will be automatically attached by the browser to _credentialed_ requests issued by the client; but `Bearer whatever` doesn't count as credential in that sense.\n\nIs the Fetch standard accurate in that respect? What am I missing? And if only some authentication entries count as credentials, is there some authoritative list of them?","format":"org.matrix.custom.html","formatted_body":"<p>The Fetch standard <a href=\"https://fetch.spec.whatwg.org/#credentials\">states</a> the following</p>\n<blockquote>\n<p>Credentials are HTTP cookies, TLS client certificates, and authentication entries (for HTTP authentication).</p>\n</blockquote>\n<p>and it also <a href=\"https://fetch.spec.whatwg.org/#authentication-entry\">defines</a> authentication entries as follows:</p>\n<blockquote>\n<p>An authentication entry and a proxy-authentication entry are tuples of username, password, and realm, used for HTTP authentication and HTTP proxy authentication, and associated with one or more requests.</p>\n</blockquote>\n<p>However, unless I'm missing something, not all authentication entries count as credentials in the sense used by the Fetch standard. For instance, <code>Basic Zm9vOmJhcgo=</code> does count as credential and, if present, will be automatically attached by the browser to <em>credentialed</em> requests issued by the client; but <code>Bearer whatever</code> doesn't count as credential in that sense.</p>\n<p>Is the Fetch standard accurate in that respect? What am I missing? And if only some authentication entries count as credentials, is there some authoritative list of them?</p>\n","msgtype":"m.text"},"m.relates_to":{"event_id":"$LyomzZl1e5ampRAtQnLAW3S_3O9RC5KA3ftLBADDA-Y","rel_type":"m.replace"},"msgtype":"m.text"},"ts":1679143001919,"senderName":"jub0bs","senderId":"@jub0bs:matrix.org","id":"$YDEE5YR_VFrmnygUrwseVIGv9_GMznJ3Vj-URHy5uMk"},
{"content":{"body":" * The Fetch standard [states](https://fetch.spec.whatwg.org/#credentials) the following\n\n> Credentials are HTTP cookies, TLS client certificates, and authentication entries (for HTTP authentication).\n\nand it also [defines](https://fetch.spec.whatwg.org/#authentication-entry) authentication entries as follows:\n\n> An authentication entry and a proxy-authentication entry are tuples of username, password, and realm, used for HTTP authentication and HTTP proxy authentication, and associated with one or more requests.\n\nHowever, unless I'm missing something, not all authentication entries count as credentials in the sense used by CORS. For instance, `Basic Zm9vOmJhcgo=` does count as credential and, if present, will be automatically attached by the browser to _credentialed_ requests issued by the client; but `Bearer whatever` doesn't count as credential in that sense.\n\nIs the Fetch standard accurate in that respect? What am I missing? And if only some authentication entries count as credentials, is there some authoritative list of them?","format":"org.matrix.custom.html","formatted_body":" * <p>The Fetch standard <a href=\"https://fetch.spec.whatwg.org/#credentials\">states</a> the following</p>\n<blockquote>\n<p>Credentials are HTTP cookies, TLS client certificates, and authentication entries (for HTTP authentication).</p>\n</blockquote>\n<p>and it also <a href=\"https://fetch.spec.whatwg.org/#authentication-entry\">defines</a> authentication entries as follows:</p>\n<blockquote>\n<p>An authentication entry and a proxy-authentication entry are tuples of username, password, and realm, used for HTTP authentication and HTTP proxy authentication, and associated with one or more requests.</p>\n</blockquote>\n<p>However, unless I'm missing something, not all authentication entries count as credentials in the sense used by CORS. For instance, <code>Basic Zm9vOmJhcgo=</code> does count as credential and, if present, will be automatically attached by the browser to <em>credentialed</em> requests issued by the client; but <code>Bearer whatever</code> doesn't count as credential in that sense.</p>\n<p>Is the Fetch standard accurate in that respect? What am I missing? And if only some authentication entries count as credentials, is there some authoritative list of them?</p>\n","m.new_content":{"body":"The Fetch standard [states](https://fetch.spec.whatwg.org/#credentials) the following\n\n> Credentials are HTTP cookies, TLS client certificates, and authentication entries (for HTTP authentication).\n\nand it also [defines](https://fetch.spec.whatwg.org/#authentication-entry) authentication entries as follows:\n\n> An authentication entry and a proxy-authentication entry are tuples of username, password, and realm, used for HTTP authentication and HTTP proxy authentication, and associated with one or more requests.\n\nHowever, unless I'm missing something, not all authentication entries count as credentials in the sense used by CORS. For instance, `Basic Zm9vOmJhcgo=` does count as credential and, if present, will be automatically attached by the browser to _credentialed_ requests issued by the client; but `Bearer whatever` doesn't count as credential in that sense.\n\nIs the Fetch standard accurate in that respect? What am I missing? And if only some authentication entries count as credentials, is there some authoritative list of them?","format":"org.matrix.custom.html","formatted_body":"<p>The Fetch standard <a href=\"https://fetch.spec.whatwg.org/#credentials\">states</a> the following</p>\n<blockquote>\n<p>Credentials are HTTP cookies, TLS client certificates, and authentication entries (for HTTP authentication).</p>\n</blockquote>\n<p>and it also <a href=\"https://fetch.spec.whatwg.org/#authentication-entry\">defines</a> authentication entries as follows:</p>\n<blockquote>\n<p>An authentication entry and a proxy-authentication entry are tuples of username, password, and realm, used for HTTP authentication and HTTP proxy authentication, and associated with one or more requests.</p>\n</blockquote>\n<p>However, unless I'm missing something, not all authentication entries count as credentials in the sense used by CORS. For instance, <code>Basic Zm9vOmJhcgo=</code> does count as credential and, if present, will be automatically attached by the browser to <em>credentialed</em> requests issued by the client; but <code>Bearer whatever</code> doesn't count as credential in that sense.</p>\n<p>Is the Fetch standard accurate in that respect? What am I missing? And if only some authentication entries count as credentials, is there some authoritative list of them?</p>\n","msgtype":"m.text"},"m.relates_to":{"event_id":"$LyomzZl1e5ampRAtQnLAW3S_3O9RC5KA3ftLBADDA-Y","rel_type":"m.replace"},"msgtype":"m.text"},"ts":1679151173504,"senderName":"jub0bs","senderId":"@jub0bs:matrix.org","id":"$8LboixARMVwYzkTCKC9WNODgCYkBM7FneF69d7b72PY"},
{"content":{"body":"jub0bs: I'd support a PR renaming \"credentials\" to \"user-agent-bound credentials\"","format":"org.matrix.custom.html","formatted_body":"<a href=\"https://matrix.to/#/@jub0bs:matrix.org\">jub0bs</a>: I'd support a PR renaming &quot;credentials&quot; to &quot;user-agent-bound credentials&quot;","msgtype":"m.text"},"ts":1679152020505,"senderName":"annevk","senderId":"@annevk:matrix.org","id":"$cL1TToehUm-edwx7Zk_TChDhVkTkfC4h0XjUmO1yJCk"},
{"content":{"body":"There are indeed many other forms of credentials, but they're not mediated by the user agent in some fashion","msgtype":"m.text"},"ts":1679152080510,"senderName":"annevk","senderId":"@annevk:matrix.org","id":"$1yinq8Y_7W71xD6y_6vXTOQUdjupH6HDSLFdLZGsLLw"},
{"content":{"body":"Thanks annevk. I do like the more precise term you suggest. But then my question becomes the following: Is there a standard definition of user-agent-bound credentials?","format":"org.matrix.custom.html","formatted_body":"Thanks <a href=\"https://matrix.to/#/@annevk:matrix.org\">annevk</a>. I do like the more precise term you suggest. But then my question becomes the following: Is there a standard definition of user-agent-bound credentials?","msgtype":"m.text"},"ts":1679156146700,"senderName":"jub0bs","senderId":"@jub0bs:matrix.org","id":"$CQ2TL8BpG4kyfvAXbaFVJH8RjPKtSkGehTCj2kiLNfw"},
{"content":{"body":"> <@annevk:matrix.org> asuth: zcorpan: if Gecko has any feedback on https://github.com/whatwg/fs/pull/96 Monday would be a good time to chime in, Austin and I are pretty close to calling it done\n\nThanks, I'll pass it along.","format":"org.matrix.custom.html","formatted_body":"<mx-reply><blockquote><a href=\"https://matrix.to/#/!AGetWbsMpFPdSgUrbs:matrix.org/$FNXrz-Ryu1zhliXE709X5f01VAB2wsv96NKzXyc_8sI?via=matrix.org&via=mozilla.org&via=igalia.com\">In reply to</a> <a href=\"https://matrix.to/#/@annevk:matrix.org\">@annevk:matrix.org</a><br><a href=\"https://matrix.to/#/@asuth:mozilla.org\">asuth</a>: <a href=\"https://matrix.to/#/@zcorpan:mozilla.org\">zcorpan</a>: if Gecko has any feedback on https://github.com/whatwg/fs/pull/96 Monday would be a good time to chime in, Austin and I are pretty close to calling it done</blockquote></mx-reply>Thanks, I'll pass it along.","m.relates_to":{"m.in_reply_to":{"event_id":"$FNXrz-Ryu1zhliXE709X5f01VAB2wsv96NKzXyc_8sI"}},"msgtype":"m.text"},"ts":1679157480712,"senderName":"asuth","senderId":"@asuth:mozilla.org","id":"$EeadTmHbn7dmF5A0YA6ncn9Q96XtVarNT1Ih6Ro0rPI"}
]