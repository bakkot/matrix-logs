[
{"content":{"body":"Hmm, why this lowercases charset: (new Response(new Blob([\"data\"]), {headers: [[\"Content-Type\", \"text/plain;charset=UTF-8\"]]})).blob().then((b)=> { console.log(b.type); });","msgtype":"m.text","org.matrix.msc1767.text":"Hmm, why this lowercases charset: (new Response(new Blob([\"data\"]), {headers: [[\"Content-Type\", \"text/plain;charset=UTF-8\"]]})).blob().then((b)=> { console.log(b.type); });"},"ts":1655933434316,"senderName":"smaug","senderId":"@smaug:mozilla.org","id":"$tBDiIQQ2pUTxUqX7rFPAX2N2vly9as16RZGL1PldiPU"},
{"content":{"body":"Blob constructor does convert type to lowercase, but fetch spec doesn't use that constructor ","msgtype":"m.text","org.matrix.msc1767.text":"Blob constructor does convert type to lowercase, but fetch spec doesn't use that constructor "},"ts":1655933520669,"senderName":"smaug","senderId":"@smaug:mozilla.org","id":"$65tUhWM_fLUnTtttJZJJgoRL4nLK2nIb9mxYWUVU9zo"},
{"content":{"body":"`blob.type` always returns a lowercase string:\n\n> On getting, user agents must return the type of a Blob as an ASCII-encoded string in lower case\n\nSee https://w3c.github.io/FileAPI/#attributes-blob","format":"org.matrix.custom.html","formatted_body":"<p><code>blob.type</code> always returns a lowercase string:</p>\n<blockquote>\n<p>On getting, user agents must return the type of a Blob as an ASCII-encoded string in lower case</p>\n</blockquote>\n<p>See https://w3c.github.io/FileAPI/#attributes-blob</p>\n","msgtype":"m.text","org.matrix.msc1767.message":[{"body":"`blob.type` always returns a lowercase string:\n\n> On getting, user agents must return the type of a Blob as an ASCII-encoded string in lower case\n\nSee https://w3c.github.io/FileAPI/#attributes-blob","mimetype":"text/plain"},{"body":"<p><code>blob.type</code> always returns a lowercase string:</p>\n<blockquote>\n<p>On getting, user agents must return the type of a Blob as an ASCII-encoded string in lower case</p>\n</blockquote>\n<p>See https://w3c.github.io/FileAPI/#attributes-blob</p>\n","mimetype":"text/html"}]},"ts":1655938730964,"senderName":"Luca Casonato","senderId":"@lucacasonato:matrix.org","id":"$o1yz5HlvhpgKJV74nzXtoL-FR08zWYTCHb5myfVpRP4"},
{"content":{"body":" * `blob.type` is specified to always returns a lowercase string:\n\n> On getting, user agents must return the type of a Blob as an ASCII-encoded string in lower case\n\nSee https://w3c.github.io/FileAPI/#attributes-blob","format":"org.matrix.custom.html","formatted_body":" * <p><code>blob.type</code> is specified to always returns a lowercase string:</p>\n<blockquote>\n<p>On getting, user agents must return the type of a Blob as an ASCII-encoded string in lower case</p>\n</blockquote>\n<p>See https://w3c.github.io/FileAPI/#attributes-blob</p>\n","m.new_content":{"body":"`blob.type` is specified to always returns a lowercase string:\n\n> On getting, user agents must return the type of a Blob as an ASCII-encoded string in lower case\n\nSee https://w3c.github.io/FileAPI/#attributes-blob","format":"org.matrix.custom.html","formatted_body":"<p><code>blob.type</code> is specified to always returns a lowercase string:</p>\n<blockquote>\n<p>On getting, user agents must return the type of a Blob as an ASCII-encoded string in lower case</p>\n</blockquote>\n<p>See https://w3c.github.io/FileAPI/#attributes-blob</p>\n","msgtype":"m.text","org.matrix.msc1767.message":[{"body":"`blob.type` is specified to always returns a lowercase string:\n\n> On getting, user agents must return the type of a Blob as an ASCII-encoded string in lower case\n\nSee https://w3c.github.io/FileAPI/#attributes-blob","mimetype":"text/plain"},{"body":"<p><code>blob.type</code> is specified to always returns a lowercase string:</p>\n<blockquote>\n<p>On getting, user agents must return the type of a Blob as an ASCII-encoded string in lower case</p>\n</blockquote>\n<p>See https://w3c.github.io/FileAPI/#attributes-blob</p>\n","mimetype":"text/html"}]},"m.relates_to":{"event_id":"$o1yz5HlvhpgKJV74nzXtoL-FR08zWYTCHb5myfVpRP4","rel_type":"m.replace"},"msgtype":"m.text","org.matrix.msc1767.message":[{"body":" * `blob.type` is specified to always returns a lowercase string:\n\n> On getting, user agents must return the type of a Blob as an ASCII-encoded string in lower case\n\nSee https://w3c.github.io/FileAPI/#attributes-blob","mimetype":"text/plain"},{"body":" * <p><code>blob.type</code> is specified to always returns a lowercase string:</p>\n<blockquote>\n<p>On getting, user agents must return the type of a Blob as an ASCII-encoded string in lower case</p>\n</blockquote>\n<p>See https://w3c.github.io/FileAPI/#attributes-blob</p>\n","mimetype":"text/html"}]},"ts":1655938833177,"senderName":"Luca Casonato","senderId":"@lucacasonato:matrix.org","id":"$uoggMRzmlu3NT39oaumPzJYs-1Hh021MvrBZG5e4oGk"}
]