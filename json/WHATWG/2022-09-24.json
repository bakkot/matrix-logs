[
{"content":{"body":"About the `SpeechRecognition` (`webkitSpeechRecognition`) part of the Web Speech API: As far as I can tell, there’s no way to programatically determine what languages the browser recognizes/supports for speech input/recognition?\n\nIn comparison, for the TTS part of the API, I know I can do `window.speechSynthesis.getVoices()` to get an array of the supported voices/languages — but there’s no similar method or property for `SpeechRecognition`.\n\nBut I wonder if there might be some browser-specific ways to get the supported languages back from Chrome and Safari.","format":"org.matrix.custom.html","formatted_body":"<p>About the <code>SpeechRecognition</code> (<code>webkitSpeechRecognition</code>) part of the Web Speech API: As far as I can tell, there’s no way to programatically determine what languages the browser recognizes/supports for speech input/recognition?</p>\n<p>In comparison, for the TTS part of the API, I know I can do <code>window.speechSynthesis.getVoices()</code> to get an array of the supported voices/languages — but there’s no similar method or property for <code>SpeechRecognition</code>.</p>\n<p>But I wonder if there might be some browser-specific ways to get the supported languages back from Chrome and Safari.</p>\n","msgtype":"m.text","org.matrix.msc1767.message":[{"body":"About the `SpeechRecognition` (`webkitSpeechRecognition`) part of the Web Speech API: As far as I can tell, there’s no way to programatically determine what languages the browser recognizes/supports for speech input/recognition?\n\nIn comparison, for the TTS part of the API, I know I can do `window.speechSynthesis.getVoices()` to get an array of the supported voices/languages — but there’s no similar method or property for `SpeechRecognition`.\n\nBut I wonder if there might be some browser-specific ways to get the supported languages back from Chrome and Safari.","mimetype":"text/plain"},{"body":"<p>About the <code>SpeechRecognition</code> (<code>webkitSpeechRecognition</code>) part of the Web Speech API: As far as I can tell, there’s no way to programatically determine what languages the browser recognizes/supports for speech input/recognition?</p>\n<p>In comparison, for the TTS part of the API, I know I can do <code>window.speechSynthesis.getVoices()</code> to get an array of the supported voices/languages — but there’s no similar method or property for <code>SpeechRecognition</code>.</p>\n<p>But I wonder if there might be some browser-specific ways to get the supported languages back from Chrome and Safari.</p>\n","mimetype":"text/html"}]},"ts":1663997774595,"senderName":"sideshowbarker","senderId":"@sideshowbarker:matrix.org","id":"$RPi3oJKKhlINtXGSL1BoYcXjROct92WLGmt5p4jq8jA"},
{"content":{"body":"I’m also wondering exactly how the speech recognition part of the API works in Chrome and Safari. I’ve gleaned that in Chrome it seems to rely on the browser calling out to some remote Google speech-recognition service. But in the Safari case it seems to be using some built-in macOS speech-recognition platform feature. But… what exactly?\n\nAnd does Edge rely on the same remote Google speech-recognition service that Chrome does?","msgtype":"m.text","org.matrix.msc1767.text":"I’m also wondering exactly how the speech recognition part of the API works in Chrome and Safari. I’ve gleaned that in Chrome it seems to rely on the browser calling out to some remote Google speech-recognition service. But in the Safari case it seems to be using some built-in macOS speech-recognition platform feature. But… what exactly?\n\nAnd does Edge rely on the same remote Google speech-recognition service that Chrome does?"},"ts":1663997992446,"senderName":"sideshowbarker","senderId":"@sideshowbarker:matrix.org","id":"$bMXD0HmiYu79zpXqW25w0OM1bcJO7lnsyYBMHR6c3fY"},
{"content":{"body":"Asking in the context of trying to provide some better documentation in MDN about how `SpeechRecognition` actually works.","format":"org.matrix.custom.html","formatted_body":"Asking in the context of trying to provide some better documentation in MDN about how <code>SpeechRecognition</code> actually works.","msgtype":"m.text","org.matrix.msc1767.message":[{"body":"Asking in the context of trying to provide some better documentation in MDN about how `SpeechRecognition` actually works.","mimetype":"text/plain"},{"body":"Asking in the context of trying to provide some better documentation in MDN about how <code>SpeechRecognition</code> actually works.","mimetype":"text/html"}]},"ts":1663998036465,"senderName":"sideshowbarker","senderId":"@sideshowbarker:matrix.org","id":"$O5mT88sapWiATOp9y_yrO0vZBKEYI1bEnGwiHIBLMVE"},
{"content":{"body":" * About the `SpeechRecognition` (`webkitSpeechRecognition`) part of the Web Speech API: As far as I can tell, there’s no way to programatically determine what languages the browser recognizes/supports for speech input/recognition, right?\n\nIn comparison, for the TTS part of the API, I know I can do `window.speechSynthesis.getVoices()` to get an array of the supported voices/languages — but there’s no similar method or property for `SpeechRecognition`.\n\nBut I wonder if there might be some browser-specific ways to get the supported languages back from Chrome and Safari.","format":"org.matrix.custom.html","formatted_body":" * <p>About the <code>SpeechRecognition</code> (<code>webkitSpeechRecognition</code>) part of the Web Speech API: As far as I can tell, there’s no way to programatically determine what languages the browser recognizes/supports for speech input/recognition, right?</p>\n<p>In comparison, for the TTS part of the API, I know I can do <code>window.speechSynthesis.getVoices()</code> to get an array of the supported voices/languages — but there’s no similar method or property for <code>SpeechRecognition</code>.</p>\n<p>But I wonder if there might be some browser-specific ways to get the supported languages back from Chrome and Safari.</p>\n","m.new_content":{"body":"About the `SpeechRecognition` (`webkitSpeechRecognition`) part of the Web Speech API: As far as I can tell, there’s no way to programatically determine what languages the browser recognizes/supports for speech input/recognition, right?\n\nIn comparison, for the TTS part of the API, I know I can do `window.speechSynthesis.getVoices()` to get an array of the supported voices/languages — but there’s no similar method or property for `SpeechRecognition`.\n\nBut I wonder if there might be some browser-specific ways to get the supported languages back from Chrome and Safari.","format":"org.matrix.custom.html","formatted_body":"<p>About the <code>SpeechRecognition</code> (<code>webkitSpeechRecognition</code>) part of the Web Speech API: As far as I can tell, there’s no way to programatically determine what languages the browser recognizes/supports for speech input/recognition, right?</p>\n<p>In comparison, for the TTS part of the API, I know I can do <code>window.speechSynthesis.getVoices()</code> to get an array of the supported voices/languages — but there’s no similar method or property for <code>SpeechRecognition</code>.</p>\n<p>But I wonder if there might be some browser-specific ways to get the supported languages back from Chrome and Safari.</p>\n","msgtype":"m.text","org.matrix.msc1767.message":[{"body":"About the `SpeechRecognition` (`webkitSpeechRecognition`) part of the Web Speech API: As far as I can tell, there’s no way to programatically determine what languages the browser recognizes/supports for speech input/recognition, right?\n\nIn comparison, for the TTS part of the API, I know I can do `window.speechSynthesis.getVoices()` to get an array of the supported voices/languages — but there’s no similar method or property for `SpeechRecognition`.\n\nBut I wonder if there might be some browser-specific ways to get the supported languages back from Chrome and Safari.","mimetype":"text/plain"},{"body":"<p>About the <code>SpeechRecognition</code> (<code>webkitSpeechRecognition</code>) part of the Web Speech API: As far as I can tell, there’s no way to programatically determine what languages the browser recognizes/supports for speech input/recognition, right?</p>\n<p>In comparison, for the TTS part of the API, I know I can do <code>window.speechSynthesis.getVoices()</code> to get an array of the supported voices/languages — but there’s no similar method or property for <code>SpeechRecognition</code>.</p>\n<p>But I wonder if there might be some browser-specific ways to get the supported languages back from Chrome and Safari.</p>\n","mimetype":"text/html"}]},"m.relates_to":{"event_id":"$RPi3oJKKhlINtXGSL1BoYcXjROct92WLGmt5p4jq8jA","rel_type":"m.replace"},"msgtype":"m.text","org.matrix.msc1767.message":[{"body":" * About the `SpeechRecognition` (`webkitSpeechRecognition`) part of the Web Speech API: As far as I can tell, there’s no way to programatically determine what languages the browser recognizes/supports for speech input/recognition, right?\n\nIn comparison, for the TTS part of the API, I know I can do `window.speechSynthesis.getVoices()` to get an array of the supported voices/languages — but there’s no similar method or property for `SpeechRecognition`.\n\nBut I wonder if there might be some browser-specific ways to get the supported languages back from Chrome and Safari.","mimetype":"text/plain"},{"body":" * <p>About the <code>SpeechRecognition</code> (<code>webkitSpeechRecognition</code>) part of the Web Speech API: As far as I can tell, there’s no way to programatically determine what languages the browser recognizes/supports for speech input/recognition, right?</p>\n<p>In comparison, for the TTS part of the API, I know I can do <code>window.speechSynthesis.getVoices()</code> to get an array of the supported voices/languages — but there’s no similar method or property for <code>SpeechRecognition</code>.</p>\n<p>But I wonder if there might be some browser-specific ways to get the supported languages back from Chrome and Safari.</p>\n","mimetype":"text/html"}]},"ts":1663998058265,"senderName":"sideshowbarker","senderId":"@sideshowbarker:matrix.org","id":"$YX8pD8MJr9IiiGq8Ko3RrPLcTEsRtZhflVa5xw3NqpA"},
{"content":{"body":"For a while \"good first issue\" yielded mostly good results, but now it just seems like an endless stream of requests for getting assigned to an issue. Maybe we should go back to a different label (e.g., \"beginner\") and just outline some instructions for beginners in the README (or through a pointer from there)?","msgtype":"m.text","org.matrix.msc1767.text":"For a while \"good first issue\" yielded mostly good results, but now it just seems like an endless stream of requests for getting assigned to an issue. Maybe we should go back to a different label (e.g., \"beginner\") and just outline some instructions for beginners in the README (or through a pointer from there)?"},"ts":1664006656623,"senderName":"annevk","senderId":"@annevk:matrix.org","id":"$Rdom6K2xCA0TGCFBRjC5rGJT0oQGqKhCOPbaNGMSU8U"},
{"content":{"body":"I suppose we could try to outline some clearer instructions first, but I've the feeling the label contributes due to dedicated GitHub support.","msgtype":"m.text","org.matrix.msc1767.text":"I suppose we could try to outline some clearer instructions first, but I've the feeling the label contributes due to dedicated GitHub support."},"ts":1664006736903,"senderName":"annevk","senderId":"@annevk:matrix.org","id":"$YTIf1xtqukdD6XIeXrtrS0JPuboy_Sdu2KJ4ReKmvxk"},
{"content":{"body":"Help/comments welcome at https://github.com/mdn/content/issues/20730\n\nThe OP asserts _“`preventDefault()` doesn't work in Chrome”_ and objects to MDN marking the `returnValue` property of `Event` as deprecated “despite it being the only working feature across all browsers”.","format":"org.matrix.custom.html","formatted_body":"<p>Help/comments welcome at https://github.com/mdn/content/issues/20730</p>\n<p>The OP asserts <em>“<code>preventDefault()</code> doesn't work in Chrome”</em> and objects to MDN marking the <code>returnValue</code> property of <code>Event</code> as deprecated “despite it being the only working feature across all browsers”.</p>\n","msgtype":"m.text","org.matrix.msc1767.message":[{"body":"Help/comments welcome at https://github.com/mdn/content/issues/20730\n\nThe OP asserts _“`preventDefault()` doesn't work in Chrome”_ and objects to MDN marking the `returnValue` property of `Event` as deprecated “despite it being the only working feature across all browsers”.","mimetype":"text/plain"},{"body":"<p>Help/comments welcome at https://github.com/mdn/content/issues/20730</p>\n<p>The OP asserts <em>“<code>preventDefault()</code> doesn't work in Chrome”</em> and objects to MDN marking the <code>returnValue</code> property of <code>Event</code> as deprecated “despite it being the only working feature across all browsers”.</p>\n","mimetype":"text/html"}]},"ts":1664011402856,"senderName":"sideshowbarker","senderId":"@sideshowbarker:matrix.org","id":"$S3dWbXVywNJPDuo8XsgSf1C-VCGkENMOQJa1e0geWdo"}
]