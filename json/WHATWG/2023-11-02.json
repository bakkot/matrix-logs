[
{"content":{"body":"I have a question regarding `<main>` and `<article>`","format":"org.matrix.custom.html","formatted_body":"I have a question regarding <code>&lt;main&gt;</code> and <code>&lt;article&gt;</code>","m.mentions":{},"msgtype":"m.text"},"ts":1698928959154,"senderName":"bilal adnan","senderId":"@boybilal:matrix.org","id":"$oCVxnzLmOmW6tDG_UnSdaHuDmaSKCw8lwkbQnFYyB74"},
{"content":{"body":"Let's say I have a blog article page. Should the main content be labeled with `<main>` or `<article>`? I personally feel `<main>` is a better choice but I am confused in one thing.\n\n<main> can't be nested inside any <article> element, as per the WHATWG HTML standard, to be **hierarchically correct**. But because the main content of the article should go in <main>, why would anyone want to use <article> for the main content of the article. I guess the HTML standard showcases such an example.","format":"org.matrix.custom.html","formatted_body":"<p>Let's say I have a blog article page. Should the main content be labeled with <code>&lt;main&gt;</code> or <code>&lt;article&gt;</code>? I personally feel <code>&lt;main&gt;</code> is a better choice but I am confused in one thing.</p>\n&lt;main&gt; can't be nested inside any &lt;article&gt; element, as per the WHATWG HTML standard, to be **hierarchically correct**. But because the main content of the article should go in &lt;main&gt;, why would anyone want to use &lt;article&gt; for the main content of the article. I guess the HTML standard showcases such an example.","m.mentions":{},"msgtype":"m.text"},"ts":1698929163808,"senderName":"bilal adnan","senderId":"@boybilal:matrix.org","id":"$UG4BH_MQAWGFA2ICwfT_p8RBx1451aR-q2jxFueadFw"},
{"content":{"body":" * Let's say I have a blog article page. Should the main content be labeled with `<main>` or `<article>`? I personally feel `<main>` is a better choice but I am confused in one thing.\n\n\n`<main>` can't be nested inside any \\<article> element, as per the WHATWG HTML standard, to be **hierarchically correct**. But because the main content of the article should go in \\<main>, why would anyone want to use \\<article> for the main content of the article. I guess the HTML standard showcases such an example.","format":"org.matrix.custom.html","formatted_body":" * <p>Let's say I have a blog article page. Should the main content be labeled with <code>&lt;main&gt;</code> or <code>&lt;article&gt;</code>? I personally feel <code>&lt;main&gt;</code> is a better choice but I am confused in one thing.</p>\n<p><code>&lt;main&gt;</code> can't be nested inside any &lt;article&gt; element, as per the WHATWG HTML standard, to be <strong>hierarchically correct</strong>. But because the main content of the article should go in &lt;main&gt;, why would anyone want to use &lt;article&gt; for the main content of the article. I guess the HTML standard showcases such an example.</p>\n","m.mentions":{},"m.new_content":{"body":"Let's say I have a blog article page. Should the main content be labeled with `<main>` or `<article>`? I personally feel `<main>` is a better choice but I am confused in one thing.\n\n\n`<main>` can't be nested inside any \\<article> element, as per the WHATWG HTML standard, to be **hierarchically correct**. But because the main content of the article should go in \\<main>, why would anyone want to use \\<article> for the main content of the article. I guess the HTML standard showcases such an example.","format":"org.matrix.custom.html","formatted_body":"<p>Let's say I have a blog article page. Should the main content be labeled with <code>&lt;main&gt;</code> or <code>&lt;article&gt;</code>? I personally feel <code>&lt;main&gt;</code> is a better choice but I am confused in one thing.</p>\n<p><code>&lt;main&gt;</code> can't be nested inside any &lt;article&gt; element, as per the WHATWG HTML standard, to be <strong>hierarchically correct</strong>. But because the main content of the article should go in &lt;main&gt;, why would anyone want to use &lt;article&gt; for the main content of the article. I guess the HTML standard showcases such an example.</p>\n","m.mentions":{},"msgtype":"m.text"},"m.relates_to":{"event_id":"$UG4BH_MQAWGFA2ICwfT_p8RBx1451aR-q2jxFueadFw","rel_type":"m.replace"},"msgtype":"m.text"},"ts":1698929188575,"senderName":"bilal adnan","senderId":"@boybilal:matrix.org","id":"$B9Fho5D0zO-b8VrCEpB-xSLbXFAuxnAUU357tBSgg-E"},
{"content":{"body":" * Let's say I have a blog article page. Should the main content be labeled with `<main>` or `<article>`? I personally feel `<main>` is a better choice but I am confused in one thing.\n\n`<main>` can't be nested inside any \\<article> element, as per the WHATWG HTML standard, to be **hierarchically correct**. But because the main content of the article should go in `<main>`, why would anyone want to use `<article>` for the main content of the article. I guess the HTML standard showcases such an example.","format":"org.matrix.custom.html","formatted_body":" * <p>Let's say I have a blog article page. Should the main content be labeled with <code>&lt;main&gt;</code> or <code>&lt;article&gt;</code>? I personally feel <code>&lt;main&gt;</code> is a better choice but I am confused in one thing.</p>\n<p><code>&lt;main&gt;</code> can't be nested inside any &lt;article&gt; element, as per the WHATWG HTML standard, to be <strong>hierarchically correct</strong>. But because the main content of the article should go in <code>&lt;main&gt;</code>, why would anyone want to use <code>&lt;article&gt;</code> for the main content of the article. I guess the HTML standard showcases such an example.</p>\n","m.mentions":{},"m.new_content":{"body":"Let's say I have a blog article page. Should the main content be labeled with `<main>` or `<article>`? I personally feel `<main>` is a better choice but I am confused in one thing.\n\n`<main>` can't be nested inside any \\<article> element, as per the WHATWG HTML standard, to be **hierarchically correct**. But because the main content of the article should go in `<main>`, why would anyone want to use `<article>` for the main content of the article. I guess the HTML standard showcases such an example.","format":"org.matrix.custom.html","formatted_body":"<p>Let's say I have a blog article page. Should the main content be labeled with <code>&lt;main&gt;</code> or <code>&lt;article&gt;</code>? I personally feel <code>&lt;main&gt;</code> is a better choice but I am confused in one thing.</p>\n<p><code>&lt;main&gt;</code> can't be nested inside any &lt;article&gt; element, as per the WHATWG HTML standard, to be <strong>hierarchically correct</strong>. But because the main content of the article should go in <code>&lt;main&gt;</code>, why would anyone want to use <code>&lt;article&gt;</code> for the main content of the article. I guess the HTML standard showcases such an example.</p>\n","m.mentions":{},"msgtype":"m.text"},"m.relates_to":{"event_id":"$UG4BH_MQAWGFA2ICwfT_p8RBx1451aR-q2jxFueadFw","rel_type":"m.replace"},"msgtype":"m.text"},"ts":1698929199762,"senderName":"bilal adnan","senderId":"@boybilal:matrix.org","id":"$m3ULro-TvnUr-citCOiJeJ4eS8LaP2jX0CqFIK37xzg"},
{"content":{"body":" * Let's say I have a blog article page. Should the main content be labeled with `<main>` or `<article>`? I personally feel `<main>` is a better choice but I am confused in one thing.\n\nWhy would anyone want to use `<article>` for the main content of the article. I guess the HTML standard showcases such an example and that confuses me.","format":"org.matrix.custom.html","formatted_body":" * <p>Let's say I have a blog article page. Should the main content be labeled with <code>&lt;main&gt;</code> or <code>&lt;article&gt;</code>? I personally feel <code>&lt;main&gt;</code> is a better choice but I am confused in one thing.</p>\n<p>Why would anyone want to use <code>&lt;article&gt;</code> for the main content of the article. I guess the HTML standard showcases such an example and that confuses me.</p>\n","m.mentions":{},"m.new_content":{"body":"Let's say I have a blog article page. Should the main content be labeled with `<main>` or `<article>`? I personally feel `<main>` is a better choice but I am confused in one thing.\n\nWhy would anyone want to use `<article>` for the main content of the article. I guess the HTML standard showcases such an example and that confuses me.","format":"org.matrix.custom.html","formatted_body":"<p>Let's say I have a blog article page. Should the main content be labeled with <code>&lt;main&gt;</code> or <code>&lt;article&gt;</code>? I personally feel <code>&lt;main&gt;</code> is a better choice but I am confused in one thing.</p>\n<p>Why would anyone want to use <code>&lt;article&gt;</code> for the main content of the article. I guess the HTML standard showcases such an example and that confuses me.</p>\n","m.mentions":{},"msgtype":"m.text"},"m.relates_to":{"event_id":"$UG4BH_MQAWGFA2ICwfT_p8RBx1451aR-q2jxFueadFw","rel_type":"m.replace"},"msgtype":"m.text"},"ts":1698929513133,"senderName":"bilal adnan","senderId":"@boybilal:matrix.org","id":"$tKeSrsbqJ2_FinK-8XeTTUvgZO-iedqGOGdZv_EVfCI"},
{"content":{"body":"sideshowbarker: sorry, I didn't realize lang-related changes around `slot` were also being reverted","format":"org.matrix.custom.html","formatted_body":"<a href=\"https://matrix.to/#/@sideshowbarker:matrix.org\">sideshowbarker</a>: sorry, I didn't realize lang-related changes around <code>slot</code> were also being reverted","m.mentions":{"user_ids":["@sideshowbarker:matrix.org"]},"msgtype":"m.text"},"ts":1698936807095,"senderName":"annevk","senderId":"@annevk:matrix.org","id":"$d6p6pIkdxVb37KGzop61fD8edJgIBR_Ug6cB8-iRVhk"},
{"content":{"body":"> <@annevk:matrix.org> sideshowbarker: sorry, I didn't realize lang-related changes around `slot` were also being reverted\n\nNo worries — it gave me a chance to go in and read through the spec PRs and related issue comments, and to figure out what it looked like in some actual code. So it was some good learning for me, and I reckon what I learned from it is very likely to be useful for me with some other patch later","format":"org.matrix.custom.html","formatted_body":"<mx-reply><blockquote><a href=\"https://matrix.to/#/!AGetWbsMpFPdSgUrbs:matrix.org/$d6p6pIkdxVb37KGzop61fD8edJgIBR_Ug6cB8-iRVhk?via=matrix.org&via=mozilla.org&via=igalia.com\">In reply to</a> <a href=\"https://matrix.to/#/@annevk:matrix.org\">@annevk:matrix.org</a><br /><a href=\"https://matrix.to/#/@sideshowbarker:matrix.org\">sideshowbarker</a>: sorry, I didn't realize lang-related changes around <code>slot</code> were also being reverted</blockquote></mx-reply>No worries — it gave me a chance to go in and read through the spec PRs and related issue comments, and to figure out what it looked like in some actual code. So it was some good learning for me, and I reckon what I learned from it is very likely to be useful for me with some other patch later","m.relates_to":{"m.in_reply_to":{"event_id":"$d6p6pIkdxVb37KGzop61fD8edJgIBR_Ug6cB8-iRVhk"}},"msgtype":"m.text"},"ts":1698955543857,"senderName":"sideshowbarker","senderId":"@sideshowbarker:matrix.org","id":"$VM2NmPoXiaWwPH0ADlWXK1FDW-fwUVtc6UbM0EcZ-28"},
{"content":{"body":"So I’m looking at the test at https://github.com/WebKit/WebKit/blob/main/LayoutTests/http/tests/security/xss-DENIED-script-inject-into-inactive-window2.html#L42 and trying to figure out some way to detect that the “victim” content has loaded — other than by checking `location.href`\n\nWhat that tests does is this:\n1. Creates an `a` link\n2. Sets the link’s `target` to `_blank`\n3. Sets the link’s `rel` value to opener\n4. Sets the link’s `href` to `?actually-attack`\n5. Clicks the link (with `link.click()`)\n\nThe, after the navigation to `?actually-attack` happens:\n\n1. Creates another `a` link\n2. Sets the link’s `target` to `_self`\n3. Sets the link’s `href` to `http://localhost:8000/security/resources/innocent-victim.html`\n4. Clicks the link (with `link.click()`)\n\nSo I want to figure out a way to detect when the `http://localhost:8000/security/resources/innocent-victim.html` content has loaded.\n\n","format":"org.matrix.custom.html","formatted_body":"<p>So I’m looking at the test at https://github.com/WebKit/WebKit/blob/main/LayoutTests/http/tests/security/xss-DENIED-script-inject-into-inactive-window2.html#L42 and trying to figure out some way to detect that the “victim” content has loaded — other than by checking <code>location.href</code></p>\n<p>What that tests does is this:</p>\n<ol>\n<li>Creates an <code>a</code> link</li>\n<li>Sets the link’s <code>target</code> to <code>_blank</code></li>\n<li>Sets the link’s <code>rel</code> value to opener</li>\n<li>Sets the link’s <code>href</code> to <code>?actually-attack</code></li>\n<li>Clicks the link (with <code>link.click()</code>)</li>\n</ol>\n<p>The, after the navigation to <code>?actually-attack</code> happens:</p>\n<ol>\n<li>Creates another <code>a</code> link</li>\n<li>Sets the link’s <code>target</code> to <code>_self</code></li>\n<li>Sets the link’s <code>href</code> to <code>http://localhost:8000/security/resources/innocent-victim.html</code></li>\n<li>Clicks the link (with <code>link.click()</code>)</li>\n</ol>\n<p>So I want to figure out a way to detect when the <code>http://localhost:8000/security/resources/innocent-victim.html</code> content has loaded.</p>\n","m.mentions":{},"msgtype":"m.text"},"ts":1698964458458,"senderName":"sideshowbarker","senderId":"@sideshowbarker:matrix.org","id":"$amntOCT5zORbGLzVhdrs_PZ8HEVEBfAww8xbOWM8QsM"}
]