[
{"content":{"body":"**Quick question:** If the [ownerDocument](https://dom.spec.whatwg.org/#dom-node-ownerdocument) is to return null if it is a document or document if it is another node type, why does the number 4 of [insert a character](https://html.spec.whatwg.org/multipage/parsing.html#insert-a-character)  says the following:\n> Otherwise, create a new `Text node` whose data is data and **whose node document is the same as that of the element in which the adjusted insertion location finds itself**, and insert the newly created node at the adjusted insertion location.\n\nI am referring to the bolded part above, why do I need to use the document of the adjusted location in the `Text node` yet again if each node has an associated node document set upon creation, wouldn't that be superfluous since they likely point to the same document anyway?","format":"org.matrix.custom.html","formatted_body":"<p><strong>Quick question:</strong> If the <a href=\"https://dom.spec.whatwg.org/#dom-node-ownerdocument\">ownerDocument</a> is to return null if it is a document or document if it is another node type, why does the number 4 of <a href=\"https://html.spec.whatwg.org/multipage/parsing.html#insert-a-character\">insert a character</a>  says the following:</p>\n<blockquote>\n<p>Otherwise, create a new <code>Text node</code> whose data is data and <strong>whose node document is the same as that of the element in which the adjusted insertion location finds itself</strong>, and insert the newly created node at the adjusted insertion location.</p>\n</blockquote>\n<p>I am referring to the bolded part above, why do I need to use the document of the adjusted location in the <code>Text node</code> yet again if each node has an associated node document set upon creation, wouldn't that be superfluous since they likely point to the same document anyway?</p>\n","msgtype":"m.text"},"ts":1639191070891,"senderName":"devsrealmguy","senderId":"@devsrealmguy:matrix.org","id":"$zMa48WLWvjx0nKb2AT2bKFU6AFSrxqUq8n-C2Joj3FE"},
{"content":{"body":" * **Quick question:** If the [ownerDocument](https://dom.spec.whatwg.org/#dom-node-ownerdocument) is to return null if it is a document or document if it is another node type, why does the number 4 of [insert a character](https://html.spec.whatwg.org/multipage/parsing.html#insert-a-character)  says the following:\n> Otherwise, create a new `Text node` whose data is data and **whose node document is the same as that of the element in which the adjusted insertion location finds itself**, and insert the newly created node at the adjusted insertion location.\n\nI am referring to the bolded part above, why do I need to use the document of the adjusted location in the `Text node` yet again if each node has an associated node document set upon creation, wouldn't that be superfluous since they likely point to the same document anyway?","format":"org.matrix.custom.html","formatted_body":" * <p><strong>Quick question:</strong> If the <a href=\"https://dom.spec.whatwg.org/#dom-node-ownerdocument\">ownerDocument</a> is to return null if it is a document or document if it is another node type, why does the number 4 of <a href=\"https://html.spec.whatwg.org/multipage/parsing.html#insert-a-character\">insert a character</a>  says the following:</p>\n<blockquote>\n<p>Otherwise, create a new <code>Text node</code> whose data is data and <strong>whose node document is the same as that of the element in which the adjusted insertion location finds itself</strong>, and insert the newly created node at the adjusted insertion location.</p>\n</blockquote>\n<p>I am referring to the bolded part above, why do I need to use the document of the adjusted location in the <code>Text node</code> yet again if each node has an associated node document set upon creation, wouldn't that be superfluous since they likely point to the same document anyway?</p>\n","m.new_content":{"body":"**Quick question:** If the [ownerDocument](https://dom.spec.whatwg.org/#dom-node-ownerdocument) is to return null if it is a document or document if it is another node type, why does the number 4 of [insert a character](https://html.spec.whatwg.org/multipage/parsing.html#insert-a-character)  says the following:\n> Otherwise, create a new `Text node` whose data is data and **whose node document is the same as that of the element in which the adjusted insertion location finds itself**, and insert the newly created node at the adjusted insertion location.\n\nI am referring to the bolded part above, why do I need to use the document of the adjusted location in the `Text node` yet again if each node has an associated node document set upon creation, wouldn't that be superfluous since they likely point to the same document anyway?","format":"org.matrix.custom.html","formatted_body":"<p><strong>Quick question:</strong> If the <a href=\"https://dom.spec.whatwg.org/#dom-node-ownerdocument\">ownerDocument</a> is to return null if it is a document or document if it is another node type, why does the number 4 of <a href=\"https://html.spec.whatwg.org/multipage/parsing.html#insert-a-character\">insert a character</a>  says the following:</p>\n<blockquote>\n<p>Otherwise, create a new <code>Text node</code> whose data is data and <strong>whose node document is the same as that of the element in which the adjusted insertion location finds itself</strong>, and insert the newly created node at the adjusted insertion location.</p>\n</blockquote>\n<p>I am referring to the bolded part above, why do I need to use the document of the adjusted location in the <code>Text node</code> yet again if each node has an associated node document set upon creation, wouldn't that be superfluous since they likely point to the same document anyway?</p>\n","msgtype":"m.text"},"m.relates_to":{"event_id":"$zMa48WLWvjx0nKb2AT2bKFU6AFSrxqUq8n-C2Joj3FE","rel_type":"m.replace"},"msgtype":"m.text"},"ts":1639191099511,"senderName":"devsrealmguy","senderId":"@devsrealmguy:matrix.org","id":"$_R6L23mZ2-uwt9KUGJW95pgAr0H2fbzs8KDC8LFUF2Q"},
{"content":{"body":"hello i was going trough the code of the ZenithVR website (looking for hidden key) and found this convo, what are you guys doing here ?","msgtype":"m.text"},"ts":1639262206069,"senderName":"naghito","senderId":"@naghito:matrix.org","id":"$gJMSdWjjc4WtUtwtTMqyXNC8hO3p_SwK3EFCoxzjE8M"},
{"content":{"body":"wow, that seems like a stretch lol (some of us aren't guys btw)","msgtype":"m.text"},"ts":1639266354610,"senderName":"DerekNonGeneric","senderId":"@dereknongeneric:mozilla.org","id":"$sNMxz_IIMI6AHLaBtMqBItnfXuTa0sJwBbvlEWi4NOs"},
{"content":{"body":"that website nearly crashed the my tab; they must be trying to run VR in the browser or something","msgtype":"m.text"},"ts":1639266401576,"senderName":"DerekNonGeneric","senderId":"@dereknongeneric:mozilla.org","id":"$KdD9Q3JMTPm-3pmPwlZSWlxfmM4i1kN8xMxfvE3I758"},
{"content":{"body":"to answer the question of \"what are we doing here\" -- not much, it's kinda the weekend and nobody is all that very talkative -- either that or they don't want to let me in on their favorite HTML5 formatter (altho HTML Tidy is a tough act to follow)","msgtype":"m.text"},"ts":1639266616652,"senderName":"DerekNonGeneric","senderId":"@dereknongeneric:mozilla.org","id":"$zprCQ2i-TG8LRU72lNi0lzzudHtYcCgxYHMmMJItcd8"}
]